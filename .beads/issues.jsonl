{"id":"chatterbox-tts-lite-sy6","title":"Fix TTS server Docker image crash on startup","description":"minimal_server.py imports audio.encoding and files modules not present in the Docker image, causing ModuleNotFoundError on startup. Pod never becomes ready. Fix: inline the two small functions (encode_to_wav_bytes, validate_voice_path) directly into minimal_server.py.","status":"closed","priority":0,"issue_type":"bug","assignee":"Codex","owner":"codex@local","created_at":"2026-04-18T14:14:29Z","created_by":"Codex","updated_at":"2026-04-18T14:15:02Z","started_at":"2026-04-18T14:14:47Z","closed_at":"2026-04-18T14:15:02Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-91a","title":"Update api.ts types for music_params, sfx_params, generate_sfx","description":"Add MusicStepParams, SfxStepParams interfaces and add music_params, sfx_params, generate_sfx fields to CreateWorkflowRequest in api.ts.","status":"in_progress","priority":1,"issue_type":"task","assignee":"shubham","owner":"shubh67678@gmail.com","created_at":"2026-04-25T18:34:29Z","created_by":"shubham","updated_at":"2026-04-25T18:44:05Z","started_at":"2026-04-25T18:36:37Z","dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"chatterbox-tts-lite-3v3","title":"Replace hardcoded workflow form with schema-driven rendering","description":"In workflows.ts: fetch schema on mount, replace hardcoded checkboxes/slider with rendered schema sections. Keep premise textarea and voice selector. Update handleCreate to use collectParams() and merge step params into the request.","status":"open","priority":1,"issue_type":"task","owner":"shubh67678@gmail.com","created_at":"2026-04-25T18:34:27Z","created_by":"shubham","updated_at":"2026-04-25T18:34:27Z","dependencies":[{"issue_id":"chatterbox-tts-lite-3v3","depends_on_id":"chatterbox-tts-lite-520","type":"blocks","created_at":"2026-04-25T18:34:46Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-3v3","depends_on_id":"chatterbox-tts-lite-7xw","type":"blocks","created_at":"2026-04-25T18:34:43Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-3v3","depends_on_id":"chatterbox-tts-lite-91a","type":"blocks","created_at":"2026-04-25T18:34:44Z","created_by":"shubham","metadata":"{}"}],"dependency_count":3,"dependent_count":1,"comment_count":0}
{"id":"chatterbox-tts-lite-7xw","title":"Create schema-form.ts — schema-driven form renderer + param collector","description":"New frontend module (~100 LOC) that takes StepParamSchemaEntry[], renders collapsible details sections per step with auto-generated inputs (bool→checkbox, int with min/max→range, string→text, enum→select), and exports collectParams() to gather values into Record\u0026lt;string, Record\u0026lt;string, unknown\u0026gt;\u0026gt;.","status":"in_progress","priority":1,"issue_type":"feature","assignee":"shubham","owner":"shubh67678@gmail.com","created_at":"2026-04-25T18:34:24Z","created_by":"shubham","updated_at":"2026-04-25T18:38:47Z","started_at":"2026-04-25T18:36:41Z","dependencies":[{"issue_id":"chatterbox-tts-lite-7xw","depends_on_id":"chatterbox-tts-lite-ccw","type":"blocks","created_at":"2026-04-25T18:34:40Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"chatterbox-tts-lite-ccw","title":"Add SfxStepParams model and wire music/sfx params_schema on StepDefs","description":"Backend: Create SfxStepParams(BaseStepParams) in json_schemas.py, add sfx_params field to WorkflowInputSchema, and wire params_schema/params_field on music_generation and sfx_generation StepDefs in content_pipeline.py.","status":"closed","priority":1,"issue_type":"task","assignee":"shubham","owner":"shubh67678@gmail.com","created_at":"2026-04-25T18:34:19Z","created_by":"shubham","updated_at":"2026-04-25T18:36:28Z","started_at":"2026-04-25T18:34:58Z","closed_at":"2026-04-25T18:36:28Z","close_reason":"Added SfxStepParams model, sfx_params field on WorkflowInputSchema, wired params_schema/params_field on music_generation and sfx_generation StepDefs, updated sfx_generation.py to use sfx_params.enabled","dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"chatterbox-tts-lite-6gc","title":"Create generate-story Claude Code skill","description":"Create `.claude/skills/generate-story/SKILL.md` — a self-contained Claude Code skill that writes horror stories optimized for audio narration.\n\n**Skill flow:**\n1. Generate a fresh premise (or accept one via $ARGUMENTS)\n2. Optionally search web for real-world inspiration\n3. Write full story (2000-4000 words) optimized for audio narration\n4. Self-review: pacing, prose quality, horror effectiveness, audio rhythm\n5. Revise: fix weak sections, trim bloat, strengthen atmosphere\n6. Save to `output/stories/YYYY-MM-DD-title.md` with YAML frontmatter\n7. Push to server via `curl POST /api/stories/ingest`\n\n**Creative constraints (from NARRATOR_VOICE_RULES):**\n- Single old male narrator, first-person past tense\n- No dialogue from other characters\n- Short paragraphs (2-4 sentences) for audio rhythm\n- Sound-based descriptions over visual\n- Lingering unease over jump scares\n- Slow escalation, routine before horror","acceptance_criteria":"- [ ] `.claude/skills/generate-story/SKILL.md` exists and is a valid Claude Code skill\n- [ ] Skill accepts optional premise via $ARGUMENTS\n- [ ] Output includes YAML frontmatter (title, premise, date, word_count)\n- [ ] Story saved to `output/stories/YYYY-MM-DD-title.md`\n- [ ] Skill calls `curl POST /api/stories/ingest` to push to server\n- [ ] All creative constraints from NARRATOR_VOICE_RULES are embedded in the skill\n- [ ] Running `/generate-story` interactively produces a story saved locally and pushed to server","status":"closed","priority":1,"issue_type":"feature","assignee":"shubham","owner":"shubh67678@gmail.com","created_at":"2026-04-25T17:04:02Z","created_by":"shubham","updated_at":"2026-04-25T17:22:38Z","started_at":"2026-04-25T17:15:40Z","closed_at":"2026-04-25T17:22:38Z","close_reason":"Completed","dependencies":[{"issue_id":"chatterbox-tts-lite-6gc","depends_on_id":"chatterbox-tts-lite-3j6","type":"blocks","created_at":"2026-04-25T17:04:13Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"chatterbox-tts-lite-3j6","title":"Add POST /api/stories/ingest endpoint","description":"Add a lightweight endpoint that stores pre-written story text directly into the DB with COMPLETED status, bypassing the full TTS/image/video pipeline.\n\n**Files to modify:**\n- `services/creepy-brain/app/schemas/story.py` — Add `IngestStoryRequest` model\n- `services/creepy-brain/app/routes/stories.py` — Add `ingest_story()` route\n- `services/creepy-brain/app/services/story_service.py` — Add `ingest()` method\n\n**Schema:** IngestStoryRequest with title (min 1 char), premise (min 10 chars), full_text (min 100 chars).\n\n**Route:** POST /api/stories/ingest → Creates Story with COMPLETED status, calculates word_count, returns StoryResponse.","acceptance_criteria":"- [ ] `IngestStoryRequest` Pydantic model exists in schemas/story.py with title, premise, full_text fields and min_length validators\n- [ ] `POST /api/stories/ingest` route exists and returns StoryResponse\n- [ ] Story is created with status=COMPLETED and correct word_count\n- [ ] Returns 422 on invalid input (missing fields, too short)\n- [ ] `python3 -m mypy services/creepy-brain/app/routes/stories.py services/creepy-brain/app/schemas/story.py services/creepy-brain/app/services/story_service.py --strict` passes with zero errors\n- [ ] curl test: `curl -X POST http://localhost:8000/api/stories/ingest -H 'Content-Type: application/json' -d '{\"title\":\"Test\",\"premise\":\"A test premise for verification\",\"full_text\":\"\u003c100+ chars\u003e\"}'` returns story_id","status":"closed","priority":1,"issue_type":"feature","assignee":"shubham","owner":"shubh67678@gmail.com","created_at":"2026-04-25T17:03:55Z","created_by":"shubham","updated_at":"2026-04-25T17:14:12Z","started_at":"2026-04-25T17:06:33Z","closed_at":"2026-04-25T17:14:12Z","close_reason":"Completed","dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"chatterbox-tts-lite-5qn","title":"Mix music bed and SFX into final video output in stitch step","description":"## What\n\nThe `music_generation` and `sfx_generation` steps already produce audio blobs stored in the DB (`MUSIC_BED`, `SFX_AUDIO`), but the `stitch_final` step never reads or mixes them. The final MP3/video output contains only the narration (TTS). This bead wires the music bed and SFX clips into the final output.\n\n## Why\n\nBackground music is the headline feature being tested. Without mixing, enabling `music_params.enabled` has no audible effect on the final output — making the feature effectively invisible to end users.\n\n## How\n\n### 1. Update `stitch.py` to load music and SFX blobs\n\nIn `StitchStepParams` or the step runner, read outputs from sibling steps:\n- `music_generation` → `MusicGenerationStepOutput.music_bed_blob_id` → load `MUSIC_BED` WAV blob\n- `sfx_generation` → list of `SFX_AUDIO` blobs with position metadata\n\n### 2. Mix music bed under narration\n\nUse `pydub` (already a dep) to:\n1. Load the full narration MP3 and the music bed WAV\n2. Trim or loop the music bed to match narration duration\n3. Apply volume ducking: music at `settings.music_volume_db` (-12 dB default) under narration\n4. Overlay and export as final MP3\n\n```python\nfrom pydub import AudioSegment\n\nnarration = AudioSegment.from_mp3(...)\nmusic = AudioSegment.from_wav(...) + settings.music_volume_db  # duck\n# Loop music if shorter than narration\nwhile len(music) \u003c len(narration):\n    music = music + music\nmusic = music[:len(narration)]\nmixed = music.overlay(narration)\n```\n\n### 3. Mix SFX cues at correct positions\n\nFor each SFX blob, read position metadata from `mime_type` field (already stored as JSON-encoded string). Place each clip at the correct time offset within the narration timeline before overlaying.\n\n### 4. Sync music to video (if stitching)\n\nIf `stitch_params.enabled` and a video is produced, use `ffmpeg` to replace the video audio track with the mixed audio:\n\n```bash\nffmpeg -i video.mp4 -i mixed_audio.mp3 -c:v copy -c:a aac -map 0:v:0 -map 1:a:0 output.mp4\n```\n\n### 5. Store result\n\nSave mixed audio as a new blob type `FINAL_AUDIO_MIX` (or reuse `FINAL_AUDIO`) and update the workflow result's `final_audio_blob_id`.\n\n## Acceptance Criteria\n\n- [ ] Running a workflow with `music_params.enabled: true` produces a final MP3/video where background music is audible under the narration\n- [ ] Music is ducked to -12 dB (configurable via `settings.music_volume_db`)\n- [ ] If music bed is shorter than narration, it loops seamlessly\n- [ ] If music is disabled, stitch behaves identically to today (no regression)\n- [ ] SFX clips (if generated) are placed at correct time positions\n- [ ] Final video's audio track contains the mixed output\n\n## Key Files\n\n- `services/creepy-brain/app/workflows/steps/stitch.py` — add mixing logic here\n- `services/creepy-brain/app/config.py` — `music_volume_db`, `sfx_volume_db` already defined\n- `services/creepy-brain/app/models/enums.py` — add `FINAL_AUDIO_MIX` blob type if needed\n- `services/creepy-brain/app/workflows/content_pipeline.py` — ensure stitch step receives sibling outputs\n","acceptance_criteria":"- Running a workflow with music_params.enabled: true produces a final MP3/video where background music is audible under the narration\n- Music is ducked to -12 dB (configurable via settings.music_volume_db)\n- If music bed is shorter than narration, it loops seamlessly\n- If music is disabled, stitch behaves identically to today (no regression)\n- SFX clips placed at correct time positions if generated\n- Final video audio track contains the mixed output","status":"closed","priority":1,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-23T20:05:09Z","created_by":"Codex","updated_at":"2026-04-23T20:19:25Z","started_at":"2026-04-23T20:05:45Z","closed_at":"2026-04-23T20:19:25Z","close_reason":"Implemented: music bed and SFX mixed into stitch_final. All adversarial findings addressed (retry safety, SR resampling, SfxGenerationStepOutput now persisted in StepOutputSchema, partial-TTS guard).","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-52l","title":"Add Alembic migration for audio design enum values","description":"Create Alembic migration to add new enum values to Postgres.\n\n**File:** `services/creepy-brain/alembic/versions/0016_add_audio_design.py`\n\n**Migration SQL:**\n```python\ndef upgrade() -\u003e None:\n    op.execute(\"ALTER TYPE stepname ADD VALUE IF NOT EXISTS 'music_generation'\")\n    op.execute(\"ALTER TYPE stepname ADD VALUE IF NOT EXISTS 'sfx_generation'\")\n    op.execute(\"ALTER TYPE blobtype ADD VALUE IF NOT EXISTS 'music_audio'\")\n    op.execute(\"ALTER TYPE blobtype ADD VALUE IF NOT EXISTS 'sfx_audio'\")\n    op.execute(\"ALTER TYPE blobtype ADD VALUE IF NOT EXISTS 'music_bed'\")\n```\n\n**Note:** `ADD VALUE IF NOT EXISTS` is idempotent and cannot be rolled back in Postgres. The downgrade should be a no-op with a comment explaining this Postgres limitation.","acceptance_criteria":"- Migration runs cleanly on fresh and existing databases\n- Enum values match exactly what Python enums define\n- Migration filename follows existing numbering convention\n- downgrade() is a documented no-op","status":"closed","priority":1,"issue_type":"task","assignee":"Codex","owner":"codex@local","created_at":"2026-04-23T12:24:54Z","created_by":"Codex","updated_at":"2026-04-23T17:52:09Z","started_at":"2026-04-23T17:41:06Z","closed_at":"2026-04-23T17:52:09Z","close_reason":"Completed: migration 0017 created with reversible downgrade","labels":["audio-design","database","phase-7"],"dependencies":[{"issue_id":"chatterbox-tts-lite-52l","depends_on_id":"chatterbox-tts-lite-ycw","type":"blocks","created_at":"2026-04-23T12:26:30Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-yfh","title":"Update pipeline DAG and JSON schemas for audio design","description":"Wire audio design steps into the content pipeline DAG and update exports.\n\n**ALREADY DONE:**\n- `app/models/json_schemas.py` — MusicGenerationStepOutput, SfxGenerationStepOutput added\n- `app/models/enums.py` — MUSIC_GENERATION, SFX_GENERATION added\n- `app/workflows/steps/music_generation.py` — implemented\n- `app/workflows/steps/sfx_generation.py` — implemented\n\n**REMAINING:**\n\n**`app/workflows/content_pipeline.py`:**\n- Add `music_generation` step (parents=[\"tts_synthesis\"], timeout=3600, max_retries=2)\n- Add `sfx_generation` step (parents=[\"tts_synthesis\"], timeout=3600, max_retries=2)\n- Update `stitch_final` parents to include `[\"image_generation\", \"music_generation\", \"sfx_generation\"]`\n- Update `_on_pipeline_complete` to extract music/sfx outputs and store blob IDs\n\n**`app/workflows/steps/__init__.py`:**\n- Export music_generation and sfx_generation step modules\n\n**New DAG structure:**\n```\ngenerate_story → tts_synthesis → music_generation  ──┐\n                               → sfx_generation    ──┤\n                               → image_generation  ──┤\n                                                     └→ stitch_final → waveform_overlay\n```","acceptance_criteria":"- mypy --strict passes on all modified files\n- DAG topo-sort produces correct parallel execution order\n- WorkflowInputSchema accepts generate_music/generate_sfx flags\n- StepOutputSchema union includes new output types\n- _on_pipeline_complete extracts music_bed_blob_id\n- Backward compatible: existing workflows without music/sfx still work","status":"closed","priority":1,"issue_type":"task","assignee":"Codex","owner":"codex@local","created_at":"2026-04-23T12:24:48Z","created_by":"Codex","updated_at":"2026-04-23T19:17:53Z","started_at":"2026-04-23T18:59:21Z","closed_at":"2026-04-23T19:17:53Z","close_reason":"DAG wired: music_generation and sfx_generation steps added after tts_synthesis; stitch_final updated to wait on all three; _on_pipeline_complete extracts music_bed_blob_id; WorkflowResultSchema updated; steps/__init__.py exports added. mypy --strict passes. Adversarial review finding fixed (embeddeddolt/ now gitignored).","labels":["audio-design","phase-6","pipeline"],"dependencies":[{"issue_id":"chatterbox-tts-lite-yfh","depends_on_id":"chatterbox-tts-lite-4dz","type":"blocks","created_at":"2026-04-23T12:26:20Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-yfh","depends_on_id":"chatterbox-tts-lite-evk","type":"blocks","created_at":"2026-04-23T12:26:15Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-yfh","depends_on_id":"chatterbox-tts-lite-hkq","type":"blocks","created_at":"2026-04-23T12:26:25Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-yfh","depends_on_id":"chatterbox-tts-lite-ycw","type":"blocks","created_at":"2026-04-23T12:26:10Z","created_by":"shubham","metadata":"{}"}],"dependency_count":4,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-hkq","title":"Add audio mixing to stitch pipeline step","description":"Modify `services/creepy-brain/app/workflows/steps/stitch.py` to mix music bed and SFX clips into the final audio.\n\n**Changes to `execute()`:**\n1. After stitching narration WAV, check for `music_generation` parent output\n2. If music bed exists: load `MUSIC_BED` blob, resample to match narration sample rate\n3. Apply volume reduction (`music_volume_db`, default -12dB)\n4. Mix: `narration + music_bed * gain`\n5. Check for `sfx_generation` parent output\n6. If SFX clips exist: load each `SFX_AUDIO` blob, place at correct timestamp based on scene timing + position (beginning/middle/end)\n7. Apply SFX volume (`sfx_volume_db`, default -6dB)\n8. Mix SFX into combined audio\n9. Encode final mixed audio to MP3\n\n**New helper function:**\n```python\ndef _mix_audio_layers(\n    narration: np.ndarray,\n    music_bed: np.ndarray | None,\n    sfx_clips: list[tuple[np.ndarray, int]],  # (audio, sample_offset)\n    sample_rate: int,\n    music_gain_db: float = -12.0,\n    sfx_gain_db: float = -6.0,\n) -\u003e np.ndarray:\n```\n\n**Key:** Must be backward-compatible — if no music/sfx parent outputs exist, stitch behaves exactly as before.","acceptance_criteria":"- mypy --strict passes\n- Backward compatible: no music/sfx outputs → identical behavior to current stitch\n- Music bed mixed at -12dB under narration\n- SFX clips placed at correct timestamps based on scene position\n- SFX mixed at -6dB\n- Final MP3 encoding works with mixed audio\n- No clipping in mixed output (peak normalization if needed)","status":"closed","priority":1,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-23T12:24:39Z","created_by":"Codex","updated_at":"2026-04-23T18:38:17Z","started_at":"2026-04-23T18:22:43Z","closed_at":"2026-04-23T18:38:17Z","close_reason":"Implemented _mix_audio_layers, _load_and_mix, and supporting helpers in stitch.py. Music bed mixed at -12dB, SFX at -6dB with correct scene timing. Backward compatible when no parent outputs. Fixed stale resume bug (force re-encode when audio design present) and partial TTS alignment bug (skip mix when TTS is partial).","labels":["audio-design","phase-5","pipeline-step"],"dependencies":[{"issue_id":"chatterbox-tts-lite-hkq","depends_on_id":"chatterbox-tts-lite-4dz","type":"blocks","created_at":"2026-04-23T12:26:06Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-hkq","depends_on_id":"chatterbox-tts-lite-evk","type":"blocks","created_at":"2026-04-23T12:26:01Z","created_by":"shubham","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"id":"chatterbox-tts-lite-4dz","title":"Implement sfx_generation pipeline step","description":"Create `services/creepy-brain/app/workflows/steps/sfx_generation.py` — pipeline step for sound effect generation.\n\n**Logic:**\n1. Check `input.generate_sfx` flag → `SkippedStepOutput` if false\n2. Get chunks from DB, group into scenes\n3. Resume check: skip scenes with existing `SFX_AUDIO` blobs\n4. Generate SFX cue lists via LLM (before GPU spin-up)\n5. Spin up sfx-server GPU pod via `workflow_gpu_pod`\n6. For each scene's cues: `POST /generate` with SFX description\n7. Store each SFX clip as `BlobType.SFX_AUDIO` blob\n\n**Output model:**\n```python\nclass SfxGenerationStepOutput(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    step_type: Literal[\"sfx_generation\"] = \"sfx_generation\"\n    pod_id: str\n    clip_count: int\n    clips: list[SfxClipResult]\n```\n\nWhere `SfxClipResult` has: scene_index, cue_index, description, blob_id, duration_sec, position.\n\n**Pattern:** Follow existing step patterns (image_generation.py) — StepContext, workflow_gpu_pod, blob storage.","acceptance_criteria":"- mypy --strict passes\n- Sub-unit resume works (skips already-generated scenes)\n- All SFX clips stored as BlobType.SFX_AUDIO\n- SkippedStepOutput returned when generate_sfx=false\n- Position metadata (beginning/middle/end) preserved for stitch step","status":"closed","priority":1,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-23T12:24:19Z","created_by":"Codex","updated_at":"2026-04-23T15:10:15Z","started_at":"2026-04-23T14:54:03Z","closed_at":"2026-04-23T15:10:15Z","close_reason":"Implemented sfx_generation.py step with LLM cue planning, hash-verified resume, GPU pod spin-up, WAV validation, and Alembic migration 0016 for sfx_audio enum values.","labels":["audio-design","phase-4","pipeline-step"],"dependencies":[{"issue_id":"chatterbox-tts-lite-4dz","depends_on_id":"chatterbox-tts-lite-y4q","type":"blocks","created_at":"2026-04-23T12:25:56Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-4dz","depends_on_id":"chatterbox-tts-lite-ycw","type":"blocks","created_at":"2026-04-23T12:25:51Z","created_by":"shubham","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"id":"chatterbox-tts-lite-evk","title":"Implement music_generation pipeline step","description":"Create `services/creepy-brain/app/workflows/steps/music_generation.py` — pipeline step for background music generation.\n\n**Logic:**\n1. Check `input.generate_music` flag → `SkippedStepOutput` if false\n2. Get chunks from DB (same as image step)\n3. Group into scenes (reuse `group_chunks_into_scenes`)\n4. Resume check: skip scenes with existing `MUSIC_AUDIO` blobs\n5. Generate music mood prompts via LLM (before GPU spin-up)\n6. Spin up music-server GPU pod via `workflow_gpu_pod`\n7. Scene 0: `POST /generate` with mood prompt + scene duration\n8. Scenes 1-N: `POST /outpaint` with tail of previous segment (continuation chaining)\n9. Store each segment as `BlobType.MUSIC_AUDIO` blob\n10. Crossfade + concatenate all segments into full-length music bed\n11. Store concatenated bed as `BlobType.MUSIC_BED` blob\n\n**Output model:**\n```python\nclass MusicGenerationStepOutput(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    step_type: Literal[\"music_generation\"] = \"music_generation\"\n    pod_id: str\n    segment_count: int\n    total_duration_sec: float\n    music_bed_blob_id: str\n    segments: list[MusicSegmentResult]\n```\n\n**Pattern:** Follow existing step patterns (image_generation.py) — StepContext, workflow_gpu_pod, blob storage.","acceptance_criteria":"- mypy --strict passes\n- Sub-unit resume works (skips already-generated scenes)\n- Continuation chaining via /outpaint produces coherent multi-scene music\n- Crossfade between segments is seamless\n- All blobs stored correctly (per-scene + full bed)\n- SkippedStepOutput returned when generate_music=false","status":"closed","priority":1,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-23T12:24:13Z","created_by":"Codex","updated_at":"2026-04-23T18:15:34Z","closed_at":"2026-04-23T18:15:34Z","close_reason":"Implementation already committed (a014923); added 18 unit tests for audio helpers covering scene_duration, validate_wav_response, extract_tail_b64, crossfade_and_concat.","labels":["audio-design","phase-4","pipeline-step"],"dependencies":[{"issue_id":"chatterbox-tts-lite-evk","depends_on_id":"chatterbox-tts-lite-y4q","type":"blocks","created_at":"2026-04-23T12:25:44Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-evk","depends_on_id":"chatterbox-tts-lite-ycw","type":"blocks","created_at":"2026-04-23T12:25:39Z","created_by":"shubham","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"id":"chatterbox-tts-lite-y4q","title":"Create LLM audio design prompt generation module","description":"Create `services/creepy-brain/app/llm/audio_design.py` — LLM-based prompt generation for music moods and SFX cue lists.\n\n**Music mood generation:**\n```python\nclass MusicMoodResult(BaseModel):\n    prompt: str        # ACE-Step prompt: \"dark ambient, sparse piano, 60 BPM, horror\"\n    intensity: int     # 1-10 scale\n\nasync def generate_music_mood(scene_text: str) -\u003e MusicMoodResult\n```\n\n**SFX cue list generation:**\n```python\nclass SfxCue(BaseModel):\n    description: str   # \"heavy wooden door creaking open\"\n    position: str      # \"beginning\" | \"middle\" | \"end\"\n    duration_sec: float = Field(ge=1.0, le=10.0)\n\nclass SfxCueListResult(BaseModel):\n    cues: list[SfxCue] = Field(max_length=5)\n\nasync def generate_sfx_cues(scene_text: str) -\u003e SfxCueListResult\n```\n\n**Pattern:** Follow `app/llm/image_prompts.py` — use `generate_structured()` with Pydantic validation and retries. System prompts should describe ACE-Step prompt format (mood, tempo, instrumentation) and SFX description best practices.","acceptance_criteria":"- mypy --strict passes\n- MusicMoodResult validates prompt content (has mood, tempo, instrumentation keywords)\n- SfxCueListResult enforces max 5 cues, duration bounds\n- Uses generate_structured() with retry logic\n- System prompts produce high-quality music/SFX descriptions","status":"closed","priority":1,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-23T12:24:02Z","created_by":"Codex","updated_at":"2026-04-23T14:47:07Z","started_at":"2026-04-23T14:42:36Z","closed_at":"2026-04-23T14:47:07Z","close_reason":"Completed: audio_design.py implemented with MusicMoodResult (instrumental-only validation) and SfxCueListResult (max 5 cues, 1-10s duration bounds), using generate_structured() with retry logic. mypy --strict passes.","labels":["audio-design","llm","phase-3"],"dependencies":[{"issue_id":"chatterbox-tts-lite-y4q","depends_on_id":"chatterbox-tts-lite-ycw","type":"blocks","created_at":"2026-04-23T12:25:34Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"id":"chatterbox-tts-lite-ycw","title":"Add audio design config settings and enum values","description":"Add configuration and enum values needed by the audio design pipeline.\n\n**`app/config.py` — Add to Settings class:**\n```python\nmusic_server_image: str = \"ghcr.io/sgoel1220/music-server:main\"\nmusic_server_port: int = 8007\nsfx_server_image: str = \"ghcr.io/sgoel1220/sfx-server:main\"\nsfx_server_port: int = 8008\nmusic_volume_db: float = -12.0   # static duck level under narration\nsfx_volume_db: float = -6.0     # SFX louder than music bed\n```\n\n**`app/models/enums.py` — Add enum values:**\n- `StepName.MUSIC_GENERATION = \"music_generation\"`\n- `StepName.SFX_GENERATION = \"sfx_generation\"`\n- `BlobType.MUSIC_AUDIO = \"music_audio\"` (per-scene WAV)\n- `BlobType.SFX_AUDIO = \"sfx_audio\"` (per-cue WAV)\n- `BlobType.MUSIC_BED = \"music_bed\"` (full-length mixed bed)\n\nNo dependencies — this is foundational config that other steps import.","acceptance_criteria":"- mypy --strict passes on config.py and enums.py\n- New enum values are accessible and serialize correctly\n- Settings load from env vars with correct defaults","status":"closed","priority":1,"issue_type":"task","assignee":"Codex","owner":"codex@local","created_at":"2026-04-23T12:23:44Z","created_by":"Codex","updated_at":"2026-04-23T14:33:03Z","started_at":"2026-04-23T14:31:54Z","closed_at":"2026-04-23T14:33:03Z","close_reason":"Completed: config settings and enum values added, mypy --strict passes","labels":["audio-design","config","phase-2"],"dependency_count":0,"dependent_count":5,"comment_count":0}
{"id":"chatterbox-tts-lite-sqf","title":"Create sfx-server GPU service (EzAudio)","description":"Create `services/sfx-server/` with FastAPI server for AI sound effect generation using EzAudio.\n\n**Files to create:**\n- `services/sfx-server/server.py` — FastAPI on port 8008\n- `services/sfx-server/Dockerfile` — `pytorch/pytorch:2.5.1-cuda12.1-cudnn9-runtime`\n- `services/sfx-server/requirements.txt` — ezaudio deps, torch, torchaudio, fastapi, uvicorn, soundfile\n\n**Endpoints:**\n- `GET /health` — liveness check\n- `GET /ready` — model loaded check\n- `POST /generate` — `{prompt, duration_sec (1-10s typical), seed}` → WAV bytes\n\n**Pattern:** Mirror `services/tts-server/minimal_server.py` — lazy-load model singleton, Pydantic request validation, raw bytes response.\n\n**Acceptance:** Server starts, `/health` returns 200, `/generate` produces valid WAV audio from a text prompt like \"old wooden door creaking open slowly\".","acceptance_criteria":"- server.py compiles cleanly (py_compile)\n- Dockerfile builds successfully\n- /health and /ready endpoints respond\n- POST /generate returns valid WAV bytes for SFX prompts\n- Pydantic models validate input (duration_sec 1-10s bounds)","status":"closed","priority":1,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-23T12:23:37Z","created_by":"Codex","updated_at":"2026-04-23T18:53:48Z","closed_at":"2026-04-23T18:53:48Z","close_reason":"sfx-server fully implemented: server.py, Dockerfile, requirements.txt with /health, /ready, /generate endpoints","labels":["audio-design","gpu-service","phase-1"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"chatterbox-tts-lite-zrn","title":"Create music-server GPU service (ACE-Step 1.5)","description":"Create `services/music-server/` with FastAPI server for AI music generation using ACE-Step 1.5.\n\n**Files to create:**\n- `services/music-server/server.py` — FastAPI on port 8007\n- `services/music-server/Dockerfile` — `pytorch/pytorch:2.5.1-cuda12.1-cudnn9-runtime`\n- `services/music-server/requirements.txt` — ace-step, torch, torchaudio, fastapi, uvicorn, soundfile\n\n**Endpoints:**\n- `GET /health` — liveness check\n- `GET /ready` — model loaded check\n- `POST /generate` — `{prompt, duration_sec, seed}` → WAV bytes (up to 600s)\n- `POST /outpaint` — `{prompt, audio_context (base64 WAV), duration_sec}` → WAV bytes (continuation chaining)\n\n**Pattern:** Mirror `services/tts-server/minimal_server.py` — lazy-load model singleton, Pydantic request validation, raw bytes response.\n\n**Acceptance:** Server starts, `/health` returns 200, `/generate` produces valid WAV audio from a text prompt.","acceptance_criteria":"- server.py compiles cleanly (py_compile)\n- Dockerfile builds successfully\n- /health and /ready endpoints respond\n- POST /generate returns valid WAV bytes\n- POST /outpaint returns valid WAV continuation\n- Pydantic models validate input (duration_sec bounds, etc.)","status":"closed","priority":1,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-23T12:23:30Z","created_by":"Codex","updated_at":"2026-04-23T14:03:42Z","started_at":"2026-04-23T13:47:27Z","closed_at":"2026-04-23T14:03:42Z","close_reason":"Completed: server.py, Dockerfile, requirements.txt created. All adversarial findings fixed.","labels":["audio-design","gpu-service","phase-1"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"chatterbox-tts-lite-92j","title":"Add audio design enums, config, and DB migration","description":"Add infrastructure for audio design steps: enums, config settings, input/output schemas, and DB migration.\n\n**Files to modify:**\n- `app/models/enums.py` — Add `StepName.MUSIC_GENERATION`, `StepName.SFX_GENERATION`, `BlobType.MUSIC_AUDIO`, `BlobType.SFX_AUDIO`, `BlobType.MUSIC_BED`\n- `app/config.py` — Add `music_server_image`, `music_server_port` (8007), `sfx_server_image`, `sfx_server_port` (8008), `music_chunks_per_scene`, `music_volume_db` (-12.0), `sfx_volume_db` (-6.0)\n- `app/models/json_schemas.py` — Add `generate_music: bool = False` and `generate_sfx: bool = False` to `WorkflowInputSchema`. Add `MusicGenerationStepOutput` and `SfxGenerationStepOutput` to `StepOutputSchema` union. Add `music_bed_blob_id` to `WorkflowResultSchema`\n\n**Files to create:**\n- `alembic/versions/0016_add_audio_design.py` — `ALTER TYPE stepname/blobtype ADD VALUE IF NOT EXISTS` for all new enum values","acceptance_criteria":"- [ ] New enum values added to StepName and BlobType\n- [ ] Config settings added with sensible defaults\n- [ ] `generate_music` and `generate_sfx` flags on WorkflowInputSchema (default False)\n- [ ] MusicGenerationStepOutput and SfxGenerationStepOutput models defined with step_type discriminator\n- [ ] Added to StepOutputSchema union\n- [ ] WorkflowResultSchema has music_bed_blob_id field\n- [ ] Alembic migration runs cleanly (upgrade + downgrade)\n- [ ] mypy --strict passes","status":"closed","priority":1,"issue_type":"task","owner":"codex@local","created_at":"2026-04-23T12:18:22Z","created_by":"Codex","updated_at":"2026-04-23T12:26:59Z","closed_at":"2026-04-23T12:26:59Z","close_reason":"Superseded by phased beads: chatterbox-tts-lite-ycw (config/enums) and chatterbox-tts-lite-52l (migration)","labels":["audio-design","infrastructure"],"dependency_count":0,"dependent_count":3,"comment_count":0}
{"id":"chatterbox-tts-lite-d03","title":"Create sfx-server GPU service (EzAudio)","description":"Create a new stateless FastAPI GPU service for sound effect generation using EzAudio.\n\n**Location:** `services/sfx-server/`\n\n**Files to create:**\n- `server.py` — FastAPI app on port 8008 with `/health`, `/ready`, `POST /generate`\n- `Dockerfile` — Based on `pytorch/pytorch:2.5.1-cuda12.1-cudnn9-runtime`, installs ezaudio\n- `requirements.txt` — ezaudio dependencies, torch, torchaudio, fastapi, uvicorn, soundfile\n\n**API contract:**\n- `POST /generate` — `{prompt, duration_sec (1-10s), seed}` → WAV bytes\n- Follow same patterns as `services/tts-server/minimal_server.py`\n\n**Model:** EzAudio (MIT license, lightweight, RTX 3090 compatible)","acceptance_criteria":"- [ ] FastAPI server starts and `/health` returns 200\n- [ ] `/ready` returns 503 until model loaded, then 200\n- [ ] `POST /generate` produces valid WAV audio from SFX text prompt (e.g. \"door creaking open\")\n- [ ] Duration parameter works (1-10s)\n- [ ] Seed parameter produces reproducible output\n- [ ] Pydantic request validation with proper error responses\n- [ ] Docker builds successfully\n- [ ] Runs on RTX 3090 (24GB VRAM)\n- [ ] All code fully typed (mypy --strict passes)","status":"closed","priority":1,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-23T12:18:16Z","created_by":"Codex","updated_at":"2026-04-23T12:27:04Z","closed_at":"2026-04-23T12:27:04Z","close_reason":"Superseded by chatterbox-tts-lite-sqf (more detailed)","labels":["audio-design","gpu-service"],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"id":"chatterbox-tts-lite-fia","title":"Create music-server GPU service (ACE-Step 1.5)","description":"Create a new stateless FastAPI GPU service for background music generation using ACE-Step 1.5.\n\n**Location:** `services/music-server/`\n\n**Files to create:**\n- `server.py` — FastAPI app on port 8007 with `/health`, `/ready`, `POST /generate`, `POST /outpaint`\n- `Dockerfile` — Based on `pytorch/pytorch:2.5.1-cuda12.1-cudnn9-runtime`, installs ace-step\n- `requirements.txt` — ace-step, torch, torchaudio, fastapi, uvicorn, soundfile\n\n**API contract:**\n- `POST /generate` — `{prompt, duration_sec (up to 600s), seed}` → WAV bytes\n- `POST /outpaint` — `{prompt, audio_context (base64 WAV tail), duration_sec}` → WAV bytes (continuation)\n- Follow same patterns as `services/tts-server/minimal_server.py`: lazy model singleton, Pydantic request models, raw bytes response\n\n**Model:** ACE-Step 1.5 (Apache 2.0 license, ~18GB VRAM, RTX 3090 compatible)","acceptance_criteria":"- [ ] FastAPI server starts and `/health` returns 200\n- [ ] `/ready` returns 503 until model loaded, then 200\n- [ ] `POST /generate` produces valid WAV audio from text prompt\n- [ ] `POST /outpaint` extends audio using context from previous segment\n- [ ] Duration parameter works (30s to 600s)\n- [ ] Seed parameter produces reproducible output\n- [ ] Pydantic request validation with proper error responses\n- [ ] Docker builds successfully\n- [ ] Runs on RTX 3090 (24GB VRAM)\n- [ ] All code fully typed (mypy --strict passes)","status":"closed","priority":1,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-23T12:18:09Z","created_by":"Codex","updated_at":"2026-04-23T12:27:08Z","closed_at":"2026-04-23T12:27:08Z","close_reason":"Superseded by chatterbox-tts-lite-zrn (more detailed)","labels":["audio-design","gpu-service"],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"id":"chatterbox-tts-lite-iyx","title":"Decompose orchestrator.run_pipeline into focused helpers","description":"Split orchestrator.run_pipeline (pipeline/orchestrator.py) into 12 focused private helpers: _transition_story_status, _run_architect, _persist_bible_and_outline, _repair_outline_loop, _request_architect_fix, _write_act_with_review, _persist_act_draft, _full_story_review_loop, _evaluate_review_decision, _rewrite_act, _finalize_story, _handle_pipeline_failure. Write tests first, then refactor.","status":"closed","priority":1,"issue_type":"task","owner":"codex@local","created_at":"2026-04-19T18:22:38Z","created_by":"Codex","updated_at":"2026-04-19T19:08:02Z","closed_at":"2026-04-19T19:08:02Z","close_reason":"Extracted 12 private helpers from run_pipeline. 33/33 tests pass, mypy clean.","dependencies":[{"issue_id":"chatterbox-tts-lite-iyx","depends_on_id":"chatterbox-tts-lite-6ii","type":"blocks","created_at":"2026-04-19T18:22:38Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-431","title":"Decompose WorkflowService into 7 focused classes","description":"Split WorkflowService (services/workflow_service.py) into: WorkflowChunkService (TTS chunk state), WorkflowSceneService (scene image state), WorkflowStepService (step attempt lifecycle), WorkflowLifecycleService (parent workflow status), WorkflowReadRepository (read-only projections), WorkflowForkService (fork logic), workflow_ids.py (ID parsing). Write tests first, then refactor.","status":"closed","priority":1,"issue_type":"task","owner":"codex@local","created_at":"2026-04-19T18:22:37Z","created_by":"Codex","updated_at":"2026-04-19T19:08:01Z","closed_at":"2026-04-19T19:08:01Z","close_reason":"Extracted 7 focused modules. 20/20 tests pass, mypy clean.","dependencies":[{"issue_id":"chatterbox-tts-lite-431","depends_on_id":"chatterbox-tts-lite-6ii","type":"blocks","created_at":"2026-04-19T18:22:36Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-2ns","title":"Decompose WorkflowRunner into 7 focused classes","description":"Split WorkflowRunner (engine/runner.py) into: WorkflowDagPlanner (graph validation/reachability), WorkflowRunState (output accumulation), CompletedStepLoader (resume hydration), StepLifecycleRepository (DB lifecycle persistence), WorkflowStepExecutor (execute one step), FailureStepRunner (failure steps), slim WorkflowRunner coordinator. Write tests first, then refactor.","status":"closed","priority":1,"issue_type":"task","owner":"codex@local","created_at":"2026-04-19T18:22:35Z","created_by":"Codex","updated_at":"2026-04-19T19:08:00Z","closed_at":"2026-04-19T19:08:00Z","close_reason":"Extracted 7 focused classes. 22/22 tests pass, mypy clean.","dependencies":[{"issue_id":"chatterbox-tts-lite-2ns","depends_on_id":"chatterbox-tts-lite-6ii","type":"blocks","created_at":"2026-04-19T18:22:35Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-aq0","title":"Decompose WorkflowEngine into 5 focused classes","description":"Split WorkflowEngine (engine/engine.py) into: WorkflowDefinitionRegistry (register/lookup), WorkflowTaskSupervisor (asyncio task scheduling/cleanup), WorkflowRetryResumeController (retry_step/resume_from_db), WorkflowStateRepository (all DB reads/writes), WorkflowResourceController (pause/cancel/GPU cleanup). Write tests first, then refactor.","status":"closed","priority":1,"issue_type":"task","owner":"codex@local","created_at":"2026-04-19T18:22:33Z","created_by":"Codex","updated_at":"2026-04-19T19:07:59Z","closed_at":"2026-04-19T19:07:59Z","close_reason":"Extracted 5 focused classes: registry, task_supervisor, retry_resume_controller, state_repository, resource_controller. 16/16 tests pass, mypy clean.","dependencies":[{"issue_id":"chatterbox-tts-lite-aq0","depends_on_id":"chatterbox-tts-lite-6ii","type":"blocks","created_at":"2026-04-19T18:22:32Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-6ii","title":"Epic: Decompose God Objects - engine, runner, service, orchestrator","description":"WorkflowEngine, WorkflowRunner, WorkflowService, and orchestrator.run_pipeline are God Objects. Split into focused single-responsibility units. Write tests first, then refactor.","status":"closed","priority":1,"issue_type":"task","assignee":"Codex","owner":"codex@local","created_at":"2026-04-19T18:18:27Z","created_by":"Codex","updated_at":"2026-04-19T19:07:34Z","started_at":"2026-04-19T18:18:30Z","closed_at":"2026-04-19T19:07:34Z","close_reason":"All 4 God Objects decomposed into focused classes. 136/139 tests pass (3 pre-existing failures). Pushed to main.","dependency_count":0,"dependent_count":4,"comment_count":0}
{"id":"chatterbox-tts-lite-nrx","title":"Epic: Decompose God Objects in engine, runner, service, and orchestrator","description":"WorkflowEngine, WorkflowRunner, WorkflowService, and orchestrator.run_pipeline are all God Objects mixing too many responsibilities. Each needs to be split into focused single-responsibility units. Approach: write comprehensive unit tests first, then refactor so tests continue to pass.","status":"open","priority":1,"issue_type":"task","owner":"codex@local","created_at":"2026-04-19T18:18:02Z","created_by":"Codex","updated_at":"2026-04-19T18:18:02Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-bsp","title":"Add GPU fallback to RTX 3080 Ti when primary GPU unavailable","description":"TTS synthesis fails with \"no instances available\" when the primary GPU type is unavailable on RunPod. Add fallback to RTX 3080 Ti so the step can retry with an alternate GPU spec instead of failing.","status":"closed","priority":1,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-19T11:35:06Z","created_by":"Codex","updated_at":"2026-04-19T11:38:18Z","started_at":"2026-04-19T11:35:21Z","closed_at":"2026-04-19T11:38:18Z","close_reason":"Implemented GPU fallback to RTX 3080 Ti","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-ayu","title":"Add encode_audio step: convert WAV chunks to MP3 after TTS","description":"Add a new pipeline step encode_audio that runs after tts_synthesis. It reads each completed WAV chunk blob, encodes to MP3 via ffmpeg, stores the MP3 blob, and saves the mp3_blob_id on the workflow_chunk row. UI then serves MP3 for streaming + shows download link. Requires: DB migration (blobtype enum + tts_mp3_blob_id column), new step file, pipeline registration, API update, UI update.","status":"closed","priority":1,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-19T08:47:32Z","created_by":"Codex","updated_at":"2026-04-19T08:47:55Z","closed_at":"2026-04-19T08:47:55Z","close_reason":"superseded by inline approach","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-631","title":"Store MP3 alongside WAV for chunk audio; fix streaming and add download link","description":"WAV blobs don't stream reliably in browsers. Fix: encode each chunk to MP3 immediately after TTS synthesis and store both in DB. Serve MP3 to UI. Also add a download link to the audio player. Requires DB migration, TTS step change, API update, and UI update.","status":"open","priority":1,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-19T08:46:59Z","created_by":"Codex","updated_at":"2026-04-19T08:46:59Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-3on","title":"Add HTTP range request support to blob endpoint for audio streaming","description":"The /api/blobs/{blob_id} endpoint returns full binary data but lacks Accept-Ranges and HTTP 206 Partial Content support. HTML5 audio players require range requests to seek, stream, and play audio beyond the initial buffer. Without it, audio cuts off mid-playback even though duration shows correctly.","status":"closed","priority":1,"issue_type":"bug","assignee":"Codex","owner":"codex@local","created_at":"2026-04-19T08:38:35Z","created_by":"Codex","updated_at":"2026-04-19T08:55:32Z","started_at":"2026-04-19T08:38:50Z","closed_at":"2026-04-19T08:55:32Z","close_reason":"WAV→MP3 encoding + download link shipped. Migrate 0012 applied.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-quo","title":"Replace Next.js frontend with vanilla HTML + esbuild TypeScript dashboard","description":"Migrate services/creepy-brain/web/ (Next.js 16 + React 19 + shadcn) to a plain HTML + TypeScript dashboard served by FastAPI static mount. Hash-based routing, setInterval polling, minimal CSS. Uses esbuild for bundling. See plan in .claude/plans/clever-floating-treasure.md.","status":"closed","priority":1,"issue_type":"task","assignee":"Codex","owner":"codex@local","created_at":"2026-04-18T21:47:01Z","created_by":"Codex","updated_at":"2026-04-18T21:59:47Z","started_at":"2026-04-18T21:47:15Z","closed_at":"2026-04-18T21:59:47Z","close_reason":"Implemented: Next.js replaced with vanilla HTML + esbuild dashboard. All adversarial review findings addressed.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-ikj","title":"Implement workflow detail page","description":"workflows/[id]/page.tsx is a stub showing \"coming soon\". Should fetch workflow detail via useWorkflow hook and display: input params, step progress with status badges, chunk list with TTS status, GPU pod info, error details for failed workflows, result data for completed ones.","status":"closed","priority":1,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-18T21:21:07Z","created_by":"Codex","updated_at":"2026-04-18T21:30:58Z","started_at":"2026-04-18T21:23:47Z","closed_at":"2026-04-18T21:30:58Z","close_reason":"Implemented full workflow detail page with steps, chunks, GPU pods, input, progress, and result display","labels":["frontend"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-8c3","title":"Add missing workflow statuses (cancelled, paused) to frontend types","description":"Frontend Workflow type only declares 4 statuses (pending, running, completed, failed) but backend has 6 (adds cancelled, paused). Workflows with cancelled/paused status will have missing badge variants and type errors. Fix: update Workflow[\"status\"] union in api.ts, add badge variants in workflow-card.tsx, add tabs for cancelled/paused in workflows/page.tsx.","status":"closed","priority":1,"issue_type":"bug","assignee":"Codex","owner":"codex@local","created_at":"2026-04-18T21:20:55Z","created_by":"Codex","updated_at":"2026-04-18T21:30:46Z","started_at":"2026-04-18T21:22:20Z","closed_at":"2026-04-18T21:30:46Z","close_reason":"Added cancelled/paused statuses to Workflow type, badge variants, and tab filters","labels":["frontend","types"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-aew","title":"Fix AGENTS typing and layering violations","description":"Address reported AGENTS.md violations around dict/tuple return types, service-layer HTTPException usage, imprecise model typing, enum fields, dataclass GPU types, and legacy typing imports. Verify which reports are accurate and make scoped fixes.","status":"closed","priority":1,"issue_type":"task","assignee":"Codex","owner":"codex@local","created_at":"2026-04-18T14:56:52Z","created_by":"Codex","updated_at":"2026-04-18T15:20:07Z","started_at":"2026-04-18T14:57:06Z","closed_at":"2026-04-18T15:20:07Z","close_reason":"Fixed AGENTS typing/layering violations and the surfaced mypy issues in llm/client.py and gpu/runpod.py","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-hbt","title":"Remove Hatchet dependency and cleanup","description":"Remove all Hatchet infrastructure, dependencies, and dead code.\n\n## Files to modify\n- pyproject.toml — remove hatchet-sdk from dependencies\n- docker-compose.yml — remove brain-worker, hatchet-engine, hatchet-setup services; remove HATCHET_CLIENT_* env vars from brain service\n\n## Files to delete\n- app/workflows/worker.py — no longer needed (engine runs in-process)\n- postgres/init-hatchet.sql — Hatchet DB no longer needed\n- postgres/ensure-hatchet.sql — Hatchet DB no longer needed\n\n## Acceptance criteria\n- No references to hatchet_sdk or hatchet anywhere in codebase (grep returns 0)\n- docker-compose has only brain + postgres services\n- worker.py deleted\n- Hatchet DB init scripts deleted\n- pip install -e . succeeds without hatchet-sdk\n- Application starts and workflows can be triggered\n- No HATCHET_CLIENT_* env vars in docker-compose or .env.example","status":"closed","priority":1,"issue_type":"task","assignee":"Codex","owner":"codex@local","created_at":"2026-04-18T11:55:58Z","created_by":"Codex","updated_at":"2026-04-18T13:16:46Z","started_at":"2026-04-18T13:10:53Z","closed_at":"2026-04-18T13:16:46Z","close_reason":"Completed: removed Hatchet SDK, worker.py, SQL init scripts, docker-compose services, env vars, and cleaned all docstring references","labels":["cleanup","creepy-brain","engine"],"dependencies":[{"issue_id":"chatterbox-tts-lite-hbt","depends_on_id":"chatterbox-tts-lite-03n","type":"blocks","created_at":"2026-04-18T11:56:11Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-03n","title":"Wire engine into FastAPI lifecycle + update routes","description":"Integrate the workflow engine into the FastAPI application lifecycle and update API routes to trigger workflows via the engine.\n\n## Files to modify\n- app/main.py — start/stop engine in lifespan (after DB init)\n- app/routes/workflows.py — trigger via engine.trigger() instead of workflow.aio_run_no_wait()\n- app/workflows/__init__.py — export engine singleton\n\n## Acceptance criteria\n- Engine starts in FastAPI lifespan (after DB init)\n- Engine stops gracefully on shutdown (cancels running workflows)\n- POST /api/workflows triggers workflow via engine\n- POST /api/workflows/test triggers test workflow via engine\n- DELETE /api/workflows/{id} cancels via engine\n- POST /api/workflows/{id}/retry creates new workflow run via engine\n- Cron-scheduled recon workflow triggers automatically every 5 minutes","status":"closed","priority":1,"issue_type":"task","assignee":"Codex","owner":"codex@local","created_at":"2026-04-18T11:55:34Z","created_by":"Codex","updated_at":"2026-04-18T13:07:35Z","started_at":"2026-04-18T12:59:27Z","closed_at":"2026-04-18T13:07:35Z","close_reason":"Closed","labels":["creepy-brain","engine","integration"],"dependencies":[{"issue_id":"chatterbox-tts-lite-03n","depends_on_id":"chatterbox-tts-lite-dlh","type":"blocks","created_at":"2026-04-18T11:56:08Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"id":"chatterbox-tts-lite-dlh","title":"Port all workflows to new engine","description":"Rewrite all three workflows (ContentPipeline, TestWorkflow, ReconOrphanedPods) to use the new in-process engine instead of Hatchet.\n\n## Files to modify\n- app/workflows/content_pipeline.py — rewrite to build WorkflowDef and register with engine\n- app/workflows/test_workflow.py — same\n- app/workflows/recon.py — same, with cron='*/5 * * * *'\n- app/workflows/__init__.py — replace hatchet client with engine instance\n- app/workflows/steps/tts.py — Context -\u003e StepContext, ctx._data.parents -\u003e ctx.parent_outputs\n- app/workflows/steps/story.py — Context -\u003e StepContext\n- app/workflows/steps/image.py — Context -\u003e StepContext\n- app/workflows/steps/stitch.py — Context -\u003e StepContext, ctx._data.parents -\u003e ctx.parent_outputs\n- app/workflows/steps/cleanup.py — Context -\u003e StepContext\n\n## Acceptance criteria\n- All three workflows defined as WorkflowDef\n- All step executors use StepContext instead of hatchet_sdk.Context\n- No imports from hatchet_sdk remain in workflow/step files\n- tracked_step pattern preserved (step lifecycle tracking)\n- Step data passing works (story_id flows from generate_story -\u003e tts_synthesis, etc.)\n- ReconOrphanedPods has cron='*/5 * * * *'","status":"closed","priority":1,"issue_type":"task","assignee":"Codex","owner":"codex@local","created_at":"2026-04-18T11:55:08Z","created_by":"Codex","updated_at":"2026-04-18T12:52:01Z","started_at":"2026-04-18T12:38:50Z","closed_at":"2026-04-18T12:52:01Z","close_reason":"All three workflows ported to WorkflowDef/StepDef, all hatchet_sdk imports removed from workflow/step files, StepContext replaces Context, parent_outputs replaces ctx._data.parents, tracked_step removed (engine runner handles lifecycle), complete_workflow preserved in stitch_final, cleanup step simplified.","labels":["creepy-brain","engine","migration"],"dependencies":[{"issue_id":"chatterbox-tts-lite-dlh","depends_on_id":"chatterbox-tts-lite-dsx","type":"blocks","created_at":"2026-04-18T11:56:07Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"chatterbox-tts-lite-dsx","title":"Create in-process workflow engine core","description":"Replace Hatchet with a lightweight in-process workflow engine that runs inside the creepy-brain FastAPI process.\n\n## Files to create\n- app/engine/__init__.py — public exports\n- app/engine/models.py — StepContext (replaces hatchet Context), StepDef, WorkflowDef (Pydantic)\n- app/engine/runner.py — WorkflowRunner: DAG topological sort, step execution with asyncio.wait_for() timeouts, retry loop, on-failure hooks, parent output collection\n- app/engine/engine.py — WorkflowEngine singleton: workflow registry, trigger() spawns asyncio task, retry_step() resets downstream + resumes, cancel()\n- app/engine/scheduler.py — CronScheduler: asyncio + croniter, sleep-until-next-trigger pattern\n\n## Add dependency\n- croniter to pyproject.toml\n\n## Key design\n- StepContext has: workflow_run_id (str), parent_outputs (dict[str, dict[str, object]])\n- Runner uses existing DB models (Workflow, WorkflowStep) for state persistence\n- Steps are async callables: (input: BaseModel, ctx: StepContext) -\u003e dict[str, object]\n- DAG resolved via topological sort of StepDef.parents\n- retry_step() resets target + transitive downstream steps to PENDING, resumes runner\n- cancel() cancels asyncio task, terminates active GPU pods\n\n## Acceptance criteria\n- Engine can register workflow definitions\n- Engine can trigger a workflow and execute steps in DAG order\n- Steps receive StepContext with correct parent_outputs\n- Step timeouts enforced via asyncio.wait_for()\n- Step retries work (up to N attempts)\n- On-failure hook runs when a step permanently fails\n- retry_step() resets target + downstream, resumes execution\n- cancel() cancels running tasks, marks workflow CANCELLED\n- All state persisted to existing Workflow/WorkflowStep DB models\n- Cron scheduler triggers workflows on schedule\n- All code fully typed, mypy passes","status":"closed","priority":1,"issue_type":"task","assignee":"Codex","owner":"codex@local","created_at":"2026-04-18T11:54:35Z","created_by":"Codex","updated_at":"2026-04-18T12:33:53Z","started_at":"2026-04-18T12:08:41Z","closed_at":"2026-04-18T12:33:53Z","close_reason":"Engine core implemented: WorkflowEngine, WorkflowRunner, CronScheduler, StepContext/StepDef/WorkflowDef models. All adversarial review findings addressed. Merged to main.","labels":["creepy-brain","engine"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-072","title":"No quality gate — stitch uses failed TTS chunks as-is","description":"stitch_final concatenates ALL chunks including ChunkStatus.FAILED ones. Failed chunks have best-effort (garbage) audio saved as blobs. get_chunks_for_image_step() returns every chunk regardless of status. Result: final audio contains silence/noise/corruption from failed chunks.\n\nFix: check tts_status before including chunk in stitch. Either skip failed chunks (with warning) or fail the workflow if too many chunks failed.\n\nAcceptance criteria:\n- stitch_final checks tts_status before including a chunk\n- Failed chunks are skipped with logging, or workflow fails if \u003e20% chunks failed\n- Workflow result includes quality metric (chunks_passed / total_chunks)","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-18T07:35:35Z","updated_at":"2026-04-18T15:33:52Z","closed_at":"2026-04-18T15:33:52Z","close_reason":"Quality gate already implemented in stitch.py lines 96-114: filters out non-COMPLETED chunks, raises if all chunks failed","labels":["pipeline","reliability"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-7cs","title":"BYTEA blob storage in Postgres — scalability and cost issue","description":"All binary data (WAV ~500KB each, PNG ~200KB, MP3 ~5MB, MP4 ~50MB+) stored as BYTEA in workflow_blobs table. A single workflow can store 50-100MB. This will crush Postgres performance, backup times, and costs as workflows accumulate.\n\nFix: use S3/R2 for blob storage, store only the key/URL in Postgres. Keep the blob_service interface, just change the backend.\n\nAcceptance criteria:\n- Blobs stored in S3-compatible storage (R2 for cost)\n- workflow_blobs table stores key/URL instead of data\n- blob_service.store() and blob_service.get() interface unchanged\n- Migration path for existing data","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-18T07:35:35Z","updated_at":"2026-04-18T10:24:55Z","closed_at":"2026-04-18T15:54:55Z","labels":["scalability","storage"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-7nv","title":"TTS retry uses same seed — retries produce identical bad audio","description":"In tts.py _synthesize_with_retry, every retry uses settings.tts_seed (static). Same text + same seed + same params = deterministically identical output. If a chunk fails validation, all 3 attempts produce the exact same bad audio. Retries are completely useless.\n\nFix: increment seed on each retry (seed + attempt_number). Simple 1-line change.\n\nAcceptance criteria:\n- Each retry attempt uses a different seed (seed + attempt)\n- Log which seed was used per attempt","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-18T07:35:35Z","updated_at":"2026-04-18T08:35:12Z","closed_at":"2026-04-18T15:54:55Z","labels":["reliability","tts"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-db1","title":"Add foreign keys for blob and pod reference columns","description":"Several schema columns store references as plain UUID/string values instead of enforcing referential integrity:\n\n- `workflow_chunks.tts_audio_blob_id` and `workflow_chunks.image_blob_id` point at `workflow_blobs.id` by convention only.\n- `runs.final_audio_blob_id` and `run_chunks.audio_blob_id` point at `workflow_blobs.id` by convention only.\n- `workflow_steps.gpu_pod_id` points at `gpu_pods.id` by convention only.\n- voice uploads store `str(blob.id)` in `voices.audio_path`, so voice audio has no FK either.\n\nImpact: DB writes can create broken media/pod references that only fail later during stitch, download, cancellation, or cleanup. Orphaned blobs/pods are also hard to reconcile.\n\nFix: add nullable FKs and indexes where the relationship is real. For voices, split local file paths from uploaded blob references if both storage modes must remain supported. Backfill/validate existing rows before enforcing constraints, and choose `ON DELETE` behavior deliberately (`SET NULL`, `RESTRICT`, or cascade).","status":"closed","priority":1,"issue_type":"bug","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T22:11:41Z","created_by":"Codex","updated_at":"2026-04-18T15:50:43Z","closed_at":"2026-04-18T15:50:43Z","close_reason":"Closed","labels":["creepy-brain","database","integrity","schema"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-d8o","title":"Make ContentPipeline runnable for story+TTS only (skip image/stitch steps)","description":"**Goal**: Run story generation + TTS and listen to the audio, without requiring full image/stitch implementation.\n\n## Problem\nCurrent `content_pipeline.py` has 4 sequential steps:\n1. `generate_story` → works\n2. `tts_synthesis` → works after 6pl + 4i2 fixes\n3. `image_generation` → raises `NotImplementedError`\n4. `stitch_final` → raises `NotImplementedError`\n\nAfter TTS completes, the workflow crashes on step 3.\n\n## Solution Options\n\n**Option A: Skip steps when disabled** (recommended)\nModify `image.py` and `stitch.py` to return early when `generate_images=False`:\n```python\n# image.py\nasync def execute(input: WorkflowInputSchema, ctx: Context) -\u003e dict[str, object]:\n    if not input.generate_images:\n        return {\"skipped\": True, \"reason\": \"generate_images=False\"}\n    raise NotImplementedError(...)\n```\n\n**Option B: Create story+TTS-only workflow**\nNew workflow with just 2 steps, no image/stitch dependencies.\n\n## Acceptance Criteria\n- [ ] ContentPipeline can complete when `generate_images=False`\n- [ ] TTS audio blobs saved to database\n- [ ] Workflow marked complete (not failed)\n- [ ] Audio can be retrieved via blob API\n\n## Notes\n- Workflow must be registered in `app/workflows/__init__.py` (currently commented out)\n- Need to uncomment `WORKFLOWS.append(content_pipeline)` after fixing steps","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T10:22:38Z","created_by":"Shubham Goel","updated_at":"2026-04-17T10:59:53Z","closed_at":"2026-04-17T16:29:53Z","close_reason":"Completed: ContentPipeline registered and runnable for story+TTS only. image/stitch steps skip when flags are False; model_validator fails fast before expensive work if unimplemented flags are requested.","labels":["mvp","phase-2","workflow"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-d8o","depends_on_id":"Chatterbox-TTS-Server-4i2","type":"blocks","created_at":"2026-04-17T15:52:43Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-d8o","depends_on_id":"Chatterbox-TTS-Server-6pl","type":"blocks","created_at":"2026-04-17T15:52:42Z","created_by":"shubham","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-8uw","title":"[P2-followup] TTS /synthesize endpoint doesn't exist - path mismatch with TTS server","description":"**TTS step uses non-existent endpoint**, guaranteed runtime failure.\n\n## Problem\n`tts.py` line 54 defines:\n```python\n_SYNTHESIZE_PATH = \"/synthesize\"\n```\n\nBut the code comment at lines 23-25 explicitly acknowledges:\n\u003e This contract requires a dedicated slim TTS image that exposes only /synthesize. The current chatterbox-tts-server image exposes /tts instead.\n\nThe TTS server's actual endpoint is `POST /tts`, not `/synthesize`.\n\n## Impact\nEvery TTS synthesis call will return HTTP 404, failing the workflow.\n\n## Fix Options\n1. Change `_SYNTHESIZE_PATH = \"/tts\"` and adapt request body to match existing TTS API\n2. Add `/synthesize` endpoint to TTS server as documented\n\nOption 1 is faster; Option 2 matches the documented GPU pod contract.\n\n## Request/Response Contract Mismatch\nThe TTS step expects:\n```python\njson={\"text\": chunk_text, \"voice\": voice_name, \"seed\": seed}\n```\n\nBut current TTS server `/tts` expects different schema (check routes.py in tts-server).\n\n## Acceptance Criteria\n- [ ] TTS step successfully calls the TTS server endpoint\n- [ ] Request body matches expected schema\n- [ ] WAV bytes returned and validated","status":"closed","priority":1,"issue_type":"bug","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T09:59:31Z","created_by":"Shubham Goel","updated_at":"2026-04-17T10:22:04Z","closed_at":"2026-04-17T15:52:04Z","close_reason":"Closed via update","labels":["api","breaking","phase-2","tts"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-6pl","title":"[P2-followup] TTS step expects full_text but generate_story schema doesn't provide it","description":"**Critical schema contract violation** between workflow steps.\n\n## Problem\n`tts.py` line 79 expects `full_text` from the parent step output:\n```python\nfull_text: str = story_output.get(\"full_text\", \"\")\n```\n\nBut `GenerateStoryStepOutput` schema (models/schemas.py:83-91) only provides:\n- `story_id`, `title`, `word_count`, `act_count`\n\nThere is NO `full_text` field in the schema.\n\n## Impact\nThe TTS step will fail at runtime with empty `full_text`, raising ValueError at line 82-83:\n```python\nif not full_text:\n    raise ValueError(\"generate_story step did not produce full_text\")\n```\n\n## Fix Options\n1. Add `full_text: str` to `GenerateStoryStepOutput` schema and return it from `story.py`\n2. Have TTS step fetch full_text from database using `story_id` (requires StoryService.get_full_text method)\n\nOption 2 is cleaner (avoids serializing large text in Hatchet step output), but requires story table to store concatenated full_text.\n\n## Acceptance Criteria\n- [ ] TTS step can successfully read the full story text from the prior step\n- [ ] Schema and implementation match\n- [ ] mypy passes","status":"closed","priority":1,"issue_type":"bug","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T09:59:25Z","created_by":"Shubham Goel","updated_at":"2026-04-17T10:48:25Z","closed_at":"2026-04-17T16:18:25Z","close_reason":"Completed: TTS step now fetches full_text from DB using story_id; UUID normalization handles both str and native uuid.UUID parent shapes.","labels":["breaking","phase-2","schema","workflow"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-9wo","title":"Call init_db() before starting Hatchet worker","description":"The `tts_synthesis` step (and any future step that writes to Postgres) accesses `app.db.async_session_maker` at runtime. If the Hatchet worker process starts workflows before `init_db()` is called, `async_session_maker` will be `None` and every DB write will raise an `AssertionError`.\n\n## Root cause\n\n`app/db.py` initializes `async_session_maker = None` at module level. `init_db()` must be called once at process startup to configure it. The worker entrypoint (`app/workflows/worker.py`) needs to call `init_db()` before starting the Hatchet worker loop.\n\n## Fix\n\nIn `app/workflows/worker.py` (or the worker startup lifespan), ensure:\n\n```python\nfrom app.db import init_db\n\nasync def startup():\n    await init_db()\n    # then start hatchet worker\n```\n\nVerify `app/main.py` lifespan also covers the worker process (it may already call `init_db()` for the FastAPI app, but the standalone worker subprocess needs it independently).\n\n## Acceptance criteria\n\n- [ ] Worker process calls `init_db()` before Hatchet starts dispatching tasks\n- [ ] `async_session_maker` is never `None` when a step tries to write to DB\n- [ ] Worker survives restart without DB connection errors","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T09:40:19Z","created_by":"Shubham Goel","updated_at":"2026-04-17T10:19:52Z","closed_at":"2026-04-17T15:49:52Z","close_reason":"Completed","labels":["bug","creepy-brain","database","worker"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-4i2","title":"Add POST /synthesize endpoint to TTS server image","description":"The `tts_synthesis` workflow step (bead duy) calls `POST /synthesize` with `{text, voice, seed}` and expects raw WAV bytes in return. The current `chatterbox-tts-server` image only exposes `/tts` (complex request model, chunking, job state, etc.).\n\nA slim stateless `/synthesize` endpoint must be added to the TTS server before the ContentPipeline workflow can produce audio.\n\n## Contract\n\n```\nPOST /synthesize\nBody: { text: str, voice: str, seed: int }\nResponse: WAV bytes (Content-Type: audio/wav), HTTP 200 always\n\nNo job state. No chunking. No validation. Single chunk, single response.\n```\n\n## Implementation notes\n\n- Add a new FastAPI route `/synthesize` in `services/tts-server/routes.py`\n- Load text into the existing Chatterbox engine, synthesize, return raw WAV bytes\n- `voice` maps to a reference audio file (same lookup as existing `/tts` route)\n- `seed` sets torch/numpy seed for reproducibility\n- No authentication, no rate limiting (pod is private to creepy-brain)\n- Keep it minimal — this endpoint must be fast and stateless\n\n## Acceptance criteria\n\n- [ ] `POST /synthesize {text, voice, seed}` returns WAV bytes with status 200\n- [ ] Voice lookup matches existing predefined voice list\n- [ ] Seed is respected for deterministic output\n- [ ] No job state created (no DB writes)\n- [ ] Docker image rebuilt and pushed: `shubh67678/chatterbox-tts-server:latest`\n- [ ] Integration test: creepy-brain tts_synthesis step succeeds end-to-end against the updated image","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T09:40:11Z","created_by":"Shubham Goel","updated_at":"2026-04-17T10:48:34Z","closed_at":"2026-04-17T16:18:34Z","close_reason":"Completed","labels":["api","blocker","gpu-pod","tts"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-2ok","title":"[P2-followup] Start Hatchet worker from docker-compose","description":"Bead Chatterbox-TTS-Server-9r1 (install hatchet-sdk + configure worker) was closed, but no worker service is declared in `services/creepy-brain/docker-compose.yml`. The `brain` container only runs uvicorn via `entrypoint.sh`. The Hatchet engine is up and the SDK is installed, but no worker process ever connects → no workflows can execute when the stack is brought up via compose.\n\nThis also means the verification criteria for vxb (\"POST /api/workflows/test triggers workflow … Workflow visible in Hatchet dashboard … Step one completes, step two runs after\") cannot be satisfied out of the box.\n\n## Acceptance criteria\n- [ ] Add a `brain-worker` service to `docker-compose.yml` that reuses the `brain` image and runs `python -m app.workflows.worker`\n- [ ] Worker depends on `hatchet-engine` being healthy and shares the same env (`HATCHET_CLIENT_TOKEN`, DB vars)\n- [ ] Worker logs visible in `docker compose logs brain-worker`\n- [ ] Confirm worker registers in Hatchet dashboard after `docker compose up`\n- [ ] README updated with the two-process setup and how to exec the test trigger","status":"closed","priority":1,"issue_type":"bug","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T16:07:02Z","created_by":"Shubham Goel","updated_at":"2026-04-17T08:30:26Z","closed_at":"2026-04-17T14:00:26Z","close_reason":"Completed","labels":["hatchet","infrastructure","phase-2","regression"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-2ok","depends_on_id":"Chatterbox-TTS-Server-9r1","type":"discovered-from","created_at":"2026-04-16T21:37:43Z","created_by":"shubham","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-se6","title":"[P2-followup] Implement missing LocalProvider + gpu_provider factory","description":"Bead Chatterbox-TTS-Server-w4n was closed but the LocalProvider was never implemented. This blocks local development — any call to `get_provider()` with an empty `RUNPOD_API_KEY` will fail when RunPod API is hit.\n\n## Evidence\n- `services/creepy-brain/app/gpu/local.py` — does NOT exist\n- `services/creepy-brain/app/gpu/__init__.py` — `get_provider(api_key)` unconditionally returns `RunPodProvider`, signature takes just an api key (design called for `get_provider(settings)` with a `gpu_provider` switch)\n- `services/creepy-brain/app/config.py` — missing `gpu_provider` and `local_tts_url` settings\n- grep `LocalProvider|gpu_provider|local_tts_url` across creepy-brain returns no matches\n\n## Acceptance criteria\n- [ ] Add `gpu_provider: Literal[\"local\", \"runpod\"] = \"local\"` and `local_tts_url: str = \"http://localhost:8005\"` to `app/config.py`\n- [ ] Create `app/gpu/local.py` with a `LocalProvider(GpuProvider)` implementation:\n  - `create_pod` returns a pod immediately with status READY, endpoint = `local_tts_url`, cost 0\n  - Supports idempotency via in-memory dict keyed on `idempotency_key`\n  - `get_pod`, `terminate_pod`, `wait_for_ready`, `list_active_pods` all implemented\n- [ ] Update `app/gpu/__init__.py`: replace `get_provider(api_key)` with `get_provider(settings)` that dispatches on `settings.gpu_provider`\n- [ ] Update `.env.example` with `GPU_PROVIDER=local` and `LOCAL_TTS_URL=http://localhost:8005`\n- [ ] mypy passes on `app/gpu/`","status":"closed","priority":1,"issue_type":"bug","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T16:06:55Z","created_by":"Shubham Goel","updated_at":"2026-04-17T08:27:04Z","closed_at":"2026-04-17T13:57:04Z","close_reason":"Cancelled — not needed.","labels":["gpu","phase-2","regression"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-se6","depends_on_id":"Chatterbox-TTS-Server-w4n","type":"discovered-from","created_at":"2026-04-16T21:37:42Z","created_by":"shubham","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-qg9","title":"[P1-cleanup] Regenerate migration for VOICE_AUDIO blob-kind enum","description":"`app/models/enums.py` adds `VOICE_AUDIO = \"voice_audio\"` to BlobKind, but no Alembic revision exists for it. Postgres enum types require an explicit `ALTER TYPE ... ADD VALUE` — runtime inserts will fail with \"invalid input value for enum\".\n\nFix: generate a new Alembic revision that adds the enum value (`op.execute(\"ALTER TYPE blobkind ADD VALUE IF NOT EXISTS 'voice_audio'\")`). Test upgrade + downgrade paths.","acceptance_criteria":"- New Alembic revision present\n- `alembic upgrade head` adds enum value\n- Inserting a voice_audio blob works","status":"closed","priority":1,"issue_type":"bug","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:14:15Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:56:46Z","closed_at":"2026-04-16T18:26:46Z","close_reason":"Completed","labels":["creepy-brain","phase1-cleanup","schema"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-9te","title":"[P1-cleanup] Add alembic upgrade head to Dockerfile entrypoint","description":"The creepy-brain Dockerfile does not run `alembic upgrade head` before `uvicorn`. In compose/dev the DB schema has to be migrated by hand. In production this silently boots against an out-of-date schema.\n\nFix: add an entrypoint script (or inline CMD wrapper) that runs `alembic upgrade head` then exec's uvicorn. Gate behind an env flag (`RUN_MIGRATIONS=true`, default true) so prod can opt-out if they run migrations as a separate job.","acceptance_criteria":"- docker-compose up creates schema automatically\n- Migration failure aborts startup cleanly\n- Flag can disable auto-migrate for prod","status":"closed","priority":1,"issue_type":"bug","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:14:06Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:52:40Z","closed_at":"2026-04-16T18:22:40Z","close_reason":"Completed","labels":["creepy-brain","infra","phase1-cleanup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-05o","title":"[P1-cleanup] Replace dict[str, Any] in story_service with Pydantic models","description":"`app/services/story_service.py:59-63` uses `dict[str, Any]` for `bible_json` and `outline_json` parameters. CLAUDE.md explicitly forbids this: all structured data must use Pydantic models.\n\nAlso: `app/pipeline/orchestrator.py:54-57` calls the service with `.model_dump(mode=\"json\")` to satisfy the dict signature — eliminating the dict type removes the unnecessary round-trip.\n\nFix: accept the typed Pydantic models (StoryBible, StoryOutline) directly, serialize only at the ORM boundary using the existing PydanticType TypeDecorator.","acceptance_criteria":"- No `dict[str, Any]` parameters in story_service\n- Callers pass typed models\n- mypy --strict clean\n- Runtime behavior unchanged","status":"closed","priority":1,"issue_type":"bug","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:51Z","created_by":"Shubham Goel","updated_at":"2026-04-16T13:00:31Z","closed_at":"2026-04-16T18:30:31Z","close_reason":"Completed","labels":["creepy-brain","phase1-cleanup","typing"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-0xw","title":"[P1-cleanup] Add default to GpuPod.created_at (model + migration)","description":"`app/models/gpu_pod.py` — `created_at: Mapped[datetime]` is `nullable=False` but has no `default=` / `server_default=`. Any insert without an explicit timestamp will fail.\n\nFix: set `server_default=func.now()` on the column (and mirror in migration). Use `DateTime(timezone=True)`.","acceptance_criteria":"- `GpuPod.created_at` has server-side default\n- Migration updated\n- Insert without explicit value succeeds","status":"closed","priority":1,"issue_type":"bug","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:22Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:30:11Z","closed_at":"2026-04-16T18:00:11Z","close_reason":"Completed","labels":["bug","creepy-brain","phase1-cleanup","schema"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-3yw","title":"[P1-cleanup] Fix RunChunk.status to use ChunkStatus enum","description":"`app/models/run.py` — `RunChunk.status` is typed as `RunStatus` but should be `ChunkStatus`. These are semantically distinct state machines (runs vs individual chunks) and conflating them permits invalid states.\n\nFix: change the model column to `ChunkStatus`, regenerate the migration (or add an alter), and update any code reading/writing this column.","acceptance_criteria":"- `RunChunk.status` uses `ChunkStatus`\n- Alembic migration added\n- All read/write sites typed correctly\n- mypy clean","status":"closed","priority":1,"issue_type":"bug","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:18Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:29:19Z","closed_at":"2026-04-16T17:59:19Z","close_reason":"Completed","labels":["bug","creepy-brain","phase1-cleanup","schema"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-yo8","title":"[P1-cleanup] Unify transaction ownership between routes and services","description":"Transaction boundaries are inconsistent. `StoryService` commits internally (`await self._session.commit()`) while module-level services (`run_service`, `voice_service`, `blob_service`) only `flush()` and expect the caller/route to commit.\n\nDecide on one pattern (recommended: services flush, routes commit) and apply uniformly. This eliminates double-commit risk and makes transactional intent obvious at the route layer.","acceptance_criteria":"- All services follow one commit pattern (flush-only or commit-internally)\n- Routes updated accordingly\n- No service commits mid-request if route pattern is chosen (or vice versa)\n- Pattern documented in a short comment or docstring","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:03Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:55:43Z","closed_at":"2026-04-16T18:25:43Z","close_reason":"Completed","labels":["architecture","creepy-brain","phase1-cleanup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-f12","title":"[P1-cleanup] Retain fire-and-forget background task reference in stories route","description":"`app/routes/stories.py:120` uses `asyncio.create_task(_run())` without holding a reference. Per asyncio docs the task can be garbage collected mid-flight, silently canceling work.\n\nFix (interim until Hatchet lands): retain task in a set on `app.state.background_tasks` and add a done-callback to discard. Long term this gets replaced by Hatchet (bead 104), so keep the fix minimal.","acceptance_criteria":"- Background tasks are tracked on app.state (or equivalent)\n- Done-callbacks remove completed tasks from the set\n- Note in code or comment that this is interim pending Hatchet","status":"closed","priority":1,"issue_type":"bug","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:12:58Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:26:40Z","closed_at":"2026-04-16T17:56:40Z","close_reason":"Completed","labels":["bug","creepy-brain","phase1-cleanup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-ymi","title":"[P1-cleanup] Replace deprecated datetime.utcnow() with timezone-aware now()","description":"`datetime.utcnow()` is deprecated in Python 3.12+ and produces naive datetimes. Occurrences:\n- `app/pipeline/orchestrator.py:129`\n- `app/services/story_service.py:129`\n- `app/models/base.py:27` (`default=datetime.utcnow`)\n\nFix: use `datetime.now(timezone.utc)` everywhere. For SQLAlchemy defaults prefer `func.now()` / `server_default=func.now()` with `DateTime(timezone=True)` columns.","acceptance_criteria":"- No `datetime.utcnow` usage remains in creepy-brain\n- Timezone-aware datetimes stored in DB\n- Tests still pass","status":"closed","priority":1,"issue_type":"bug","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:12:53Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:24:36Z","closed_at":"2026-04-16T17:54:36Z","close_reason":"Completed","labels":["bug","creepy-brain","phase1-cleanup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-kay","title":"[P1-cleanup] Fix readyz to use DbSession dependency instead of generator iteration","description":"`app/routes/health.py:21` uses `async for session in get_session():` to borrow a DB session inside the handler. This bypasses FastAPI dependency cleanup and is fragile.\n\nFix: use the `DbSession` alias already defined in `app/db.py` so session lifecycle is handled by FastAPI.","acceptance_criteria":"- `readyz` handler uses `session: DbSession` parameter\n- No direct iteration over `get_session()` generator","status":"closed","priority":1,"issue_type":"bug","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:12:48Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:21:42Z","closed_at":"2026-04-16T17:51:42Z","close_reason":"Completed","labels":["bug","creepy-brain","phase1-cleanup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-f5r","title":"[P1-cleanup] Remove duplicate inline health/root endpoints from main.py","description":"`app/main.py:68-80` defines inline `@app.get(\"/health\")` and `@app.get(\"/\")` handlers that conflict/duplicate with `app/routes/health.py` (which already provides `/healthz` and `/readyz`). The inline endpoints bypass the structured router pattern.\n\nFix: delete inline endpoints in main.py, keep `/healthz` and `/readyz` on health.router. If `/` root info is desired, move it to a dedicated `routes/root.py` or into `health.py`.","acceptance_criteria":"- No `@app.get(...)` decorators remain in `main.py`\n- `/healthz` and `/readyz` served via `routes/health.py`\n- Root `/` served from a router, not inline","status":"closed","priority":1,"issue_type":"bug","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:12:45Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:24:30Z","closed_at":"2026-04-16T17:54:30Z","close_reason":"Completed","labels":["bug","creepy-brain","phase1-cleanup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-z9p","title":"[P3] Add on_failure cleanup hook","description":"Ensure GPU pods are terminated when workflow fails.\n\n## Problem\nIf any step fails, the GPU pod may be left running, incurring costs.\n\n## Solution\nHatchet's `on_failure` hook runs when any step fails.\n\n## Implementation\n```python\n# app/workflows/content_pipeline.py\n\n@hatchet.step()\nasync def cleanup_gpu_pod(ctx):\n    '''\n    Called automatically when workflow fails.\n    Terminates any GPU pod associated with this workflow.\n    '''\n    workflow_id = ctx.workflow_run_id\n    \n    # Get pod ID from database\n    async with get_session() as session:\n        workflow_service = WorkflowService(session)\n        workflow = await workflow_service.get(workflow_id)\n        \n        if not workflow:\n            return {\"skipped\": True, \"reason\": \"workflow not found\"}\n        \n        pod_id = workflow.gpu_pod_id\n        if not pod_id:\n            return {\"skipped\": True, \"reason\": \"no pod associated\"}\n    \n    # Terminate pod\n    provider = get_provider(settings)\n    try:\n        terminated = await provider.terminate_pod(pod_id)\n        \n        # Record termination\n        async with get_session() as session:\n            await WorkflowService(session).record_pod_termination(\n                workflow_id=workflow_id,\n                reason=\"workflow_failure\",\n            )\n        \n        return {\"terminated\": terminated, \"pod_id\": pod_id}\n    except Exception as e:\n        # Log but don't fail - this is cleanup\n        logger.error(\"Failed to terminate pod\", pod_id=pod_id, error=str(e))\n        return {\"error\": str(e), \"pod_id\": pod_id}\n\n\n@hatchet.workflow(\n    timeout=\"1h\",\n    on_failure=[cleanup_gpu_pod]  # \u003c-- Key line\n)\nclass ContentPipeline:\n    # ... steps\n```\n\n## Recording Failures\nAlso update workflow status when failure occurs:\n```python\n# In cleanup_gpu_pod\nasync with get_session() as session:\n    await WorkflowService(session).fail(\n        workflow_id=workflow_id,\n        error=str(ctx.trigger_error()),\n        failed_step=ctx.trigger_step(),\n    )\n```\n\n## Verification\n- [ ] Force a step to fail (e.g., invalid voice name)\n- [ ] Verify cleanup_gpu_pod step runs\n- [ ] Verify pod is terminated\n- [ ] Verify workflow status is \"failed\" in database","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:31:03Z","created_by":"Shubham Goel","updated_at":"2026-04-17T09:52:15Z","closed_at":"2026-04-17T15:22:15Z","close_reason":"Completed","labels":["cleanup","cost-control","phase-3","workflow"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-z9p","depends_on_id":"Chatterbox-TTS-Server-cmz","type":"blocks","created_at":"2026-04-16T16:05:19Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-z9p","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:51Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-duy","title":"[P3] Implement tts_synthesis step","description":"Implement the TTS synthesis step. All pre/post processing runs in creepy-brain; the GPU pod is stateless.\n\n## GPU Pod Contract\n\n```\nPOST /synthesize\nBody: { text: str, voice: str, seed: int }\nResponse: WAV bytes (Content-Type: audio/wav), HTTP 200 always\n```\n\nThe GPU pod runs Chatterbox only. No Qwen, no chunking, no validation, no job state.\n\n## Step Behavior\n\n1. Get story `full_text` from `generate_story` step output\n2. Normalize full text via LLM API (single call, result cached in DB by text hash)\n3. `chunk_text_by_sentences(normalized_text)` → list of chunk strings\n4. Spin up TTS GPU pod (idempotent by `workflow_id`)\n5. Wait for pod ready\n6. For each chunk (sequential):\n   - `POST /synthesize { text: chunk, voice, seed }` → WAV bytes\n   - `validate_chunk_audio(wav_bytes)` — numpy: RMS, peak amplitude, voiced ratio\n   - If validation fails: retry with `seed + attempt` (up to `max_chunk_retries`)\n   - Save WAV blob to `workflow_blobs` table, record in `workflow_chunks`\n7. Return chunk blob IDs and durations\n\n## Implementation\n\n```python\n# app/workflows/steps/tts.py\nimport asyncio\nimport hashlib\nimport httpx\nimport soundfile as sf\nimport io\nfrom app.gpu import get_provider\nfrom app.services.workflow_service import WorkflowService\nfrom app.services.blob_service import BlobService\nfrom app.text.chunking import chunk_text_by_sentences\nfrom app.text.normalization import normalize_text  # calls LLM API, not local Qwen\nfrom app.audio.validation import validate_chunk_audio\nfrom app.db import get_session\nfrom app.config import settings\n\nasync def execute(ctx) -\u003e dict:\n    story_output = ctx.step_output(\"generate_story\")\n    input_data = ctx.workflow_input()\n\n    full_text: str = story_output[\"full_text\"]\n    voice_name: str = input_data.get(\"voice_name\", \"Gianna\")\n    workflow_id: str = ctx.workflow_run_id\n    max_retries: int = settings.max_chunk_retries  # default 2\n\n    # 1. Normalize full text (cached by hash)\n    normalized_text = await normalize_text(full_text)\n\n    # 2. Chunk\n    chunks: list[str] = chunk_text_by_sentences(normalized_text, settings.chunk_size)\n\n    # 3. Spin up TTS pod\n    provider = get_provider(settings)\n    pod = await provider.create_pod(\n        spec=GpuPodSpec(gpu_type=settings.tts_gpu_type, image=settings.tts_server_image),\n        idempotency_key=f\"tts-{workflow_id}\",\n    )\n    async with get_session() as session:\n        await WorkflowService(session).record_pod(\n            workflow_id=workflow_id, pod_id=pod.id, provider=pod.provider,\n        )\n    pod = await provider.wait_for_ready(pod.id, timeout_sec=300)\n\n    # 4. Synthesize each chunk\n    chunk_results: list[dict] = []\n    async with httpx.AsyncClient(base_url=pod.endpoint_url, timeout=120) as client:\n        for idx, chunk_text in enumerate(chunks):\n            wav_bytes: bytes | None = None\n            attempts_used = 0\n\n            for attempt in range(max_retries + 1):\n                resp = await client.post(\"/synthesize\", json={\n                    \"text\": chunk_text,\n                    \"voice\": voice_name,\n                    \"seed\": settings.base_seed + attempt,\n                })\n                resp.raise_for_status()\n                candidate = resp.content  # WAV bytes\n\n                validation = validate_chunk_audio(candidate)\n                attempts_used = attempt + 1\n\n                if validation.passed:\n                    wav_bytes = candidate\n                    break\n                if wav_bytes is None:\n                    wav_bytes = candidate  # keep best-effort if all attempts fail\n\n            async with get_session() as session:\n                blob_id = await BlobService(session).save(\n                    workflow_id=workflow_id,\n                    blob_type=\"chunk_audio\",\n                    data=wav_bytes,\n                    mime_type=\"audio/wav\",\n                )\n                await WorkflowService(session).update_chunk(\n                    workflow_id=workflow_id,\n                    chunk_index=idx,\n                    chunk_text=chunk_text,\n                    tts_audio_blob_id=blob_id,\n                    attempts_used=attempts_used,\n                )\n\n            chunk_results.append({\"index\": idx, \"blob_id\": str(blob_id)})\n\n    return {\n        \"pod_id\": pod.id,\n        \"chunks\": chunk_results,\n        \"chunk_count\": len(chunk_results),\n    }\n```\n\n## Validation (runs in creepy-brain)\n\n```python\n# app/audio/validation.py\nimport numpy as np\nimport soundfile as sf\nimport io\n\ndef validate_chunk_audio(wav_bytes: bytes) -\u003e ChunkValidationResult:\n    audio, sr = sf.read(io.BytesIO(wav_bytes), dtype=\"float32\")\n    # RMS, peak amplitude, voiced ratio checks\n    ...\n```\n\n## Verification\n- [ ] Full story text normalized with single LLM API call before chunking\n- [ ] Normalization result cached in DB (no double-calling for retries)\n- [ ] TTS pod created idempotently\n- [ ] Each chunk synthesized via `POST /synthesize`, validated locally\n- [ ] Retry uses incremented seed, saves best-effort audio if all fail\n- [ ] Per-chunk progress visible in `workflow_chunks` table\n- [ ] WAV blobs stored in Postgres per chunk\n- [ ] Chunk text and blob IDs passed to next step","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:30:26Z","created_by":"Shubham Goel","updated_at":"2026-04-17T09:23:38Z","closed_at":"2026-04-17T14:53:38Z","close_reason":"Completed","labels":["phase-3","tts","workflow"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-duy","depends_on_id":"Chatterbox-TTS-Server-bjx","type":"blocks","created_at":"2026-04-16T16:05:17Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-duy","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:49Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-bjx","title":"[P3] Implement generate_story step","description":"Implement the story generation step that calls the LLM pipeline.\n\n## Step Behavior\n1. Get premise from workflow input\n2. Call story pipeline (architect → writer → reviewer)\n3. Save story to database\n4. Return story_id and full_text for next step\n\n## Implementation\n```python\n# app/workflows/steps/story.py\nfrom app.pipeline.orchestrator import StoryPipeline\nfrom app.services.story_service import StoryService\nfrom app.db import get_session\n\nasync def execute(ctx):\n    '''Generate story using LLM pipeline'''\n    input_data = ctx.workflow_input()\n    premise = input_data[\"premise\"]\n    target_word_count = input_data.get(\"target_word_count\", 5000)\n    \n    # Create story record\n    async with get_session() as session:\n        story_service = StoryService(session)\n        story = await story_service.create(\n            workflow_id=ctx.workflow_run_id,\n            premise=premise,\n            status=\"generating\"\n        )\n        story_id = story.id\n    \n    # Run pipeline with limits\n    pipeline = StoryPipeline(\n        max_outline_revisions=3,\n        max_act_revisions=2,\n        max_total_tokens=500_000,\n    )\n    \n    try:\n        result = await pipeline.generate(\n            premise=premise,\n            target_word_count=target_word_count,\n        )\n        \n        # Save completed story\n        async with get_session() as session:\n            story_service = StoryService(session)\n            await story_service.complete(\n                story_id=story_id,\n                title=result.title,\n                outline=result.outline,\n                full_text=result.full_text,\n                acts=result.acts,\n                tokens_used=result.total_tokens,\n            )\n        \n        return {\n            \"story_id\": str(story_id),\n            \"title\": result.title,\n            \"full_text\": result.full_text,\n            \"word_count\": len(result.full_text.split()),\n        }\n        \n    except Exception as e:\n        async with get_session() as session:\n            story_service = StoryService(session)\n            await story_service.fail(story_id, str(e))\n        raise\n```\n\n## Safety Limits\n- `max_outline_revisions=3` - Prevent infinite outline loops\n- `max_act_revisions=2` - Prevent infinite act rewrites\n- `max_total_tokens=500_000` - Budget cap\n- Step timeout: 15 minutes (in workflow definition)\n\n## Verification\n- [ ] Story generated from premise\n- [ ] Story saved to database with acts\n- [ ] Token budget respected\n- [ ] Timeout fires if LLM hangs\n- [ ] Error saved to DB on failure","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:30:12Z","created_by":"Shubham Goel","updated_at":"2026-04-17T09:11:27Z","closed_at":"2026-04-17T14:41:27Z","close_reason":"Completed","labels":["phase-3","story","workflow"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-bjx","depends_on_id":"Chatterbox-TTS-Server-cmz","type":"blocks","created_at":"2026-04-16T16:05:16Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-bjx","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:48Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-cmz","title":"[P3] Create ContentPipeline workflow definition","description":"Define the main content pipeline workflow with all 4 steps.\n\n## Workflow Overview\n```\nContentPipeline\n├── generate_story     (local, 15m timeout)\n├── tts_synthesis      (GPU pod, 30m timeout)\n├── image_generation   (GPU pod, 10m timeout)\n└── stitch_final       (local, 5m timeout)\n```\n\n## Files to Create\n```\napp/workflows/\n├── content_pipeline.py      # Main workflow class\n└── steps/\n    ├── __init__.py\n    ├── story.py             # generate_story step\n    ├── tts.py               # tts_synthesis step\n    ├── image.py             # image_generation step\n    └── stitch.py            # stitch_final step\n```\n\n## Workflow Definition\n```python\n# app/workflows/content_pipeline.py\nfrom . import hatchet\nfrom .steps import story, tts, image, stitch\n\n@hatchet.workflow(\n    timeout=\"1h\",\n    on_failure=[cleanup_gpu_pod]  # Always cleanup on failure\n)\nclass ContentPipeline:\n    '''End-to-end content pipeline: Story → TTS → Images → Stitch'''\n    \n    @hatchet.step(timeout=\"15m\", retries=2)\n    async def generate_story(self, ctx):\n        return await story.execute(ctx)\n    \n    @hatchet.step(timeout=\"30m\", parents=[\"generate_story\"])\n    async def tts_synthesis(self, ctx):\n        return await tts.execute(ctx)\n    \n    @hatchet.step(timeout=\"10m\", parents=[\"tts_synthesis\"])\n    async def image_generation(self, ctx):\n        return await image.execute(ctx)\n    \n    @hatchet.step(timeout=\"5m\", parents=[\"image_generation\"])\n    async def stitch_final(self, ctx):\n        return await stitch.execute(ctx)\n\n@hatchet.step()\nasync def cleanup_gpu_pod(ctx):\n    '''Called on workflow failure to ensure pod is terminated'''\n    workflow_id = ctx.workflow_run_id\n    # Get pod ID from workflow state and terminate\n```\n\n## Input Schema\n```python\nclass ContentPipelineInput(BaseModel):\n    premise: str                    # Story premise\n    voice_name: str = \"Gianna\"      # Voice for TTS\n    generate_images: bool = True    # Whether to generate images\n    stitch_video: bool = True       # Whether to stitch final video\n    target_word_count: int = 5000   # Target story length\n```\n\n## Verification\n- [ ] Workflow registered in Hatchet\n- [ ] Steps execute in correct order\n- [ ] on_failure cleanup runs when step fails\n- [ ] Workflow respects 1h overall timeout","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:30:03Z","created_by":"Shubham Goel","updated_at":"2026-04-17T08:55:08Z","closed_at":"2026-04-17T14:25:08Z","close_reason":"ContentPipeline workflow scaffold implemented with 4 typed step stubs, correct DAG wiring, timeouts, and retries. Registration deferred until step executors are built.","labels":["core","phase-3","workflow"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-cmz","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:48Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-cmz","depends_on_id":"Chatterbox-TTS-Server-vxb","type":"blocks","created_at":"2026-04-16T16:05:20Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-cmz","depends_on_id":"Chatterbox-TTS-Server-w4n","type":"blocks","created_at":"2026-04-16T16:05:21Z","created_by":"shubham","metadata":"{}"}],"dependency_count":2,"dependent_count":4,"comment_count":0}
{"id":"Chatterbox-TTS-Server-vxb","title":"[P2] Create simple test workflow","description":"Create a simple Hatchet workflow to verify the setup works end-to-end.\n\n## Purpose\nBefore building the full content pipeline, create a minimal workflow that:\n1. Has 2 steps (to verify DAG works)\n2. Uses timeouts\n3. Tests retry logic\n4. Can be triggered via API\n\n## Files to Create\n```\napp/workflows/\n└── test_workflow.py\n```\n\n## Implementation\n```python\n# app/workflows/test_workflow.py\nfrom . import hatchet\nimport asyncio\n\n@hatchet.workflow(timeout=\"5m\")\nclass TestWorkflow:\n    '''Simple test workflow for verification'''\n    \n    @hatchet.step(timeout=\"1m\")\n    async def step_one(self, ctx):\n        '''First step - simulates work'''\n        await asyncio.sleep(2)\n        return {\"message\": \"Step one complete\", \"value\": 42}\n    \n    @hatchet.step(timeout=\"1m\", parents=[\"step_one\"])\n    async def step_two(self, ctx):\n        '''Second step - uses output from step one'''\n        step_one_result = ctx.step_output(\"step_one\")\n        value = step_one_result[\"value\"]\n        return {\"message\": f\"Step two complete, doubled: {value * 2}\"}\n```\n\n## API Endpoint to Trigger\n```python\n# app/routes/workflows.py\n@router.post(\"/api/workflows/test\")\nasync def trigger_test_workflow():\n    from app.workflows import hatchet\n    from app.workflows.test_workflow import TestWorkflow\n    \n    run = await hatchet.client.admin.run_workflow(\n        TestWorkflow,\n        input={\"test\": True}\n    )\n    return {\"workflow_run_id\": run.workflow_run_id}\n```\n\n## Verification\n- [ ] POST /api/workflows/test triggers workflow\n- [ ] Workflow visible in Hatchet dashboard\n- [ ] Step one completes, step two runs after\n- [ ] Final result accessible\n- [ ] Workflow respects timeout (test by making sleep \u003e timeout)","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:29:51Z","created_by":"Shubham Goel","updated_at":"2026-04-16T14:29:48Z","closed_at":"2026-04-16T19:59:48Z","close_reason":"Completed","labels":["hatchet","phase-2","verification"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-vxb","depends_on_id":"Chatterbox-TTS-Server-9r1","type":"blocks","created_at":"2026-04-16T16:05:11Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-vxb","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:44Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-w4n","title":"[P2] Implement Local GPU provider (dev mode)","description":"Implement GpuProvider for local development that returns localhost.\n\n## Purpose\nDuring development, we don't want to spin up real GPU pods. LocalProvider:\n- Returns immediately with a fake pod pointing to localhost:8005\n- Assumes tts-server is running locally\n- No actual pod lifecycle management\n\n## Files to Create\n```\napp/gpu/\n└── local.py\n```\n\n## Implementation\n```python\n# app/gpu/local.py\nfrom datetime import datetime, timezone\nfrom .base import GpuProvider, GpuPod, GpuPodSpec, PodStatus\n\nclass LocalProvider(GpuProvider):\n    '''GPU provider for local development. Returns localhost URLs.'''\n    \n    def __init__(self, base_url: str = \"http://localhost:8005\"):\n        self.base_url = base_url\n        self._pods: dict[str, GpuPod] = {}\n        \n    async def create_pod(self, spec: GpuPodSpec, idempotency_key: str) -\u003e GpuPod:\n        if idempotency_key in self._pods:\n            return self._pods[idempotency_key]\n            \n        pod = GpuPod(\n            id=f\"local-{idempotency_key}\",\n            provider=\"local\",\n            status=PodStatus.READY,  # Immediately ready\n            endpoint_url=self.base_url,\n            gpu_type=\"local\",\n            cost_per_hour_cents=0,\n            created_at=datetime.now(timezone.utc),\n        )\n        self._pods[idempotency_key] = pod\n        return pod\n        \n    async def get_pod(self, pod_id: str) -\u003e GpuPod | None:\n        return self._pods.get(pod_id.replace(\"local-\", \"\"))\n        \n    async def terminate_pod(self, pod_id: str) -\u003e bool:\n        key = pod_id.replace(\"local-\", \"\")\n        if key in self._pods:\n            self._pods[key].status = PodStatus.TERMINATED\n            return True\n        return False\n        \n    async def wait_for_ready(self, pod_id: str, timeout_sec: int = 300) -\u003e GpuPod:\n        pod = await self.get_pod(pod_id)\n        if not pod:\n            raise ValueError(f\"Pod {pod_id} not found\")\n        return pod  # Already ready\n        \n    async def list_active_pods(self) -\u003e list[GpuPod]:\n        return [p for p in self._pods.values() if p.status != PodStatus.TERMINATED]\n```\n\n## Provider Factory\n```python\n# app/gpu/__init__.py\nfrom .base import GpuProvider, GpuPod, GpuPodSpec, PodStatus\nfrom .runpod import RunPodProvider\nfrom .local import LocalProvider\n\ndef get_provider(settings) -\u003e GpuProvider:\n    if settings.gpu_provider == \"local\":\n        return LocalProvider(settings.local_tts_url)\n    elif settings.gpu_provider == \"runpod\":\n        return RunPodProvider(settings.runpod_api_key)\n    else:\n        raise ValueError(f\"Unknown provider: {settings.gpu_provider}\")\n```\n\n## Configuration\n```python\n# app/config.py\nclass Settings:\n    gpu_provider: str = Field(default=\"local\")  # local | runpod\n    local_tts_url: str = Field(default=\"http://localhost:8005\")\n```\n\n## Verification\n- [ ] LocalProvider returns pod immediately\n- [ ] Pod endpoint_url is localhost:8005\n- [ ] terminate_pod marks pod as terminated\n- [ ] list_active_pods excludes terminated\n- [ ] get_provider() returns correct provider based on config","status":"closed","priority":1,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:29:43Z","created_by":"Shubham Goel","updated_at":"2026-04-16T14:03:27Z","closed_at":"2026-04-16T19:33:27Z","close_reason":"Won't implement — local dev provider not needed. Abstraction layer (base.py) remains in place for RunPod and future providers.","labels":["gpu","local-dev","phase-2"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-w4n","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:43Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-w4n","depends_on_id":"Chatterbox-TTS-Server-x7k","type":"blocks","created_at":"2026-04-16T16:05:11Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-8dn","title":"[P2] Implement RunPod GPU provider","description":"Implement GpuProvider for RunPod using their GraphQL API.\n\n## RunPod API\nRunPod uses GraphQL. Key mutations/queries:\n- `podRentInterruptable` - Create on-demand pod\n- `pod(id)` - Get pod status\n- `terminatePod(id)` - Stop and delete pod\n- `myself { pods { ... } }` - List all pods\n\n## Files to Create\n```\napp/gpu/\n└── runpod.py\n```\n\n## Implementation\n```python\n# app/gpu/runpod.py\nimport httpx\nfrom .base import GpuProvider, GpuPod, GpuPodSpec, PodStatus\n\nRUNPOD_API = \"https://api.runpod.io/graphql\"\n\nclass RunPodProvider(GpuProvider):\n    def __init__(self, api_key: str):\n        self.api_key = api_key\n        self.client = httpx.AsyncClient(\n            headers={\"Authorization\": f\"Bearer {api_key}\"}\n        )\n    \n    async def create_pod(self, spec: GpuPodSpec, idempotency_key: str) -\u003e GpuPod:\n        # Check if pod with this key already exists (idempotency)\n        existing = await self._find_pod_by_name(idempotency_key)\n        if existing:\n            return existing\n            \n        mutation = '''\n        mutation CreatePod($input: PodRentInterruptableInput!) {\n            podRentInterruptable(input: $input) {\n                id\n                desiredStatus\n                machine { gpu }\n            }\n        }\n        '''\n        # ... GraphQL call\n        \n    async def get_pod(self, pod_id: str) -\u003e GpuPod | None:\n        query = '''\n        query GetPod($id: String!) {\n            pod(input: {podId: $id}) {\n                id\n                desiredStatus\n                runtime { ports { ip privatePort publicPort } }\n                machine { gpu costPerHr }\n            }\n        }\n        '''\n        # ... GraphQL call\n        \n    async def terminate_pod(self, pod_id: str) -\u003e bool:\n        mutation = '''\n        mutation TerminatePod($id: String!) {\n            podTerminate(input: {podId: $id})\n        }\n        '''\n        # ... GraphQL call\n        \n    async def wait_for_ready(self, pod_id: str, timeout_sec: int = 300) -\u003e GpuPod:\n        # Poll get_pod until status is READY or timeout\n        # Also hit /health endpoint on pod to confirm API is up\n        \n    async def list_active_pods(self) -\u003e list[GpuPod]:\n        query = '''\n        query ListPods {\n            myself { pods { id desiredStatus ... } }\n        }\n        '''\n        # Filter for non-terminated pods\n```\n\n## Configuration\n```python\n# app/config.py\nclass Settings:\n    runpod_api_key: str = Field(default=\"\")\n```\n\n## Verification\n- [ ] Can list existing pods\n- [ ] Can create pod (with test image or real tts-server)\n- [ ] Can poll until pod is ready\n- [ ] Can terminate pod\n- [ ] Idempotency: calling create twice with same key returns same pod","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:29:33Z","created_by":"Shubham Goel","updated_at":"2026-04-16T14:29:48Z","closed_at":"2026-04-16T19:59:48Z","close_reason":"Completed","labels":["gpu","phase-2","runpod"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-8dn","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:43Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-8dn","depends_on_id":"Chatterbox-TTS-Server-x7k","type":"blocks","created_at":"2026-04-16T16:05:10Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-x7k","title":"[P2] Create GPU provider abstraction (base class)","description":"Define abstract interface for GPU providers to enable swapping between RunPod, Modal, local dev, etc.\n\n## Design\n```python\n# app/gpu/base.py\nfrom abc import ABC, abstractmethod\nfrom dataclasses import dataclass\nfrom enum import Enum\n\nclass PodStatus(str, Enum):\n    CREATING = \"creating\"\n    RUNNING = \"running\"\n    READY = \"ready\"\n    TERMINATED = \"terminated\"\n    ERROR = \"error\"\n\n@dataclass\nclass GpuPodSpec:\n    gpu_type: str = \"RTX 4090\"\n    image: str = \"shubh67678/chatterbox-tts-server:latest\"\n    disk_size_gb: int = 25\n    ports: list[int] = field(default_factory=lambda: [8005])\n    env: dict[str, str] = field(default_factory=dict)\n    \n@dataclass\nclass GpuPod:\n    id: str\n    provider: str\n    status: PodStatus\n    endpoint_url: str | None\n    gpu_type: str | None\n    cost_per_hour_cents: int | None\n    created_at: datetime\n    \nclass GpuProvider(ABC):\n    @abstractmethod\n    async def create_pod(\n        self, \n        spec: GpuPodSpec, \n        idempotency_key: str\n    ) -\u003e GpuPod:\n        '''Create a new GPU pod. Idempotency key ensures same pod returned if called twice.'''\n        \n    @abstractmethod\n    async def get_pod(self, pod_id: str) -\u003e GpuPod | None:\n        '''Get pod status by ID.'''\n        \n    @abstractmethod\n    async def terminate_pod(self, pod_id: str) -\u003e bool:\n        '''Terminate a pod. Returns True if terminated.'''\n        \n    @abstractmethod\n    async def wait_for_ready(\n        self, \n        pod_id: str, \n        timeout_sec: int = 300\n    ) -\u003e GpuPod:\n        '''Wait for pod to be ready (health check passes).'''\n        \n    @abstractmethod\n    async def list_active_pods(self) -\u003e list[GpuPod]:\n        '''List all active (non-terminated) pods. Used for recon.'''\n```\n\n## Files to Create\n```\napp/gpu/\n├── __init__.py\n├── base.py          # Abstract class and dataclasses\n└── schemas.py       # Re-export for convenience\n```\n\n## Verification\n- [ ] Classes importable: `from app.gpu import GpuProvider, GpuPodSpec, GpuPod`\n- [ ] Type hints work correctly\n- [ ] Can subclass GpuProvider without errors","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:29:22Z","created_by":"Shubham Goel","updated_at":"2026-04-16T13:23:22Z","closed_at":"2026-04-16T18:53:22Z","close_reason":"Completed: GPU provider abstraction base classes created in services/creepy-brain/app/gpu/","labels":["abstraction","gpu","phase-2"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-x7k","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:42Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-x7k","depends_on_id":"Chatterbox-TTS-Server-u2o","type":"blocks","created_at":"2026-04-16T16:06:24Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"id":"Chatterbox-TTS-Server-9r1","title":"[P2] Install hatchet-sdk and configure worker","description":"Set up Hatchet Python SDK and register as a worker.\n\n## Dependencies\nAdd to pyproject.toml:\n```toml\nhatchet-sdk = \"^1.0.0\"\n```\n\n## Files to Create\n```\napp/\n└── workflows/\n    ├── __init__.py          # Hatchet client setup\n    └── worker.py            # Worker registration\n```\n\n## app/workflows/__init__.py\n```python\nfrom hatchet_sdk import Hatchet\n\nhatchet = Hatchet()\n\n# Import all workflows to register them\nfrom .content_pipeline import ContentPipeline  # Will create later\n```\n\n## app/workflows/worker.py\n```python\nfrom . import hatchet\n\ndef start_worker():\n    worker = hatchet.worker(\"creepy-brain-worker\")\n    worker.start()\n```\n\n## Integration with FastAPI\nOption A: Run worker in separate process\nOption B: Run worker as background task in lifespan\n\nRecommend Option A for production (separate scaling).\n\n## Verification\n- [ ] Worker connects to Hatchet engine (visible in dashboard)\n- [ ] Worker appears in \"Workers\" tab in Hatchet UI\n- [ ] Simple test workflow can be triggered and executed","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:29:12Z","created_by":"Shubham Goel","updated_at":"2026-04-16T13:35:58Z","closed_at":"2026-04-16T19:05:58Z","close_reason":"Completed: hatchet-sdk installed, worker scaffolded with explicit WORKFLOWS registry","labels":["hatchet","phase-2"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-9r1","depends_on_id":"Chatterbox-TTS-Server-104","type":"blocks","created_at":"2026-04-16T16:05:09Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-9r1","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:41Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-104","title":"[P2] Add Hatchet engine to Docker Compose","description":"Integrate Hatchet workflow engine into the deployment.\n\n## What is Hatchet\nHatchet is a self-hosted workflow orchestration engine. It provides:\n- DAG-based workflow execution\n- Automatic retries and timeouts\n- Built-in dashboard UI\n- Postgres backend (shared with our app)\n\n## Changes to docker-compose.yml\n```yaml\nservices:\n  postgres:\n    # ... existing config\n    \n  hatchet-engine:\n    image: ghcr.io/hatchet-dev/hatchet/hatchet-engine:latest\n    environment:\n      DATABASE_URL: postgresql://...\n      SERVER_AUTH_COOKIE_SECRETS: ...\n      SERVER_AUTH_COOKIE_DOMAIN: localhost\n    ports:\n      - \"8080:8080\"    # Dashboard UI\n      - \"7070:7070\"    # gRPC for workers\n    depends_on:\n      - postgres\n      \n  creepy-brain:\n    # ... add HATCHET_CLIENT_TOKEN env var\n```\n\n## Hatchet Database Setup\nHatchet uses same Postgres but separate schema. Run their migration:\n```bash\ndocker exec hatchet-engine /hatchet-admin migration run\n```\n\n## Verification\n- [ ] `docker-compose up` starts all 3 services\n- [ ] Hatchet dashboard accessible at http://localhost:8080\n- [ ] Can create API token in Hatchet dashboard\n- [ ] Worker can connect (next story)","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:29:06Z","created_by":"Shubham Goel","updated_at":"2026-04-16T13:24:23Z","closed_at":"2026-04-16T18:54:23Z","close_reason":"Completed","labels":["hatchet","infrastructure","phase-2"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-104","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:40Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-104","depends_on_id":"Chatterbox-TTS-Server-u2o","type":"blocks","created_at":"2026-04-16T16:05:12Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-u2o","title":"[P1] Verify feature parity with existing services","description":"Ensure creepy-brain matches functionality of metadata-server and story-engine.\n\n## Test Cases\n\n### Metadata Parity\n- [ ] Create voice via API\n- [ ] List voices\n- [ ] Create run with chunks\n- [ ] Get run by ID\n- [ ] List runs with pagination\n- [ ] Upload/download audio blob\n\n### Story Parity\n- [ ] Generate story from premise\n- [ ] Poll story status\n- [ ] Get completed story with all acts\n- [ ] Handle generation failure gracefully\n\n### Infrastructure\n- [ ] Docker Compose starts cleanly\n- [ ] Logs are readable\n- [ ] Config loads from env vars\n- [ ] Health endpoint works\n\n## Documentation\n- [ ] README.md with setup instructions\n- [ ] .env.example complete","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:28:56Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:03:14Z","closed_at":"2026-04-16T17:33:14Z","close_reason":"All parity checks passed: voices CRUD, runs CRUD, blob upload/download, story generate/poll/list, health endpoint, structured logs, Docker Compose start. Fixed pydantic import bug, added python-multipart dep, created .env.example, wired env_file in docker-compose for ANTHROPIC_API_KEY, updated README.","labels":["phase-1","verification"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-u2o","depends_on_id":"Chatterbox-TTS-Server-d7c","type":"blocks","created_at":"2026-04-16T16:05:05Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-u2o","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:37Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-u2o","depends_on_id":"Chatterbox-TTS-Server-nzd","type":"blocks","created_at":"2026-04-16T16:05:05Z","created_by":"shubham","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"id":"Chatterbox-TTS-Server-nzd","title":"[P1] Migrate story-engine code","description":"Copy and adapt code from `services/story-engine/` to creepy-brain.\n\n## Source Files (story-engine)\n- `app/llm/` → `app/llm/`\n- `app/pipeline/` → `app/pipeline/`\n- `app/models/` → merge into `app/schemas/`\n\n## Key Changes\n1. Update imports\n2. Add SQLAlchemy persistence (currently in-memory)\n3. Merge config into single `config.py`\n4. Remove HTTP server (will be called directly)\n\n## Files to Create\n```\napp/\n├── llm/\n│   ├── __init__.py\n│   ├── client.py            # OpenAI/Anthropic client\n│   └── prompts.py           # Story prompts\n├── pipeline/\n│   ├── __init__.py\n│   ├── orchestrator.py      # Pipeline orchestrator\n│   ├── architect.py         # Outline generation\n│   ├── writer.py            # Act writing\n│   └── reviewer.py          # Review steps\n├── services/\n│   └── story_service.py     # Story CRUD\n├── routes/\n│   └── stories.py           # /api/stories/*\n└── schemas/\n    └── story.py\n```\n\n## Verification\n- [ ] `POST /api/stories/generate` with premise starts generation\n- [ ] `GET /api/stories/{id}` returns story with acts\n- [ ] Story saved to postgres","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:28:54Z","created_by":"Shubham Goel","updated_at":"2026-04-16T11:50:49Z","closed_at":"2026-04-16T17:20:49Z","close_reason":"Migrated story-engine LLM pipeline to creepy-brain: added app/llm/ (Anthropic SDK client + prompts), app/pipeline/ (models, architect, writer, reviewer, orchestrator with direct DB persistence), app/services/story_service.py, and app/routes/stories.py with POST /api/stories/generate + GET endpoints.","labels":["migration","phase-1"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-nzd","depends_on_id":"Chatterbox-TTS-Server-4me","type":"blocks","created_at":"2026-04-16T16:05:05Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-nzd","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:36Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-d7c","title":"[P1] Migrate metadata-server code","description":"Copy and adapt code from `services/metadata-server/` to creepy-brain.\n\n## Source Files (metadata-server)\n- `app/services/` → `app/services/`\n- `app/routes/` → `app/routes/`\n- `app/storage.py` → `app/services/blob_service.py`\n- `app/converters.py` → `app/schemas/`\n\n## Key Changes\n1. Update imports to use new model locations\n2. Adapt to async SQLAlchemy patterns\n3. Merge config into single `config.py`\n4. Remove auth (single-user system)\n\n## Files to Create\n```\napp/\n├── services/\n│   ├── __init__.py\n│   ├── run_service.py       # Run CRUD\n│   ├── voice_service.py     # Voice CRUD\n│   └── blob_service.py      # Blob storage\n├── routes/\n│   ├── __init__.py\n│   ├── runs.py              # /api/runs/*\n│   ├── voices.py            # /api/voices/*\n│   └── health.py            # /health\n└── schemas/\n    ├── __init__.py\n    ├── run.py\n    └── common.py\n```\n\n## Verification\n- [ ] `GET /api/runs` returns empty list\n- [ ] `GET /api/voices` returns empty list (or seeded voices)\n- [ ] Blob upload/download works","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:28:51Z","created_by":"Shubham Goel","updated_at":"2026-04-16T11:46:48Z","closed_at":"2026-04-16T17:16:48Z","close_reason":"Migrated metadata-server services/routes/schemas to creepy-brain. Pushed to main.","labels":["migration","phase-1"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-d7c","depends_on_id":"Chatterbox-TTS-Server-4me","type":"blocks","created_at":"2026-04-16T16:05:05Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-d7c","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:35Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-4me","title":"[P1] Set up SQLAlchemy models and Alembic migrations","description":"Create database models and migration infrastructure.\n\n## Files to Create\n```\napp/\n├── db.py                    # AsyncEngine, async_session_maker\n└── models/\n    ├── __init__.py          # Import all models\n    ├── base.py              # Base class with id, created_at\n    ├── workflow.py          # Workflow, WorkflowStep, WorkflowChunk\n    ├── story.py             # Story, StoryAct\n    ├── run.py               # Run, RunChunk\n    ├── voice.py             # Voice\n    └── gpu_pod.py           # GpuPod\n    \nalembic/\n├── env.py\n├── script.py.mako\n└── versions/\n    └── 0001_initial_schema.py\n```\n\n## Models\nPer schema in `docs/CONTENT_PIPELINE_ORCHESTRATION.md#database-schema`\n\n## Verification\n- [ ] `alembic upgrade head` runs without error\n- [ ] All tables created in postgres\n- [ ] Models can be imported: `from app.models import Workflow, Story, Run`","status":"closed","priority":1,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:28:48Z","created_by":"Shubham Goel","updated_at":"2026-04-16T11:16:41Z","closed_at":"2026-04-16T16:46:41Z","close_reason":"Completed SQLAlchemy models and Alembic migrations with all verification passing","labels":["database","phase-1"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-4me","depends_on_id":"Chatterbox-TTS-Server-4sz","type":"blocks","created_at":"2026-04-16T15:58:48Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-4me","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:34Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"id":"Chatterbox-TTS-Server-4sz","title":"[P1] Create creepy-brain service scaffold","description":"Create the base folder structure for the new creepy-brain service.\n\n## Location\n`services/creepy-brain/` - NEW folder, independent of existing services.\n\n## Files to Create\n```\nservices/creepy-brain/\n├── app/\n│   ├── __init__.py\n│   ├── main.py              # FastAPI factory stub\n│   └── config.py            # Pydantic settings\n├── pyproject.toml           # Dependencies\n├── Dockerfile\n├── docker-compose.yml       # brain + postgres (no hatchet yet)\n├── .env.example\n└── README.md\n```\n\n## Dependencies (pyproject.toml)\n- fastapi\n- uvicorn\n- sqlalchemy[asyncio]\n- asyncpg\n- pydantic-settings\n- httpx\n- structlog\n\n## Verification\n- [ ] `cd services/creepy-brain \u0026\u0026 docker-compose up` starts\n- [ ] App listens on port 8006\n- [ ] `GET /health` returns `{\"status\": \"ok\"}`","status":"closed","priority":1,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:28:42Z","created_by":"Shubham Goel","updated_at":"2026-04-16T10:46:06Z","closed_at":"2026-04-16T16:16:06Z","close_reason":"Scaffold complete - all files created and verified working","labels":["phase-1","scaffold"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-4sz","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:33Z","created_by":"shubham","metadata":"{}"}],"dependency_count":0,"dependent_count":4,"comment_count":0}
{"id":"Chatterbox-TTS-Server-fme","title":"Content Pipeline Workflow Orchestration","description":"End-to-end workflow orchestration system that chains: Story Generation → TTS Synthesis → Image Generation → Final Stitching.\n\n## Why\nCurrently, three services (tts-server, metadata-server, story-engine) operate independently with no cross-service orchestration. This creates manual intervention, no retry/resume, GPU cost leaks, and no unified visibility.\n\n## What\nConsolidate into a single **creepy-brain** service using Hatchet for workflow orchestration, with GPU provider abstraction and a web UI dashboard.\n\n## Key Decisions\n- **Workflow Engine**: Hatchet (self-hosted, Postgres-backed)\n- **ORM**: SQLAlchemy (Python standard)\n- **GPU Communication**: Per-chunk synchronous `POST /synthesize` calls — stateless GPU, retry logic in orchestrator\n- **TTS GPU image**: Chatterbox only — no Qwen, no chunking, no stitching, no job state. Single endpoint: `POST /synthesize {text, voice, seed} → WAV bytes`\n- **Image GPU image**: Separate Docker image (SDXL only) — never runs on same pod as TTS\n- **Text normalization**: Single LLM API call (OpenRouter/Anthropic) on full story text before chunking; result cached in DB\n- **Audio validation**: Runs in creepy-brain on returned WAV bytes (numpy: RMS, peak, voiced ratio). GPU returns HTTP 200 always — validation and retry are orchestration concerns, not synthesis concerns\n- **Blob Storage**: Postgres BYTEA (single DB, transactional)\n- **Location**: New folder `services/creepy-brain/` (independent of current code)\n\n## Documentation\nSee `docs/CONTENT_PIPELINE_ORCHESTRATION.md` for full details.","status":"closed","priority":1,"issue_type":"epic","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:28:09Z","created_by":"Shubham Goel","updated_at":"2026-04-18T10:24:55Z","closed_at":"2026-04-18T15:54:55Z","labels":["creepy-brain","hatchet","orchestration"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-6s0","title":"Verify: schema endpoint, UI rendering, form submission, mypy, build","description":"Run verification: GET /api/workflows/schema returns all steps, mypy --strict on json_schemas.py passes, esbuild bundle succeeds, docker compose rebuild works.","status":"open","priority":2,"issue_type":"task","owner":"shubh67678@gmail.com","created_at":"2026-04-25T18:34:34Z","created_by":"shubham","updated_at":"2026-04-25T18:34:34Z","dependencies":[{"issue_id":"chatterbox-tts-lite-6s0","depends_on_id":"chatterbox-tts-lite-3v3","type":"blocks","created_at":"2026-04-25T18:34:48Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-520","title":"Add collapsible step-section CSS to dashboard.html","description":"Add CSS rules for .step-section (details element), .step-toggle (enabled checkbox in summary), .step-fields (grid of form fields) in dark theme to dashboard.html.","status":"open","priority":2,"issue_type":"task","owner":"shubh67678@gmail.com","created_at":"2026-04-25T18:34:32Z","created_by":"shubham","updated_at":"2026-04-25T18:37:15Z","started_at":"2026-04-25T18:36:39Z","dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"chatterbox-tts-lite-x6w","title":"UI cannot view ingest-created stories (no acts, no workflow link)","description":"Stories created via POST /api/stories/ingest have full_text but no StoryAct rows and no workflow association. The current UI only renders story content via story.acts (workflow-detail page) or by workflow ID. Ingested stories appear in listings (if a list view existed) but cannot be read through the UI.\n\n**Fix options (pick one):**\n1. Have story_service.ingest() create a single synthetic StoryAct from full_text (act_number=1, title=story title, content=full_text)\n2. Add a standalone story detail page that falls back to rendering full_text when acts is empty\n3. Update workflow-detail to also render full_text when no acts exist\n\nOption 1 is simplest and keeps data consistent with the existing act-based model.\n\n**Acceptance criteria:**\n- [ ] A story created via /api/stories/ingest is viewable in the UI\n- [ ] The full text is displayed (either via synthetic act or direct full_text rendering)\n- [ ] Existing workflow-based stories are unaffected","status":"closed","priority":2,"issue_type":"bug","assignee":"shubham","owner":"shubh67678@gmail.com","created_at":"2026-04-25T17:22:19Z","created_by":"shubham","updated_at":"2026-04-25T17:57:39Z","started_at":"2026-04-25T17:49:51Z","closed_at":"2026-04-25T17:57:39Z","close_reason":"Completed: ingest() now creates a synthetic StoryAct so ingested stories are viewable via the act-based UI. XSS fixed in index.html, title truncated to 200 chars.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-5ss","title":"Create story-cron.sh script and output directory","description":"Create the cron wrapper script and output directory for automated story generation.\n\n**Files to create:**\n- `scripts/story-cron.sh` — Bash script that invokes Claude Code headless with the generate-story skill\n- `output/stories/.gitkeep` — Output directory for generated stories\n\n**Also update:** `.gitignore` to add `output/stories/*.md`\n\nThe cron script should:\n- cd to the project root\n- Source ANTHROPIC_API_KEY from env or ~/.anthropic_key\n- Run `claude -p` with the skill content, appropriate tools, and permission mode\n- Log to /var/log/story-gen.log","acceptance_criteria":"- [ ] `scripts/story-cron.sh` exists and is executable (chmod +x)\n- [ ] Script sources API key from env or fallback file\n- [ ] Script invokes `claude -p` with correct flags (--allowedTools, --permission-mode, --output-format)\n- [ ] `output/stories/.gitkeep` exists\n- [ ] `.gitignore` includes `output/stories/*.md`\n- [ ] `bash -n scripts/story-cron.sh` passes (valid syntax)\n- [ ] Running `scripts/story-cron.sh` manually produces log output","status":"open","priority":2,"issue_type":"task","owner":"shubh67678@gmail.com","created_at":"2026-04-25T17:04:06Z","created_by":"shubham","updated_at":"2026-04-25T17:04:06Z","dependencies":[{"issue_id":"chatterbox-tts-lite-5ss","depends_on_id":"chatterbox-tts-lite-6gc","type":"blocks","created_at":"2026-04-25T17:04:14Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-wkb","title":"Show music bed segments with prompts in workflow detail UI","description":"Store music mood prompts and intensity per segment in MusicSegmentResult, expose them via the API, and render per-segment audio + prompts in the workflow detail page alongside the full music bed.","status":"closed","priority":2,"issue_type":"feature","assignee":"shubham","owner":"shubh67678@gmail.com","created_at":"2026-04-25T11:38:03Z","created_by":"shubham","updated_at":"2026-04-25T11:46:21Z","started_at":"2026-04-25T11:38:09Z","closed_at":"2026-04-25T11:46:21Z","close_reason":"Implemented and deployed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-7y0","title":"Save SFX prompts and audio blobs to DB (like image generation)","description":"Follow the same pattern as image generation (`app/workflows/steps/image.py`) to persist SFX generation artifacts.\n\n**What needs to be done:**\n1. Save each SFX prompt to the DB record BEFORE the GPU pod spins up (mirrors `_generate_and_save_prompts`)\n2. Save the returned audio blob (WAV/MP3) via `blob_service.store` with `BlobType.SFX`\n3. Link blob IDs back to the workflow/chunk records\n4. Expose blob IDs in `result_json` so the UI can display them (mirrors `image_blob_id` in `SceneImageResult`)\n5. Resume logic: skip re-generating if prompt already saved (idempotent)\n\n**Acceptance criteria:**\n- SFX prompt string(s) written to DB before GPU pod starts\n- Audio bytes stored as blobs; blob_ids in step result_json\n- mypy --strict passes, pytest passes","status":"open","priority":2,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-24T15:25:12Z","created_by":"Codex","updated_at":"2026-04-24T15:25:12Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-e0v","title":"Save music bed prompts and audio blobs to DB (like image generation)","description":"Follow the same pattern as image generation (`app/workflows/steps/image.py`) to persist music bed generation artifacts.\n\n**What needs to be done:**\n1. Save the music generation prompt to the DB record BEFORE the GPU pod spins up (mirrors `_generate_and_save_prompts`)\n2. Save the returned audio blob (MP3/WAV) via `blob_service.store` with `BlobType.MUSIC`\n3. Link the blob ID back to the workflow step result\n4. Expose the blob ID in `result_json` so the UI can display it (mirrors `image_blob_id` in `SceneImageResult`)\n5. Resume logic: skip re-generating if prompt already saved (idempotent)\n\n**Acceptance criteria:**\n- Music prompt string written to DB before GPU pod starts\n- Audio bytes stored as blob; blob_id in step result_json\n- mypy --strict passes, pytest passes","status":"open","priority":2,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-24T15:25:07Z","created_by":"Codex","updated_at":"2026-04-24T15:25:07Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-cm1","title":"Add music-server and sfx-server to CI/CD Docker builds","description":"Add `music-server` and `sfx-server` to the GitHub Actions CI/CD build matrix.\n\n**Changes to `.github/workflows/docker.yml` (or equivalent):**\n- Add `music-server` to the build matrix alongside tts-server and image-server\n- Add `sfx-server` to the build matrix\n- Push images to:\n  - `ghcr.io/sgoel1220/music-server:main`\n  - `ghcr.io/sgoel1220/sfx-server:main`\n\n**Pattern:** Follow existing tts-server/image-server CI config — same triggers, same GHCR push logic.","acceptance_criteria":"- CI builds music-server and sfx-server Docker images on push to main\n- Images pushed to GHCR with correct tags\n- Build matrix includes all 4 GPU services","status":"closed","priority":2,"issue_type":"task","assignee":"Codex","owner":"codex@local","created_at":"2026-04-23T12:25:00Z","created_by":"Codex","updated_at":"2026-04-24T12:11:10Z","started_at":"2026-04-24T12:09:42Z","closed_at":"2026-04-24T12:11:10Z","close_reason":"Already implemented: music-server and sfx-server were added to docker-build.yml matrix in prior work alongside the server implementations.","labels":["audio-design","ci-cd","phase-8"],"dependencies":[{"issue_id":"chatterbox-tts-lite-cm1","depends_on_id":"chatterbox-tts-lite-sqf","type":"blocks","created_at":"2026-04-23T12:26:40Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-cm1","depends_on_id":"chatterbox-tts-lite-zrn","type":"blocks","created_at":"2026-04-23T12:26:35Z","created_by":"shubham","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-zuc","title":"Wire audio design steps into pipeline DAG","description":"Add music_generation and sfx_generation steps to the content pipeline definition and update the on_complete hook.\n\n**File to modify:** `app/workflows/content_pipeline.py`\n\n**Changes:**\n1. Import music_generation and sfx_generation step modules\n2. Add StepDef for music_generation (parents=[\"tts_synthesis\"], timeout=3600, retries=2)\n3. Add StepDef for sfx_generation (parents=[\"tts_synthesis\"], timeout=3600, retries=2)\n4. Update stitch_final parents to [\"image_generation\", \"music_generation\", \"sfx_generation\"]\n5. Update `_on_pipeline_complete` to extract music_bed_blob_id from music output and store in WorkflowResultSchema\n\n**New DAG:** story → tts → {music, sfx, image} → stitch → waveform → cleanup","acceptance_criteria":"- [ ] music_generation and sfx_generation steps in pipeline definition\n- [ ] Both run in parallel after tts_synthesis\n- [ ] stitch_final waits for all three parallel steps\n- [ ] on_complete hook extracts music_bed_blob_id\n- [ ] Pipeline works with all audio flags false (backward compatible)\n- [ ] Pipeline works with generate_music=True and/or generate_sfx=True\n- [ ] mypy --strict passes","status":"closed","priority":2,"issue_type":"task","owner":"codex@local","created_at":"2026-04-23T12:19:04Z","created_by":"Codex","updated_at":"2026-04-23T12:27:30Z","closed_at":"2026-04-23T12:27:30Z","close_reason":"Superseded by detailed phased beads","labels":["audio-design","pipeline"],"dependencies":[{"issue_id":"chatterbox-tts-lite-zuc","depends_on_id":"chatterbox-tts-lite-6kz","type":"blocks","created_at":"2026-04-23T12:20:19Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-zuc","depends_on_id":"chatterbox-tts-lite-b77","type":"blocks","created_at":"2026-04-23T12:20:10Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-zuc","depends_on_id":"chatterbox-tts-lite-bw9","type":"blocks","created_at":"2026-04-23T12:20:25Z","created_by":"shubham","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-bw9","title":"Add audio mixing to stitch step","description":"Modify the stitch step to mix background music and SFX under the narration audio before MP3 encoding.\n\n**File to modify:** `app/workflows/steps/stitch.py`\n\n**Changes:**\n1. After concatenating narration WAV, check for `music_generation` parent output\n2. If music bed exists: load MUSIC_BED blob, resample to match narration sample rate, apply -12dB gain, mix with narration\n3. Check for `sfx_generation` parent output\n4. If SFX clips exist: load each SFX_AUDIO blob, calculate placement timestamp from scene timing + position (beginning/middle/end), apply -6dB gain, mix into audio\n5. Encode final mixed audio to MP3\n6. Both music and SFX are optional — step works with none, one, or both\n\n**New helper:** `_mix_audio_layers(narration, music_bed, sfx_clips, sample_rate, music_gain_db, sfx_gain_db) -\u003e np.ndarray`\n\n**Audio mixing is static for v1:** constant volume reduction, no dynamic ducking.","acceptance_criteria":"- [ ] Stitch works unchanged when no music/sfx outputs present\n- [ ] Music bed mixed at -12dB under narration\n- [ ] SFX clips placed at correct timestamps based on scene timing + position\n- [ ] SFX mixed at -6dB\n- [ ] Audio resampled if sample rates don't match\n- [ ] Final MP3 includes all layers\n- [ ] No clipping (output normalized if needed)\n- [ ] mypy --strict passes","status":"closed","priority":2,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-23T12:18:57Z","created_by":"Codex","updated_at":"2026-04-23T12:27:29Z","closed_at":"2026-04-23T12:27:29Z","close_reason":"Superseded by detailed phased beads","labels":["audio-design","pipeline"],"dependencies":[{"issue_id":"chatterbox-tts-lite-bw9","depends_on_id":"chatterbox-tts-lite-6kz","type":"blocks","created_at":"2026-04-23T12:20:05Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-bw9","depends_on_id":"chatterbox-tts-lite-b77","type":"blocks","created_at":"2026-04-23T12:20:00Z","created_by":"shubham","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"id":"chatterbox-tts-lite-6kz","title":"Implement sfx_generation pipeline step","description":"Create the SFX generation workflow step that generates per-scene sound effects.\n\n**File to create:** `app/workflows/steps/sfx_generation.py`\n\n**Logic:**\n1. Check `input.generate_sfx` flag → SkippedStepOutput if false\n2. Fetch chunks from DB, group into scenes\n3. Resume check: skip scenes with existing SFX_AUDIO blobs\n4. Generate SFX cue lists via LLM (before GPU spin-up)\n5. Spin up sfx-server GPU pod via `workflow_gpu_pod` with `_sfx_pod_spec()`\n6. For each scene's cues: `POST /generate` with SFX description + duration\n7. Store each SFX clip as BlobType.SFX_AUDIO blob\n8. Return SfxGenerationStepOutput with all clip metadata\n\n**Key patterns:** Follow image step pattern. Each SFX cue has position (beginning/middle/end) for placement in stitch step.","acceptance_criteria":"- [ ] Step skips cleanly when generate_sfx=False\n- [ ] Sub-unit resume: skips already-completed scenes\n- [ ] LLM cue generation BEFORE GPU pod spin-up\n- [ ] GPU pod uses workflow_gpu_pod context manager\n- [ ] Each SFX clip stored as SFX_AUDIO blob\n- [ ] Position metadata (beginning/middle/end) preserved in output\n- [ ] Output model matches SfxGenerationStepOutput schema\n- [ ] mypy --strict passes","status":"closed","priority":2,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-23T12:18:48Z","created_by":"Codex","updated_at":"2026-04-23T12:27:28Z","closed_at":"2026-04-23T12:27:28Z","close_reason":"Superseded by detailed phased beads","labels":["audio-design","pipeline"],"dependencies":[{"issue_id":"chatterbox-tts-lite-6kz","depends_on_id":"chatterbox-tts-lite-92j","type":"blocks","created_at":"2026-04-23T12:19:36Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-6kz","depends_on_id":"chatterbox-tts-lite-d03","type":"blocks","created_at":"2026-04-23T12:19:55Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-6kz","depends_on_id":"chatterbox-tts-lite-xzu","type":"blocks","created_at":"2026-04-23T12:19:51Z","created_by":"shubham","metadata":"{}"}],"dependency_count":3,"dependent_count":2,"comment_count":0}
{"id":"chatterbox-tts-lite-b77","title":"Implement music_generation pipeline step","description":"Create the music generation workflow step that generates per-scene background music and chains it into a full-length music bed.\n\n**File to create:** `app/workflows/steps/music_generation.py`\n\n**Logic:**\n1. Check `input.generate_music` flag → SkippedStepOutput if false\n2. Fetch chunks from DB, group into scenes (reuse `group_chunks_into_scenes`)\n3. Resume check: skip scenes with existing MUSIC_AUDIO blobs\n4. Generate music mood prompts via LLM (before GPU spin-up)\n5. Spin up music-server GPU pod via `workflow_gpu_pod` with `_music_pod_spec()`\n6. Scene 0: `POST /generate` with mood prompt + scene duration\n7. Scenes 1-N: `POST /outpaint` with tail of previous segment as context (ACE-Step continuation)\n8. Store each segment as BlobType.MUSIC_AUDIO blob\n9. Crossfade + concatenate all segments into full-length music bed\n10. Store concatenated bed as BlobType.MUSIC_BED blob\n11. Return MusicGenerationStepOutput\n\n**Key patterns:** Follow image step pattern for GPU pod lifecycle, resume support, LLM prompt generation before GPU spin-up.","acceptance_criteria":"- [ ] Step skips cleanly when generate_music=False\n- [ ] Scene grouping uses existing group_chunks_into_scenes\n- [ ] Sub-unit resume: skips already-completed scenes\n- [ ] LLM prompts generated BEFORE GPU pod spin-up\n- [ ] GPU pod uses workflow_gpu_pod context manager (auto-terminate)\n- [ ] Outpainting chains segments with context from previous segment\n- [ ] Per-scene blobs stored as MUSIC_AUDIO\n- [ ] Full music bed stored as MUSIC_BED\n- [ ] Crossfade between segments (2-3s overlap)\n- [ ] Output model matches MusicGenerationStepOutput schema\n- [ ] mypy --strict passes","status":"closed","priority":2,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-23T12:18:43Z","created_by":"Codex","updated_at":"2026-04-23T12:27:29Z","closed_at":"2026-04-23T12:27:29Z","close_reason":"Superseded by detailed phased beads","labels":["audio-design","pipeline"],"dependencies":[{"issue_id":"chatterbox-tts-lite-b77","depends_on_id":"chatterbox-tts-lite-92j","type":"blocks","created_at":"2026-04-23T12:19:31Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-b77","depends_on_id":"chatterbox-tts-lite-fia","type":"blocks","created_at":"2026-04-23T12:19:46Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-b77","depends_on_id":"chatterbox-tts-lite-xzu","type":"blocks","created_at":"2026-04-23T12:19:41Z","created_by":"shubham","metadata":"{}"}],"dependency_count":3,"dependent_count":2,"comment_count":0}
{"id":"chatterbox-tts-lite-xzu","title":"Create LLM audio design prompt generation module","description":"Create LLM prompt generation for music moods and SFX cue lists, following the pattern in `app/llm/image_prompts.py`.\n\n**File to create:** `app/llm/audio_design.py`\n\n**Music mood prompts:**\n- `MusicMoodResult(prompt: str, intensity: int)` — ACE-Step formatted prompt\n- `generate_music_mood(scene_text: str) -\u003e MusicMoodResult`\n- System prompt teaches LLM the ACE-Step prompt format (genre, BPM, instrumentation, mood)\n\n**SFX cue lists:**\n- `SfxCue(description: str, position: str, duration_sec: float)` — per-cue model\n- `SfxCueListResult(cues: list[SfxCue])` — max 5 cues per scene\n- `generate_sfx_cues(scene_text: str) -\u003e SfxCueListResult`\n- System prompt identifies concrete sound effects in the narration\n\n**Pattern:** Use `generate_structured()` with Pydantic validation + retries, same as image prompts.","acceptance_criteria":"- [ ] MusicMoodResult and SfxCueListResult Pydantic models with field validation\n- [ ] generate_music_mood() returns valid ACE-Step prompts from scene text\n- [ ] generate_sfx_cues() returns valid SFX cue lists (max 5 per scene)\n- [ ] Uses generate_structured() with retry on validation failure\n- [ ] System prompts produce good results for horror/creepypasta content\n- [ ] Field validators enforce word count limits, required elements\n- [ ] mypy --strict passes","status":"closed","priority":2,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-23T12:18:31Z","created_by":"Codex","updated_at":"2026-04-23T12:27:28Z","closed_at":"2026-04-23T12:27:28Z","close_reason":"Superseded by detailed phased beads","labels":["audio-design","llm"],"dependencies":[{"issue_id":"chatterbox-tts-lite-xzu","depends_on_id":"chatterbox-tts-lite-92j","type":"blocks","created_at":"2026-04-23T12:19:26Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"id":"chatterbox-tts-lite-22i","title":"Redesign waveform_overlay as full-frame waveform visualization","description":"Replace the 140px player-bar overlay with a full-frame standalone waveform visualization: dark navy background (#1a2030), full-width symmetric bars above/below center, monochromatic blue-grey (#3a4a5e), cross/diamond burst at the playhead that pulses per word via per-frame amplitude. Quiet bars are tiny nubs (flat dotted rope). Staircase quantization gives pixelated aesthetic. Drop shadow on tall bars.","status":"closed","priority":2,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-22T01:25:55Z","created_by":"Codex","updated_at":"2026-04-22T01:27:35Z","started_at":"2026-04-22T01:26:03Z","closed_at":"2026-04-22T01:27:35Z","close_reason":"Implemented full-frame waveform visualization; pushed in 4f2af2b","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-exd","title":"Replace static waveform bars with scrolling waveform","description":"The waveform overlay currently shows all 120 bars statically (heights fixed, only color changes from gray to pink as progress advances). The user wants the bars to scroll as the audio plays — showing the actual audio amplitude centered at the current playback position. This creates a dynamic \"scrolling waveform\" effect like a DAW or professional audio player, where left bars (played) are pink and right bars (upcoming) are gray, and the waveform continuously scrolls leftward as playback progresses.","status":"closed","priority":2,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-22T01:15:43Z","created_by":"Codex","updated_at":"2026-04-22T01:17:12Z","started_at":"2026-04-22T01:15:51Z","closed_at":"2026-04-22T01:17:12Z","close_reason":"Implemented scrolling waveform; pushed in 2c96a78","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-4c2","title":"Add animated waveform overlay pipeline step","description":"Add waveform_overlay step after stitch_final that burns a 140px animated player bar (waveform + progress bar + album art + title) into a second video. Both clean and waveform videos stored and downloadable.","status":"closed","priority":2,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-21T16:37:45Z","created_by":"Codex","updated_at":"2026-04-21T16:42:21Z","started_at":"2026-04-21T16:37:53Z","closed_at":"2026-04-21T16:42:21Z","close_reason":"Implemented and pushed. All tests pass, mypy clean.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-1v3","title":"Centralized error handling: single exception handler, kill per-route try/except","description":"Add FastAPI exception handlers to main.py for ResourceNotFoundError→404 and ValueError→400. Remove the 4 pure-translation try/except blocks in runs.py, blobs.py, and workflows.py. Keep business-logic try/except blocks that do real work (mark FAILED, per-chunk continue-on-error, 416 range error).","status":"closed","priority":2,"issue_type":"task","assignee":"shubham","owner":"shubh67678@gmail.com","created_at":"2026-04-19T19:56:00Z","created_by":"shubham","updated_at":"2026-04-19T20:01:30Z","started_at":"2026-04-19T19:56:05Z","closed_at":"2026-04-19T20:01:30Z","close_reason":"Implemented and pushed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-037","title":"feat: fork-from-step on workflow detail UI","description":"Add \"Fork from here\" button per step in the workflow detail page. Clicking it creates a new workflow that copies all prior step data (story, chunks, scenes) and re-runs from the selected step onward. The original workflow is untouched.\n\nAcceptance:\n- POST /api/workflows/{id}/fork { from_step } endpoint\n- Copies workflow_chunks/workflow_scenes to new workflow_id\n- Seeds completed WorkflowStep rows for predecessors\n- Triggers engine on the new workflow\n- UI button visible on each completed step of a completed workflow\n- Navigates to new workflow on fork","status":"closed","priority":2,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-19T13:52:16Z","created_by":"Codex","updated_at":"2026-04-19T13:59:53Z","started_at":"2026-04-19T13:52:34Z","closed_at":"2026-04-19T13:59:53Z","close_reason":"Implemented and pushed. Fork-from-step feature complete.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-sp3","title":"Show stitched audio/video output in workflow detail UI","description":"Replace the raw JSON Result block in workflow-detail.ts with a proper Output section. Should show an HTML5 audio player for final_audio_blob_id, an HTML5 video player for final_video_blob_id (if present), duration/chunk/cost stats, and download links. Blob endpoint is /api/blobs/{id} with range request support.","status":"closed","priority":2,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-19T12:48:39Z","created_by":"Codex","updated_at":"2026-04-19T12:49:47Z","started_at":"2026-04-19T12:48:44Z","closed_at":"2026-04-19T12:49:47Z","close_reason":"Implemented and pushed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-2jr","title":"Add live step logs viewer to workflow detail UI","description":"Users want to see log output from each pipeline step and GPU containers on the workflow detail page. Implement in-memory per-workflow log buffer, expose via GET /api/workflows/{id}/logs, and show a live-updating Logs section in the UI.","status":"closed","priority":2,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-19T12:40:03Z","created_by":"Codex","updated_at":"2026-04-19T12:44:28Z","started_at":"2026-04-19T12:40:19Z","closed_at":"2026-04-19T12:44:28Z","close_reason":"Implemented and deployed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-lzd","title":"Add TTS retry UI: per-chunk and full-step retry","description":"In the dashboard UI, add retry controls for TTS:\n1. Per-chunk retry button next to each failed/completed chunk\n2. Full TTS step retry button\nThese should call the existing retry API endpoints.","status":"closed","priority":2,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-19T08:59:54Z","created_by":"Codex","updated_at":"2026-04-19T09:11:42Z","started_at":"2026-04-19T08:59:59Z","closed_at":"2026-04-19T09:11:42Z","close_reason":"Implemented and pushed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-da7","title":"Fix GpuPod type mismatch with backend schema","description":"Frontend GpuPod interface has fields that don't exist in backend GpuPodResponse: gpu_type, cost_per_hour_cents, workflow_id. Backend only returns: id, provider, status, created_at, ready_at, terminated_at, total_cost_cents. Also isActivePod only checks terminated/error but backend has stopped as non-active too. Fix types to match backend and update isActivePod.","status":"open","priority":2,"issue_type":"bug","owner":"codex@local","created_at":"2026-04-18T21:21:09Z","created_by":"Codex","updated_at":"2026-04-18T21:21:09Z","labels":["frontend","types"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-k1d","title":"Step-level retry API + pause/resume endpoints","description":"Add PAUSED status to WorkflowStatus enum. Add pause() and resume_from_db() methods to WorkflowEngine. Add POST /{id}/retry-step, POST /{id}/pause, POST /{id}/resume API endpoints with proper Pydantic models.","status":"closed","priority":2,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-18T17:57:47Z","created_by":"Codex","updated_at":"2026-04-18T18:19:35Z","started_at":"2026-04-18T18:11:32Z","closed_at":"2026-04-18T18:19:35Z","close_reason":"Added PAUSED status, engine pause/resume_from_db methods, and POST /{id}/retry-step, /{id}/pause, /{id}/resume API endpoints.","dependencies":[{"issue_id":"chatterbox-tts-lite-k1d","depends_on_id":"chatterbox-tts-lite-o0s","type":"blocks","created_at":"2026-04-18T17:57:51Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-o0s","title":"Make every step internally resumable (TTS, stitch, story gen)","description":"Add sub-unit resume to TTS step (skip already-completed chunks), stitch step (skip if final blobs exist), and story gen step (skip if completed story exists). Add duration_sec to ChunkForImageStep model.","status":"closed","priority":2,"issue_type":"feature","assignee":"Codex","owner":"codex@local","created_at":"2026-04-18T17:57:27Z","created_by":"Codex","updated_at":"2026-04-18T18:11:04Z","started_at":"2026-04-18T17:57:58Z","closed_at":"2026-04-18T18:11:04Z","close_reason":"All steps (TTS, stitch, story) now support sub-unit resume. TTS skips completed chunks, stitch checks for existing final blobs, story reuses completed stories.","dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"chatterbox-tts-lite-bt8","title":"Extract pod lifecycle helpers into app.gpu.lifecycle module","description":"During the tts.py refactor, code was written that imports create_recorded_pod and wait_for_recorded_ready from app.gpu.lifecycle, but those functions don't exist yet. The tts.py changes were discarded to avoid a runtime breakage.\n\nWhat needs to be done:\n1. Create app/gpu/lifecycle.py with create_recorded_pod and wait_for_recorded_ready helpers\n2. Move pod creation/readiness logic out of tts.py workflow step into these helpers\n3. Update tts.py to import from app.gpu.lifecycle\n4. Add type annotations and run mypy --strict\n5. Test that workflow still runs end-to-end\n\nThis is a refactor to improve separation of concerns between the workflow step and the GPU pod lifecycle management.","status":"closed","priority":2,"issue_type":"task","assignee":"Codex","owner":"codex@local","created_at":"2026-04-18T16:17:43Z","created_by":"Codex","updated_at":"2026-04-19T19:56:29Z","started_at":"2026-04-19T19:55:25Z","closed_at":"2026-04-19T19:56:29Z","close_reason":"workflow_gpu_pod added to lifecycle.py; tts.py and image.py updated; mypy --strict passes","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-0gx","title":"Fix RunPodProvider parse behavior or tests","description":"Targeted pytest run in services/creepy-brain found existing failures in tests/test_runpod_parse_pod.py: RunPodProvider._parse_pod always builds proxy URLs, while tests expect public IP/privatePort matching and None for non-public or non-matching ports; tests also expect a TypeError for non-dict machine data while implementation silently returns None fields.\\n\\nThis is out of scope for the DRY refactor. Decide the intended RunPod endpoint contract, then update either _parse_pod or the tests so they agree.\\n\\nAcceptance criteria:\\n- /tmp or clean venv pytest tests/test_runpod_parse_pod.py passes.\\n- Endpoint URL behavior is documented in RunPodProvider._parse_pod or tests.\\n- Non-dict machine behavior is explicit and tested.","status":"closed","priority":2,"issue_type":"bug","owner":"codex@local","created_at":"2026-04-18T15:15:44Z","created_by":"Codex","updated_at":"2026-04-18T15:40:06Z","closed_at":"2026-04-18T15:40:06Z","close_reason":"Fixed _parse_pod to use IP/port URLs and raise TypeError for non-dict machine; all 14 tests pass","labels":["gpu","runpod","tests"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-kdg","title":"Make local quality gates reproducible","description":"During verification, python3 -m pytest, python3 -m black, and python3 -m ruff were unavailable in the current environment, and full strict mypy without --ignore-missing-imports failed on missing third-party modules such as numpy, structlog, and anthropic. Provide a documented dev environment setup or tooling wrapper so agents can run the required tests, formatter, linter, and strict mypy consistently.","notes":"Also observed that the required /adversarial-review command is not available in this shell environment (/bin/bash: /adversarial-review: No such file or directory). The reproducible gate wrapper should cover this required review step too.","status":"open","priority":2,"issue_type":"task","owner":"codex@local","created_at":"2026-04-18T15:15:32Z","created_by":"Codex","updated_at":"2026-04-18T15:16:13Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-s9j","title":"Persist workflow step output models","description":"WorkflowRunner._load_completed_steps expects WorkflowStep.output_json to contain completed parent outputs, but _db_complete_step/WorkflowService.complete_step never persist the output model. Retry/resume paths can therefore hydrate EmptyStepOutput and downstream steps may fail because required parent fields are missing. Add an output parameter to completion persistence, store compatible Pydantic step outputs, and cover retry/resume with tests.","status":"closed","priority":2,"issue_type":"bug","owner":"codex@local","created_at":"2026-04-18T15:14:43Z","created_by":"Codex","updated_at":"2026-04-18T15:45:57Z","closed_at":"2026-04-18T15:45:57Z","close_reason":"Closed","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-8dy","title":"Fix creepy-brain editable install packaging","description":"Running python -m pip install -e '.[dev]' from services/creepy-brain fails because setuptools auto-discovery sees multiple top-level packages/directories: app, static, and alembic. This blocks creating an isolated verification environment for pytest/mypy.\\n\\nFix by adding explicit package discovery/build metadata in services/creepy-brain/pyproject.toml, for example limiting packages to app* and excluding static/alembic from package discovery while preserving runtime access to migrations/static assets as needed.\\n\\nAcceptance criteria:\\n- python -m pip install -e '.[dev]' succeeds from services/creepy-brain in a clean venv.\\n- pytest can be invoked from the venv.\\n- mypy can be invoked from the venv without packaging setup failure.","status":"closed","priority":2,"issue_type":"bug","owner":"codex@local","created_at":"2026-04-18T15:11:36Z","created_by":"Codex","updated_at":"2026-04-18T15:39:53Z","closed_at":"2026-04-18T15:39:53Z","close_reason":"Added explicit package discovery (app* only) and mypy ignore overrides for numpy/anthropic/structlog","labels":["creepy-brain","packaging"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-o4d","title":"Fix pre-existing mypy errors in llm/client.py and gpu/runpod.py","description":"Pre-existing mypy strict errors surfaced during hbt cleanup:\\n\\n- app/llm/client.py:42,52: Unused 'type: ignore' comments\\n- app/gpu/runpod.py:83,109,161,176: Returning Any from functions declared to return dict/list\\n\\nThese are out of scope for the Hatchet cleanup bead. Fix to pass mypy --strict.","status":"closed","priority":2,"issue_type":"bug","owner":"codex@local","created_at":"2026-04-18T13:14:35Z","created_by":"Codex","updated_at":"2026-04-18T15:20:07Z","closed_at":"2026-04-18T15:20:07Z","close_reason":"Fixed AGENTS typing/layering violations and the surfaced mypy issues in llm/client.py and gpu/runpod.py","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-b6o","title":"Add step re-trigger API endpoint","description":"Add a new API endpoint to re-trigger individual failed steps within a workflow.\n\n## Files to modify\n- app/routes/workflows.py — add POST /api/workflows/{id}/steps/{step_name}/retry\n\n## Design\n- Validate workflow exists and is in FAILED/COMPLETED state\n- Validate step_name is valid StepName enum\n- Call engine.retry_step(workflow_id, step_name)\n- Engine resets target step + all transitive downstream steps to PENDING\n- Sets workflow status back to RUNNING\n- Spawns new runner that skips already-COMPLETED steps\n- Returns updated workflow status\n\n## Acceptance criteria\n- Endpoint validates workflow exists, step exists, workflow is in terminal state\n- Resets target step + all downstream steps to PENDING\n- Resumes workflow execution from that step\n- Returns updated workflow response\n- Properly typed Pydantic request/response models\n- Returns 404 if workflow/step not found\n- Returns 409 if workflow is still running","status":"open","priority":2,"issue_type":"task","owner":"codex@local","created_at":"2026-04-18T11:55:47Z","created_by":"Codex","updated_at":"2026-04-18T11:55:47Z","labels":["api","creepy-brain","engine"],"dependencies":[{"issue_id":"chatterbox-tts-lite-b6o","depends_on_id":"chatterbox-tts-lite-03n","type":"blocks","created_at":"2026-04-18T11:56:10Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-43o","title":"Story.workflow_id always NULL — no Story-to-Workflow DB join","description":"story.py:57-59 explicitly leaves workflow_id as None because 'assigning ctx.workflow_run_id would violate the FK until a Workflow row exists.' This means you can't query which story belongs to which workflow via DB.\n\nFix: either create the Workflow DB row before the story (in the API layer or first step), or pass workflow_run_id as a non-FK field. Depends on bead 2sk (workflow lifecycle tracking).\n\nAcceptance criteria:\n- Story.workflow_id is populated when created from a workflow\n- Story can be joined to its Workflow in queries","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-18T07:35:35Z","updated_at":"2026-04-18T15:47:28Z","closed_at":"2026-04-18T15:47:28Z","close_reason":"Closed","labels":["pipeline","schema"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-84e","title":"Blocking subprocess.run() in async context blocks event loop","description":"_create_video (stitch.py) and encode_wav_to_mp3 (encoding.py) use subprocess.run() which blocks the asyncio event loop during ffmpeg encoding (10+ seconds). This blocks all other async tasks in the worker.\n\nFix: use asyncio.create_subprocess_exec() instead of subprocess.run().\n\nAcceptance criteria:\n- All subprocess calls use asyncio subprocess API\n- Event loop is not blocked during encoding","status":"closed","priority":2,"issue_type":"task","assignee":"Codex","created_at":"2026-04-18T07:35:35Z","updated_at":"2026-04-18T13:23:40Z","started_at":"2026-04-18T13:19:57Z","closed_at":"2026-04-18T13:23:40Z","close_reason":"Replaced subprocess.run() with asyncio.create_subprocess_exec() in encoding.py and stitch.py","labels":["async","performance"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-ewi","title":"Video timing wrong — images don't match audio duration per scene","description":"_create_video uses -framerate 1/5 (5s per image) with -shortest. If 5 scenes = 25s of images but 3+ minutes of audio, video ends at 25s and most audio is cut off. The image duration should match the audio duration of its corresponding scene chunks.\n\nFix: calculate per-scene audio duration from chunk durations, create an ffmpeg concat demuxer with correct durations per image.\n\nAcceptance criteria:\n- Each scene image displays for the duration of its corresponding audio chunks\n- Full audio plays without cutoff\n- Video length matches audio length","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-18T07:35:35Z","updated_at":"2026-04-18T08:35:12Z","closed_at":"2026-04-18T15:54:55Z","labels":["stitch","video"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-db2","title":"Add DB check constraints for enum fields and non-negative counters","description":"The current migrations store enum-like fields as plain `VARCHAR` columns with no database-level checks. Numeric fields such as chunk indexes, attempt numbers, durations, byte sizes, token counts, word counts, revision counts, and GPU costs also lack non-negative/range constraints.\n\nImpact: invalid states can enter through direct SQL, future services, bad migrations, or partial failure handling even when Python/Pydantic code is correct.\n\nFix: add Alembic constraints for valid enum values on workflow, step, chunk, blob, GPU pod, story, and run status/type columns. Add check constraints such as `chunk_index \u003e= 0`, `attempt_number \u003e= 1`, `duration_sec \u003e= 0`, `size_bytes \u003e= 0`, `word_count \u003e= 0`, `revision_count \u003e= 0`, `total_tokens_used \u003e= 0`, and cost fields `\u003e= 0`. Include timestamp invariants where useful, e.g. completed/terminated timestamps should not precede creation/start timestamps.","status":"closed","priority":2,"issue_type":"bug","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T22:11:41Z","created_by":"Codex","updated_at":"2026-04-18T15:50:45Z","closed_at":"2026-04-18T15:50:45Z","close_reason":"Closed","labels":["constraints","creepy-brain","database","schema"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-db3","title":"Enforce a single default voice and explicit voice audio storage","description":"`voices.is_default` is a plain boolean, so the database permits multiple default voices. Voice upload also stores a workflow blob UUID string in `voices.audio_path`, which makes the column ambiguous: it can read like a filesystem path but currently holds uploaded blob IDs.\n\nImpact: default voice selection can become nondeterministic, and voice audio integrity cannot be enforced or queried cleanly.\n\nFix: add a partial unique index allowing only one row where `is_default = true`, update the voice service so setting a new default clears the old one transactionally, and migrate voice audio storage to explicit fields such as `audio_blob_id UUID REFERENCES workflow_blobs(id)` plus a separate path/source field if built-in file voices are still needed.","status":"closed","priority":2,"issue_type":"bug","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T22:11:41Z","created_by":"Codex","updated_at":"2026-04-18T15:50:46Z","closed_at":"2026-04-18T15:50:46Z","close_reason":"Closed","labels":["creepy-brain","database","schema","voices"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-db4","title":"Add missing indexes for foreign-key and filter query paths","description":"Postgres does not automatically index foreign-key columns. Some hot or likely-hot schema paths are currently unindexed, including `runs.workflow_id`, `runs.story_id`, `runs.voice_id`, and `stories.workflow_id`. Filtered list endpoints also use patterns such as workflow status plus created time that would benefit from composite indexes as data grows.\n\nImpact: deletes/updates on parent tables can scan child tables, list/detail endpoints can degrade, and FK enforcement can take heavier locks under load.\n\nFix: audit current query paths with `EXPLAIN`, then add Alembic indexes for missing FK columns and useful composite filters, avoiding duplicates where unique constraints already provide coverage (`run_chunks(run_id, chunk_index)`, `story_acts(story_id, act_number)`, etc.).","status":"closed","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T22:11:41Z","created_by":"Codex","updated_at":"2026-04-18T16:01:14Z","closed_at":"2026-04-18T16:01:14Z","close_reason":"Migration 0009 adds indexes for all unindexed FK columns (runs.workflow_id, runs.story_id, runs.voice_id, runs.final_audio_blob_id, run_chunks.audio_blob_id, stories.workflow_id, story_acts.story_id) plus composite indexes on workflows(status,created_at) and runs(status,created_at).","labels":["creepy-brain","database","performance","schema"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-db6","title":"Align workflow JSONB schemas with actual step outputs","description":"`WorkflowStep.output_json` is typed as `PydanticType(StepOutputSchema)`, but the actual workflow step outputs do not match the persisted schema models. Examples: `tts_synthesis` returns `pod_id` and `chunks`, `image_generation` returns `scenes` and `scene_count`, and `stitch_final` can return a skipped payload or final audio with optional video, while `StitchFinalStepOutput` requires `final_video_blob_id` and has no `final_audio_blob_id`.\n\nImpact: once step persistence is wired, valid runtime outputs can fail JSONB binding/validation or lose important fields. The DB schema contract for resumability and observability will not match the real workflow contract.\n\nFix: replace the stale step output models with Pydantic models that exactly match current task return shapes, including skipped variants as discriminated unions. Add tests that serialize each step output through `PydanticType(StepOutputSchema)` before saving to `workflow_steps.output_json`.","status":"closed","priority":2,"issue_type":"bug","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T22:11:41Z","created_by":"Codex","updated_at":"2026-04-18T08:35:12Z","closed_at":"2026-04-18T15:54:55Z","labels":["creepy-brain","database","schema","workflow"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-2sk","title":"Persist Workflow lifecycle from worker: status, current_step, result, WorkflowStep records","description":"The Workflow table (status, current_step, result_json) and WorkflowStep table are never updated by the Hatchet worker steps. The API creates a Workflow row with status=RUNNING but nothing ever transitions it to COMPLETED or FAILED, and no WorkflowStep records are ever created.\n\nConsequences:\n- GET /api/workflows always shows RUNNING for completed/failed runs\n- POST /api/workflows/{id}/retry's FAILED-only gate is unreachable\n- The retry endpoint is effectively broken\n\n## Fix\n\nAdd lifecycle hooks in each step executor (or via Hatchet on_success/on_failure hooks) to:\n1. Set `Workflow.current_step` at the start of each step\n2. Create/update `WorkflowStep` records (started_at, completed_at, status, error)\n3. On stitch_final success: set `Workflow.status=COMPLETED`, `result_json`, `completed_at`\n4. On cleanup_gpu_pod (failure hook): set `Workflow.status=FAILED`, `error`, `completed_at`\n\nAlso: persist `GpuPod` rows at pod creation time (in tts and image steps) so the cancel endpoint can find and terminate them.\n\n## Acceptance criteria\n- [ ] Workflow.status transitions: RUNNING → COMPLETED or FAILED\n- [ ] Workflow.current_step updated at each step start\n- [ ] WorkflowStep records created for all four steps\n- [ ] GpuPod rows persisted at creation with workflow_id\n- [ ] GET /api/workflows/{id} shows accurate status for a completed run","status":"closed","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T22:02:07Z","created_by":"Shubham Goel","updated_at":"2026-04-18T10:24:55Z","closed_at":"2026-04-18T15:54:55Z","labels":["creepy-brain","hatchet","workflow"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-6sk","title":"Cancel in-flight engine tasks on DELETE /api/workflows/{id}","description":"The DELETE /api/workflows/{id} endpoint marks the DB row CANCELLED and terminates GPU pods, but never signals the custom WorkflowEngine to stop running tasks. Live steps (story, tts, image, stitch) can continue executing, create new pods, and write blobs after 204 is returned.\n\n**NOTE:** This bead was originally written for a Hatchet-based architecture. The codebase uses a custom engine (`app/engine/engine.py`). The fix must use the engine's cancellation mechanism, not Hatchet's admin API.\n\n## Fix\n\nLook up the engine cancel/stop mechanism in `app/engine/engine.py` and call it for the given workflow_id before marking the DB row CANCELLED. Wrap in try/except — log errors but still mark CANCELLED (best-effort).\n\n## Acceptance criteria\n- [ ] Engine tasks are cancelled via the WorkflowEngine on DELETE\n- [ ] If engine cancel fails, error is logged and DB still marked CANCELLED\n- [ ] GPU pods found in gpu_pods table are still terminated","status":"open","priority":2,"issue_type":"bug","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T22:01:59Z","created_by":"Shubham Goel","updated_at":"2026-04-23T18:54:07Z","labels":["creepy-brain","hatchet","workflow"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-6yo","title":"Increase image_generation task timeout and add retry/resume","description":"The image_generation step runs scenes sequentially with up to 180s per image generation. Combined with the 300s pod readiness budget and the ContentPipeline task timeout of 10 minutes, a normal multi-scene workflow can be killed after committing some scene blobs but before returning a complete step output.\n\n## Issue\n- 10 min timeout is insufficient for workflows with many scenes\n- Partial commits occur: some scenes saved, step output never returned\n- No resume mechanism if killed mid-way\n\n## Recommendations\n1. Increase Hatchet task timeout to cover worst-case: `scene_count * per_image_timeout + pod_readiness + llm_prompt_time`\n2. Make scene generation idempotent/resumable — skip scenes that already have image_blob_id set\n3. Consider lowering per-image timeout and adding retry logic\n\n## Context\nFound during adversarial review of bead 83y (image_generation step implementation).\n","status":"closed","priority":2,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T19:58:48Z","created_by":"Shubham Goel","updated_at":"2026-04-17T21:40:51Z","closed_at":"2026-04-18T03:10:51Z","close_reason":"Completed","labels":["images","phase-3","reliability","workflow"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-58x","title":"Implement scene-level image prompt generation via LLM","description":"Generate a cinematic background image prompt for each scene (group of 5-10 chunks) using an LLM API call.\n\n## Why\nEach scene in the video needs one atmospheric background image. The LLM reads the combined text of all chunks in the scene and produces a single SDXL-optimized prompt describing the visual setting.\n\n## Implementation\n\nCreate a function in `app/llm/` (e.g. `app/llm/image_prompts.py`):\n\n```python\nasync def generate_scene_image_prompt(scene_text: str) -\u003e ImagePromptResult:\n    ...\n```\n\n### ImagePromptResult model:\n```python\nclass ImagePromptResult(BaseModel):\n    prompt: str              # positive prompt for SDXL\n    negative_prompt: str     # negative prompt (text, watermarks, etc.)\n```\n\n### LLM prompt template:\n```\nYou are creating a background image prompt for a horror narration video.\n\nThe following is a scene from a creepy pasta story:\n---\n{scene_text}\n---\n\nGenerate:\n1. A cinematic SDXL image prompt describing the visual setting/atmosphere of this scene. Focus on environment, lighting, mood. No characters or text.\n2. A negative prompt to avoid unwanted elements.\n\nRespond in JSON: {\"prompt\": \"...\", \"negative_prompt\": \"...\"}\n```\n\n### Key decisions:\n- One LLM call per scene (not per chunk) — cost-efficient\n- Prompt focuses on **setting/atmosphere**, not character actions\n- Negative prompt always includes: \"text, words, letters, watermark, blurry, low quality\"\n- Use structured JSON output parsing\n- Uses the existing LLM client from `app/llm/client.py`\n\n## Acceptance Criteria\n- [ ] Function takes scene combined text, returns ImagePromptResult\n- [ ] LLM prompt emphasizes atmospheric/environmental description for horror\n- [ ] Negative prompt always includes anti-text/watermark terms\n- [ ] Response parsed as structured JSON\n- [ ] Error handling for malformed LLM responses\n- [ ] Fully typed, passes mypy --strict\n","status":"closed","priority":2,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T18:47:06Z","created_by":"Shubham Goel","updated_at":"2026-04-17T19:15:31Z","closed_at":"2026-04-18T00:45:31Z","close_reason":"Completed - implemented scene-level image prompt generation with validation","labels":["creepy-brain","images","llm","phase-3"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-5h4","title":"Implement chunk-to-scene grouping for image generation","description":"Group consecutive text chunks (from TTS chunking) into \"scenes\" of a fixed size for image generation. Instead of generating one image per chunk, we generate one background image per scene.\n\n## Why\nEach chunk is ~120-200 words (~1 min narration at 150 wpm). Grouping chunks per scene means each background image holds for **5-10 minutes** of video — natural pacing for a creepy pasta narration where the visual atmosphere changes slowly.\n\n## Implementation\n\nCreate a function in `app/text/scene_grouping.py`:\n\n```python\nclass Scene(BaseModel):\n    scene_index: int\n    chunk_indices: list[int]\n    combined_text: str\n\ndef group_chunks_into_scenes(\n    chunks: list[str],\n    chunks_per_scene: int = 7,\n) -\u003e list[Scene]:\n    ...\n```\n\n### Grouping logic (fixed size):\n- Always take `chunks_per_scene` chunks per scene (default 7, configurable)\n- Last scene gets the remainder (can be smaller)\n- Example: 20 chunks with chunks_per_scene=7 → scenes of [7, 7, 6]\n- Pure function, no DB or LLM calls\n\n### Config (app/config.py)\nAdd to Settings:\n```python\nchunks_per_scene: int = 7\n```\nRead from env var `CHUNKS_PER_SCENE`. Passed to the grouping function.\n\n## Acceptance Criteria\n- [ ] Function takes list of chunk strings + chunks_per_scene, returns list of Scene models\n- [ ] Fixed-size grouping: always `chunks_per_scene` per scene, remainder in last scene\n- [ ] `chunks_per_scene` added to app config (default 7)\n- [ ] combined_text is chunk texts joined with spaces\n- [ ] Fully typed, passes mypy --strict\n- [ ] Unit tests cover edge cases (\u003c chunks_per_scene, exactly chunks_per_scene, remainder scenarios)","status":"closed","priority":2,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T18:46:55Z","created_by":"Shubham Goel","updated_at":"2026-04-17T19:44:30Z","closed_at":"2026-04-18T01:14:30Z","close_reason":"Completed","labels":["creepy-brain","images","phase-3"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-nqj","title":"Register ContentPipeline workflow once all steps are implemented","description":"`services/creepy-brain/app/workflows/content_pipeline.py` defines the full ContentPipeline workflow but intentionally does NOT register it (the `WORKFLOWS.append(content_pipeline)` call is commented out) until all four step executors are implemented.\n\n## Steps needed before registration\n\n- [x] `generate_story` (bead bjx — in progress)\n- [x] `tts_synthesis` (bead duy — done)\n- [ ] `image_generation` (bead 83y — open)\n- [ ] `stitch_final` (bead ea6 — open)\n\n## Fix\n\nOnce all four beads above are closed, uncomment line 68 in `content_pipeline.py`:\n\n```python\nWORKFLOWS.append(content_pipeline)\n```\n\nThen do an end-to-end smoke test: trigger the workflow via the API and verify all four steps complete and produce a final output.\n\n## Acceptance criteria\n\n- [ ] `WORKFLOWS.append(content_pipeline)` uncommented\n- [ ] Worker registers and starts the ContentPipeline workflow without errors\n- [ ] End-to-end smoke test: workflow triggered, all four steps execute, output produced","status":"closed","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T09:40:26Z","created_by":"Shubham Goel","updated_at":"2026-04-17T21:48:09Z","closed_at":"2026-04-18T03:18:09Z","close_reason":"Implemented in commit c16d76a — WORKFLOWS.append(content_pipeline) uncommented in content_pipeline.py","labels":["creepy-brain","integration","workflow"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-hhg","title":"[P2-followup] Fix RunPod _parse_pod: hardcoded port, bare assert, created_at, READY mapping","description":"Several correctness issues in `app/gpu/runpod.py::_parse_pod` and related logic:\n\n1. **Hardcoded `privatePort == 8005`** (line ~105): TTS server listens on 8005, but the image-server is a **separate Docker image** on a different port. Port must come from `GpuPodSpec.ports` or be parsed from the pod's exposed port list dynamically.\n2. **Bare `assert isinstance(machine, dict)`** (line ~114): asserts are stripped under `python -O`. Use explicit `raise TypeError(...)`.\n3. **`created_at=datetime.now(timezone.utc)` is always \"now\"** (line ~126): for pods fetched via `list_active_pods` or idempotency recovery this is wrong. Query `createdAt`/`startedAt` from RunPod GraphQL or mark field `Optional`.\n4. **Status mapping never produces `PodStatus.READY`**: only `wait_for_ready` upgrades status via health probe, so `get_pod`/`list_active_pods` always reports `RUNNING` at best. Document intent or collapse `RUNNING`/`READY` in the enum.\n\n## Acceptance criteria\n- [ ] Port selection derived from pod's exposed ports (not hardcoded 8005) — works for both tts-server and image-server pods\n- [ ] Replace bare `assert isinstance` with explicit `raise TypeError`\n- [ ] `created_at` is either accurate (from API) or clearly `Optional`\n- [ ] Status mapping documented; consistent between create/get/list/wait\n- [ ] mypy passes on `app/gpu/`","status":"closed","priority":2,"issue_type":"bug","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T16:07:18Z","created_by":"Shubham Goel","updated_at":"2026-04-17T11:06:27Z","closed_at":"2026-04-17T16:36:27Z","close_reason":"Completed","labels":["gpu","phase-2","runpod"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-hhg","depends_on_id":"Chatterbox-TTS-Server-8dn","type":"discovered-from","created_at":"2026-04-16T21:37:45Z","created_by":"shubham","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-vja","title":"[P1-cleanup] Make anthropic_api_key required in config","description":"`app/config.py` declares `anthropic_api_key: str = \"\"`. The empty default allows the service to boot misconfigured and only fail at first LLM call.\n\nFix: drop the default so pydantic-settings raises at startup if the env var is missing. Update `.env.example` so devs know to fill it in (already present).","acceptance_criteria":"- `anthropic_api_key` has no default in config.py\n- Missing env var causes startup failure with clear error\n- docker-compose still works when .env is supplied","status":"in_progress","priority":2,"issue_type":"bug","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:54Z","created_by":"Shubham Goel","updated_at":"2026-04-16T13:15:25Z","labels":["config","creepy-brain","phase1-cleanup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-z6z","title":"[P1-cleanup] Convert StoryService class to module-level functions","description":"`app/services/story_service.py` uses a `StoryService` class, while `run_service.py`, `voice_service.py`, and `blob_service.py` use module-level async functions that accept a session parameter. The class pattern hides dependencies in `__init__` and makes the transaction-ownership issue worse.\n\nFix: convert StoryService methods to module-level functions taking `session: AsyncSession` explicitly. Remove the class.","acceptance_criteria":"- `app/services/story_service.py` exposes module-level functions only\n- Callers updated\n- Transaction handling aligned with the unified pattern (see companion bead)","status":"closed","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:45Z","created_by":"Shubham Goel","updated_at":"2026-04-18T16:01:30Z","closed_at":"2026-04-18T16:01:30Z","close_reason":"Converted StoryService class to module-level async functions taking explicit AsyncSession. All callers (orchestrator, routes/stories, steps/story, steps/tts) updated. Pattern now consistent with run_service, voice_service, blob_service.","labels":["creepy-brain","phase1-cleanup","refactor"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-9nx","title":"[P1-cleanup] Move stories.py inline schemas to app/schemas/story.py","description":"`app/routes/stories.py` defines request/response Pydantic models inline (GenerateStoryRequest, StoryResponse, etc.). Other routes already use `app/schemas/*` modules. Inline schemas prevent reuse, hide the contract, and make OpenAPI docs harder to navigate.\n\nFix: extract into `app/schemas/story.py`, import from there. Also: re-export via `app/schemas/__init__.py` if the convention exists.","acceptance_criteria":"- `app/schemas/story.py` contains all story request/response models\n- `routes/stories.py` imports them\n- No inline Pydantic models in routes","status":"closed","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:31Z","created_by":"Shubham Goel","updated_at":"2026-04-18T16:01:34Z","closed_at":"2026-04-18T16:01:34Z","close_reason":"Extracted GenerateStoryRequest, GenerateStoryResponse, ActResponse, StoryResponse, StoryListItem from routes/stories.py into app/schemas/story.py. Route now imports from the schemas module. No inline Pydantic models remain in the route.","labels":["creepy-brain","phase1-cleanup","refactor"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-vrl","title":"[P1-cleanup] Unify logging: switch pipeline/llm modules to structlog","description":"creepy-brain has dual logging: `main.py` and `middleware.py` use structlog; `app/pipeline/*` and `app/llm/*` use stdlib `logging.getLogger(__name__)`. stdlib loggers don't pick up the request context (request_id, run_id) bound via structlog contextvars, breaking correlation in JSON logs.\n\nFix: replace `logging.getLogger(__name__)` with `structlog.get_logger()` in pipeline and llm modules. Keep keyword-event style (`logger.info(\"event\", key=value)`).","acceptance_criteria":"- No `logging.getLogger` in `app/pipeline/*` or `app/llm/*`\n- Pipeline logs include request/run context\n- Log output still readable in dev mode","status":"closed","priority":2,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:27Z","created_by":"Shubham Goel","updated_at":"2026-04-17T11:22:04Z","closed_at":"2026-04-17T16:52:04Z","close_reason":"Completed","labels":["creepy-brain","logging","phase1-cleanup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-ca4","title":"[P1-cleanup] Move stories route _semaphore to app.state","description":"`app/routes/stories.py:22` defines a module-level `_semaphore: Optional[asyncio.Semaphore] = None` with lazy init inside the handler. This creates a global mutable state surface that's hard to reason about in tests and under multiple event loops.\n\nFix: initialize the semaphore in the lifespan (`main.py`) and store on `app.state.generation_semaphore`. Routes acquire it from `request.app.state`.","acceptance_criteria":"- Semaphore created in lifespan using `settings.max_concurrent_generations`\n- Route reads it from `request.app.state`\n- No module-level mutable singleton remains","status":"closed","priority":2,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:14Z","created_by":"Shubham Goel","updated_at":"2026-04-17T11:16:49Z","closed_at":"2026-04-17T16:46:49Z","close_reason":"Completed","labels":["architecture","creepy-brain","phase1-cleanup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-yyh","title":"[P1-cleanup] Replace bare assert with explicit raise in LLM retry loop","description":"`app/llm/client.py:75` uses `assert last_exc is not None`. Asserts are stripped with `python -O` and the line communicates intent poorly.\n\nFix: replace with an explicit `raise RuntimeError(\"retry loop exited without exception\")` (or better, restructure so the control flow is statically obvious).","acceptance_criteria":"- No bare `assert` in client.py\n- Retry loop raises a typed error on exhaustion\n- mypy-strict clean","status":"closed","priority":2,"issue_type":"bug","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:10Z","created_by":"Shubham Goel","updated_at":"2026-04-16T13:03:18Z","closed_at":"2026-04-16T18:33:18Z","close_reason":"Completed","labels":["bug","creepy-brain","phase1-cleanup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-d4z","title":"[P1-cleanup] Make AsyncAnthropic a module-level singleton","description":"`app/llm/client.py:39` creates a new `anthropic.AsyncAnthropic(...)` on every call. The client maintains an httpx connection pool — instantiating per-call throws away the pool and slows down calls.\n\nFix: create the client at module import (or lazily via a module-level accessor) and reuse.","acceptance_criteria":"- `AsyncAnthropic` instantiated once per process\n- Calls reuse the same client\n- No behavioral changes in retry/error paths","status":"closed","priority":2,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:06Z","created_by":"Shubham Goel","updated_at":"2026-04-16T13:04:16Z","closed_at":"2026-04-16T18:34:16Z","close_reason":"Completed","labels":["creepy-brain","performance","phase1-cleanup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-dyy","title":"[P5] Write documentation and runbooks","description":"Create operational documentation for the service.\n\n## Documentation Structure\n```\nservices/creepy-brain/\n├── README.md                # Getting started\n├── docs/\n│   ├── ARCHITECTURE.md      # System design\n│   ├── API.md               # API reference\n│   ├── DEPLOYMENT.md        # Deployment guide\n│   └── RUNBOOKS.md          # Operational runbooks\n```\n\n## README.md\n- Quick start (docker-compose up)\n- Prerequisites (Docker, API keys)\n- Configuration overview\n- Development setup\n- Links to detailed docs\n\n## ARCHITECTURE.md\n- System overview diagram\n- Component descriptions\n- Data flow\n- Technology choices (why Hatchet, why polling)\n- Database schema overview\n\n## API.md\n- OpenAPI/Swagger spec\n- Authentication (if any)\n- Endpoint descriptions\n- Request/response examples\n- Error codes\n\n## DEPLOYMENT.md\n- Prerequisites\n- Docker Compose setup\n- Environment variables reference\n- Production considerations\n- Scaling notes\n- Backup/restore\n\n## RUNBOOKS.md\nOperational procedures for common scenarios:\n\n### Runbook: Orphaned Pod\n```markdown\n## Symptoms\n- Pod running \u003e 2 hours\n- No associated workflow\n- Cost accumulating\n\n## Investigation\n1. Check recon job logs: `docker logs creepy-brain | grep recon`\n2. List active pods: `curl localhost:8006/api/gpu-pods`\n3. Check Hatchet dashboard for stuck workflows\n\n## Resolution\n1. Manual terminate: `curl -X DELETE localhost:8006/api/gpu-pods/{pod_id}`\n2. Verify termination in RunPod dashboard\n3. If recon failing, check cron job registration in Hatchet\n```\n\n### Runbook: Workflow Stuck\n```markdown\n## Symptoms\n- Workflow status \"running\" for \u003e 1 hour\n- No progress in steps\n\n## Investigation\n1. Check Hatchet dashboard for worker status\n2. Check step logs in Hatchet\n3. Check if GPU pod is responsive\n\n## Resolution\n1. Cancel workflow: `curl -X DELETE localhost:8006/api/workflows/{id}`\n2. Verify pod terminated\n3. Retry if appropriate\n```\n\n### Runbook: High GPU Costs\n```markdown\n## Symptoms\n- Daily cost exceeds threshold\n- Multiple pods running\n\n## Investigation\n1. List active pods: `curl localhost:8006/api/gpu-pods`\n2. Check for long-running workflows\n3. Verify recon job is running\n\n## Resolution\n1. Terminate unnecessary pods\n2. Review workflow inputs (story length)\n3. Adjust cost thresholds\n```\n\n### Runbook: Database Issues\n```markdown\n## Symptoms\n- API returning 500 errors\n- \"connection refused\" in logs\n\n## Investigation\n1. Check postgres: `docker logs postgres`\n2. Verify connection: `docker exec postgres pg_isready`\n\n## Resolution\n1. Restart postgres: `docker-compose restart postgres`\n2. Check disk space\n3. Review connection pool settings\n```\n\n## Verification\n- [ ] README enables new developer to start in \u003c 10 minutes\n- [ ] API docs match actual endpoints\n- [ ] Runbooks cover common failure modes\n- [ ] Deployment guide tested on fresh server","status":"open","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:34:27Z","created_by":"Shubham Goel","updated_at":"2026-04-16T10:34:27Z","labels":["documentation","phase-5"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-dyy","depends_on_id":"Chatterbox-TTS-Server-9pe","type":"blocks","created_at":"2026-04-16T16:06:30Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-dyy","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:06:09Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-9pe","title":"[P5] Write comprehensive tests","description":"Test coverage for critical paths.\n\n## Test Structure\n```\ntests/\n├── conftest.py              # Fixtures\n├── unit/\n│   ├── test_gpu_provider.py\n│   ├── test_workflow_service.py\n│   ├── test_story_pipeline.py\n│   └── test_blob_service.py\n├── integration/\n│   ├── test_content_pipeline.py\n│   ├── test_api.py\n│   └── test_recon.py\n└── e2e/\n    └── test_full_workflow.py\n```\n\n## Fixtures\n```python\n# tests/conftest.py\nimport pytest\nimport asyncio\nfrom sqlalchemy.ext.asyncio import create_async_engine, AsyncSession\nfrom app.models import Base\n\n@pytest.fixture(scope=\"session\")\ndef event_loop():\n    loop = asyncio.get_event_loop_policy().new_event_loop()\n    yield loop\n    loop.close()\n\n@pytest.fixture\nasync def db_session():\n    engine = create_async_engine(\"sqlite+aiosqlite:///:memory:\")\n    async with engine.begin() as conn:\n        await conn.run_sync(Base.metadata.create_all)\n    \n    async with AsyncSession(engine) as session:\n        yield session\n\n@pytest.fixture\ndef local_gpu_provider():\n    return LocalProvider(base_url=\"http://localhost:8005\")\n\n@pytest.fixture\ndef mock_llm_client():\n    # Returns mock that returns predefined story\n    pass\n```\n\n## Unit Tests\n```python\n# tests/unit/test_gpu_provider.py\nasync def test_local_provider_creates_pod():\n    provider = LocalProvider()\n    pod = await provider.create_pod(GpuPodSpec(), \"test-key\")\n    \n    assert pod.id == \"local-test-key\"\n    assert pod.status == PodStatus.READY\n    assert pod.endpoint_url == \"http://localhost:8005\"\n\nasync def test_local_provider_idempotent():\n    provider = LocalProvider()\n    pod1 = await provider.create_pod(GpuPodSpec(), \"same-key\")\n    pod2 = await provider.create_pod(GpuPodSpec(), \"same-key\")\n    \n    assert pod1.id == pod2.id\n\nasync def test_terminate_marks_pod_terminated():\n    provider = LocalProvider()\n    pod = await provider.create_pod(GpuPodSpec(), \"test-key\")\n    await provider.terminate_pod(pod.id)\n    \n    updated = await provider.get_pod(pod.id)\n    assert updated.status == PodStatus.TERMINATED\n\n\n# tests/unit/test_workflow_service.py\nasync def test_create_workflow(db_session):\n    service = WorkflowService(db_session)\n    workflow = await service.create(\n        workflow_type=\"content_pipeline\",\n        input_json={\"premise\": \"Test premise\"},\n    )\n    \n    assert workflow.id is not None\n    assert workflow.status == \"pending\"\n\nasync def test_workflow_status_transitions(db_session):\n    service = WorkflowService(db_session)\n    workflow = await service.create(\"content_pipeline\", {})\n    \n    # pending -\u003e running\n    await service.start(workflow.id)\n    assert (await service.get(workflow.id)).status == \"running\"\n    \n    # running -\u003e completed\n    await service.complete(workflow.id, {\"result\": \"ok\"})\n    assert (await service.get(workflow.id)).status == \"completed\"\n```\n\n## Integration Tests\n```python\n# tests/integration/test_content_pipeline.py\nasync def test_full_pipeline_with_local_provider(db_session, mock_llm_client):\n    '''Test full pipeline with local GPU provider and mocked LLM'''\n    # This test requires tts-server running locally\n    \n    # Create workflow\n    workflow = await create_workflow(\n        db_session,\n        input={\"premise\": \"A short test story\", \"target_word_count\": 500}\n    )\n    \n    # Run pipeline steps\n    # ... mock or run actual steps\n    \n    # Verify results\n    assert workflow.status == \"completed\"\n    assert workflow.result_json[\"final_audio_blob_id\"] is not None\n\n\nasync def test_pipeline_retry_on_failure():\n    '''Test that retry works from failed step'''\n    pass\n\nasync def test_pod_cleanup_on_failure():\n    '''Test that pod is terminated when step fails'''\n    pass\n```\n\n## E2E Tests\n```python\n# tests/e2e/test_full_workflow.py\n# These tests require full infrastructure running\n\nasync def test_api_trigger_and_poll():\n    '''Test triggering workflow via API and polling until complete'''\n    async with httpx.AsyncClient(base_url=\"http://localhost:8006\") as client:\n        # Trigger\n        resp = await client.post(\"/api/workflows\", json={\n            \"workflow_type\": \"content_pipeline\",\n            \"input\": {\"premise\": \"Test\", \"voice_name\": \"Gianna\"}\n        })\n        workflow_id = resp.json()[\"id\"]\n        \n        # Poll\n        while True:\n            status = await client.get(f\"/api/workflows/{workflow_id}\")\n            if status.json()[\"status\"] in (\"completed\", \"failed\"):\n                break\n            await asyncio.sleep(5)\n        \n        assert status.json()[\"status\"] == \"completed\"\n```\n\n## Test Configuration\n```toml\n# pyproject.toml\n[tool.pytest.ini_options]\nasyncio_mode = \"auto\"\ntestpaths = [\"tests\"]\n```\n\n## Verification\n- [ ] Unit tests pass\n- [ ] Integration tests pass with local provider\n- [ ] E2E tests pass with full stack\n- [ ] Coverage \u003e 70% on critical paths\n- [ ] CI runs tests on PR","status":"open","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:34:14Z","created_by":"Shubham Goel","updated_at":"2026-04-16T10:34:14Z","labels":["phase-5","testing"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-9pe","depends_on_id":"Chatterbox-TTS-Server-4yj","type":"blocks","created_at":"2026-04-16T16:06:30Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-9pe","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:06:08Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-8gi","title":"[P5] Implement cost tracking and alerts","description":"Track GPU costs and alert on thresholds.\n\n## Cost Tracking\n\n### Per-Pod Cost\n```python\n# When pod becomes ready, start tracking\nasync def start_cost_tracking(pod_id: str, cost_per_hour_cents: int):\n    async with get_session() as session:\n        await session.execute(\n            update(GpuPod)\n            .where(GpuPod.id == pod_id)\n            .values(\n                cost_per_hour_cents=cost_per_hour_cents,\n                ready_at=datetime.now(timezone.utc),\n            )\n        )\n        await session.commit()\n\n# When pod is terminated, calculate final cost\nasync def finalize_cost(pod_id: str):\n    async with get_session() as session:\n        pod = await session.get(GpuPod, pod_id)\n        if pod and pod.ready_at:\n            runtime_hours = (datetime.now(timezone.utc) - pod.ready_at).total_seconds() / 3600\n            total_cost = int(runtime_hours * pod.cost_per_hour_cents)\n            \n            await session.execute(\n                update(GpuPod)\n                .where(GpuPod.id == pod_id)\n                .values(\n                    total_cost_cents=total_cost,\n                    terminated_at=datetime.now(timezone.utc),\n                )\n            )\n            await session.commit()\n            \n            return total_cost\n    return 0\n```\n\n### Per-Workflow Cost\n```python\n# Sum of all pods used by workflow\nasync def get_workflow_cost(workflow_id: str) -\u003e int:\n    async with get_session() as session:\n        result = await session.execute(\n            select(func.sum(GpuPod.total_cost_cents))\n            .where(GpuPod.workflow_id == workflow_id)\n        )\n        return result.scalar() or 0\n```\n\n### Daily/Monthly Aggregation\n```python\n# API endpoint for cost summary\n@router.get(\"/api/costs/summary\")\nasync def get_cost_summary():\n    async with get_session() as session:\n        today = date.today()\n        month_start = today.replace(day=1)\n        \n        # Today's cost\n        today_cost = await session.execute(\n            select(func.sum(GpuPod.total_cost_cents))\n            .where(func.date(GpuPod.created_at) == today)\n        )\n        \n        # Month's cost\n        month_cost = await session.execute(\n            select(func.sum(GpuPod.total_cost_cents))\n            .where(GpuPod.created_at \u003e= month_start)\n        )\n        \n        return {\n            \"today_cents\": today_cost.scalar() or 0,\n            \"month_cents\": month_cost.scalar() or 0,\n            \"active_pods\": await count_active_pods(session),\n        }\n```\n\n## Cost Alerts\n\n### Daily Budget Alert\n```python\n# Cron job: check daily spend\n@hatchet.cron(\"0 * * * *\")  # Every hour\nasync def check_cost_alerts():\n    async with get_session() as session:\n        today_cost = await get_today_cost(session)\n        \n        if today_cost \u003e settings.daily_cost_alert_threshold_cents:\n            await send_slack_notification(\n                title=\"💰 Daily Cost Alert\",\n                message=f\"Today's GPU cost (${today_cost/100:.2f}) exceeded threshold\",\n                color=\"#ff0000\",\n            )\n```\n\n### Per-Workflow Cost Alert\n```python\n# In workflow steps, track cumulative\nasync def check_workflow_cost(workflow_id: str, max_cents: int = 500):\n    cost = await get_workflow_cost(workflow_id)\n    if cost \u003e max_cents:\n        logger.warning(\n            \"workflow_cost_high\",\n            workflow_id=workflow_id,\n            cost_cents=cost,\n            threshold=max_cents,\n        )\n```\n\n## Configuration\n```python\nclass Settings:\n    daily_cost_alert_threshold_cents: int = Field(default=1000)  # $10\n    workflow_cost_limit_cents: int = Field(default=500)  # $5\n```\n\n## Dashboard Integration\nCost displayed in:\n- GPU pods page (per-pod and total)\n- Workflow detail (per-workflow)\n- Dashboard header (today/month)\n\n## Verification\n- [ ] Pod cost calculated on termination\n- [ ] Workflow cost sums all pods\n- [ ] Daily/monthly aggregation correct\n- [ ] Alert fires when threshold exceeded\n- [ ] Costs display in UI","status":"open","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:33:56Z","created_by":"Shubham Goel","updated_at":"2026-04-16T10:33:56Z","labels":["cost-control","monitoring","phase-5"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-8gi","depends_on_id":"Chatterbox-TTS-Server-4yj","type":"blocks","created_at":"2026-04-16T16:06:29Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-8gi","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:06:07Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-vqu","title":"[P5] Add workflow-level timeout","description":"Ensure workflows don't run indefinitely.\n\n## Timeout Strategy\n\n### Hatchet Workflow Timeout\n```python\n@hatchet.workflow(\n    timeout=\"1h\",  # Overall workflow timeout\n    on_failure=[cleanup_gpu_pod]\n)\nclass ContentPipeline:\n    pass\n```\n\n### Step Timeouts\n```python\n@hatchet.step(timeout=\"15m\", retries=2)\nasync def generate_story(self, ctx):\n    pass\n\n@hatchet.step(timeout=\"30m\")  # TTS can take a while for long stories\nasync def tts_synthesis(self, ctx):\n    pass\n\n@hatchet.step(timeout=\"10m\")\nasync def image_generation(self, ctx):\n    pass\n\n@hatchet.step(timeout=\"5m\")\nasync def stitch_final(self, ctx):\n    pass\n```\n\n### HTTP Client Timeouts\n```python\n# In tts.py step\nasync with httpx.AsyncClient(\n    base_url=pod.endpoint_url,\n    timeout=httpx.Timeout(\n        connect=10.0,    # Connection timeout\n        read=300.0,      # Read timeout (5 min for long responses)\n        write=60.0,      # Write timeout\n        pool=10.0,       # Pool timeout\n    )\n) as client:\n    pass\n```\n\n### Polling Timeout\n```python\n# In tts_synthesis polling loop\nMAX_POLL_TIME = 25 * 60  # 25 minutes (leave buffer before step timeout)\nstart_time = time.time()\n\nwhile True:\n    if time.time() - start_time \u003e MAX_POLL_TIME:\n        raise TimeoutError(f\"TTS job {job_id} did not complete in {MAX_POLL_TIME}s\")\n    \n    status = await client.get(f\"/api/jobs/{job_id}\")\n    # ...\n    await asyncio.sleep(2)\n```\n\n### GPU Pod Wait Timeout\n```python\n# In provider.wait_for_ready\nasync def wait_for_ready(self, pod_id: str, timeout_sec: int = 300) -\u003e GpuPod:\n    start = time.time()\n    while time.time() - start \u003c timeout_sec:\n        pod = await self.get_pod(pod_id)\n        if pod and pod.status == PodStatus.READY:\n            # Also check health endpoint\n            if await self._health_check(pod.endpoint_url):\n                return pod\n        await asyncio.sleep(5)\n    \n    raise TimeoutError(f\"Pod {pod_id} did not become ready in {timeout_sec}s\")\n```\n\n## Timeout Handling\nWhen timeout occurs:\n1. Hatchet marks step as failed\n2. on_failure hook runs (cleanup_gpu_pod)\n3. Workflow status set to \"failed\"\n4. Notification sent\n\n## Testing\n```python\n# Force timeout for testing\n@hatchet.step(timeout=\"5s\")  # Very short timeout\nasync def test_timeout_step(self, ctx):\n    await asyncio.sleep(10)  # Will timeout\n```\n\n## Verification\n- [ ] Workflow times out after 1 hour\n- [ ] Step times out at configured duration\n- [ ] HTTP timeouts prevent hanging\n- [ ] Polling has max time limit\n- [ ] Pod wait has timeout\n- [ ] Cleanup runs on timeout","status":"open","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:33:43Z","created_by":"Shubham Goel","updated_at":"2026-04-16T10:33:43Z","labels":["phase-5","reliability","timeout"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-vqu","depends_on_id":"Chatterbox-TTS-Server-cmz","type":"blocks","created_at":"2026-04-16T16:06:28Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-vqu","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:06:06Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-d72","title":"[P5] Configure Slack/Discord alerts","description":"Send notifications on workflow failure and completion.\n\n## Alert Types\n1. **Workflow Failed** - Always notify\n2. **Workflow Completed** - Optional\n3. **Pod Orphaned** - From recon job\n4. **High Cost Alert** - If daily cost exceeds threshold\n\n## Implementation\n```python\n# app/notifications.py\nimport httpx\nimport structlog\nfrom app.config import settings\n\nlogger = structlog.get_logger()\n\nasync def send_slack_notification(\n    title: str,\n    message: str,\n    color: str = \"#36a64f\",  # green\n    fields: list[dict] | None = None,\n):\n    '''Send notification to Slack webhook'''\n    if not settings.slack_webhook_url:\n        logger.warning(\"slack_webhook_not_configured\")\n        return\n    \n    payload = {\n        \"attachments\": [{\n            \"color\": color,\n            \"title\": title,\n            \"text\": message,\n            \"fields\": fields or [],\n            \"footer\": \"Creepy Brain\",\n            \"ts\": int(time.time()),\n        }]\n    }\n    \n    async with httpx.AsyncClient() as client:\n        try:\n            resp = await client.post(settings.slack_webhook_url, json=payload)\n            resp.raise_for_status()\n        except Exception as e:\n            logger.error(\"slack_notification_failed\", error=str(e))\n\n\nasync def notify_workflow_failed(workflow_id: str, error: str, step: str):\n    await send_slack_notification(\n        title=\"❌ Workflow Failed\",\n        message=f\"Workflow `{workflow_id}` failed at step `{step}`\",\n        color=\"#ff0000\",  # red\n        fields=[\n            {\"title\": \"Error\", \"value\": error[:500], \"short\": False},\n            {\"title\": \"Step\", \"value\": step, \"short\": True},\n        ],\n    )\n\n\nasync def notify_workflow_completed(workflow_id: str, duration_sec: float, cost_cents: int):\n    if not settings.notify_on_complete:\n        return\n        \n    await send_slack_notification(\n        title=\"✅ Workflow Completed\",\n        message=f\"Workflow `{workflow_id}` completed successfully\",\n        color=\"#36a64f\",  # green\n        fields=[\n            {\"title\": \"Duration\", \"value\": f\"{duration_sec/60:.1f} min\", \"short\": True},\n            {\"title\": \"Cost\", \"value\": f\"${cost_cents/100:.2f}\", \"short\": True},\n        ],\n    )\n\n\nasync def notify_orphaned_pod(pod_id: str, reason: str):\n    await send_slack_notification(\n        title=\"⚠️ Orphaned Pod Terminated\",\n        message=f\"Recon terminated pod `{pod_id}`\",\n        color=\"#ffcc00\",  # yellow\n        fields=[\n            {\"title\": \"Reason\", \"value\": reason, \"short\": False},\n        ],\n    )\n```\n\n## Discord Support\nDiscord webhooks use similar format but slightly different structure:\n```python\nasync def send_discord_notification(title: str, message: str, color: int):\n    payload = {\n        \"embeds\": [{\n            \"title\": title,\n            \"description\": message,\n            \"color\": color,\n        }]\n    }\n    # ... similar to Slack\n```\n\n## Configuration\n```python\n# app/config.py\nclass Settings:\n    slack_webhook_url: str = Field(default=\"\")\n    discord_webhook_url: str = Field(default=\"\")\n    notify_on_failure: bool = Field(default=True)\n    notify_on_complete: bool = Field(default=False)\n    cost_alert_threshold_cents: int = Field(default=500)  # $5\n```\n\n## Integration Points\n```python\n# In on_failure cleanup\nawait notify_workflow_failed(workflow_id, str(error), failed_step)\n\n# In stitch_final completion\nawait notify_workflow_completed(workflow_id, duration, cost)\n\n# In recon job\nawait notify_orphaned_pod(pod_id, reason)\n```\n\n## Verification\n- [ ] Slack notification sends on failure\n- [ ] Discord notification sends on failure\n- [ ] Completed notification respects setting\n- [ ] Orphaned pod notification works\n- [ ] Invalid webhook URL handled gracefully","status":"open","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:33:30Z","created_by":"Shubham Goel","updated_at":"2026-04-16T10:33:30Z","labels":["alerts","observability","phase-5"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-d72","depends_on_id":"Chatterbox-TTS-Server-4sz","type":"blocks","created_at":"2026-04-16T16:06:27Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-d72","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:06:06Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-7tt","title":"[P5] Add Prometheus metrics endpoint","description":"Expose metrics for monitoring and alerting.\n\n## Metrics to Track\n\n### Workflow Metrics\n- `workflow_started_total` - Counter of workflows started\n- `workflow_completed_total` - Counter of completed workflows\n- `workflow_failed_total` - Counter of failed workflows\n- `workflow_duration_seconds` - Histogram of workflow duration\n- `workflow_active_count` - Gauge of currently running workflows\n\n### GPU Pod Metrics\n- `gpu_pod_created_total` - Counter of pods created\n- `gpu_pod_terminated_total` - Counter of pods terminated\n- `gpu_pod_active_count` - Gauge of active pods\n- `gpu_pod_cost_cents_total` - Counter of total cost\n\n### Step Metrics\n- `step_duration_seconds` - Histogram by step name\n- `step_failed_total` - Counter by step name\n\n## Dependencies\n```toml\nprometheus-client = \"^0.20.0\"\nprometheus-fastapi-instrumentator = \"^6.0.0\"\n```\n\n## Implementation\n```python\n# app/metrics.py\nfrom prometheus_client import Counter, Histogram, Gauge, generate_latest\nfrom prometheus_fastapi_instrumentator import Instrumentator\n\n# Workflow metrics\nworkflow_started = Counter(\n    \"workflow_started_total\",\n    \"Total workflows started\",\n    [\"workflow_type\"]\n)\nworkflow_completed = Counter(\n    \"workflow_completed_total\",\n    \"Total workflows completed\",\n    [\"workflow_type\"]\n)\nworkflow_failed = Counter(\n    \"workflow_failed_total\",\n    \"Total workflows failed\",\n    [\"workflow_type\", \"failed_step\"]\n)\nworkflow_duration = Histogram(\n    \"workflow_duration_seconds\",\n    \"Workflow duration in seconds\",\n    [\"workflow_type\"],\n    buckets=[60, 300, 600, 1800, 3600]  # 1m, 5m, 10m, 30m, 1h\n)\nworkflow_active = Gauge(\n    \"workflow_active_count\",\n    \"Currently running workflows\"\n)\n\n# GPU metrics\ngpu_pod_created = Counter(\n    \"gpu_pod_created_total\",\n    \"Total GPU pods created\",\n    [\"provider\"]\n)\ngpu_pod_active = Gauge(\n    \"gpu_pod_active_count\",\n    \"Currently active GPU pods\",\n    [\"provider\"]\n)\ngpu_pod_cost = Counter(\n    \"gpu_pod_cost_cents_total\",\n    \"Total GPU cost in cents\",\n    [\"provider\"]\n)\n\n# Step metrics\nstep_duration = Histogram(\n    \"step_duration_seconds\",\n    \"Step duration in seconds\",\n    [\"step_name\"],\n    buckets=[10, 60, 300, 600, 1800]\n)\n```\n\n## FastAPI Integration\n```python\n# app/main.py\nfrom prometheus_fastapi_instrumentator import Instrumentator\n\ndef create_app():\n    app = FastAPI()\n    \n    # Auto-instrument HTTP metrics\n    Instrumentator().instrument(app).expose(app, endpoint=\"/metrics\")\n    \n    return app\n```\n\n## Usage in Code\n```python\n# When workflow starts\nworkflow_started.labels(workflow_type=\"content_pipeline\").inc()\nworkflow_active.inc()\n\n# When workflow completes\nworkflow_completed.labels(workflow_type=\"content_pipeline\").inc()\nworkflow_active.dec()\nworkflow_duration.labels(workflow_type=\"content_pipeline\").observe(duration_sec)\n\n# When step completes\nstep_duration.labels(step_name=\"tts_synthesis\").observe(duration_sec)\n```\n\n## Verification\n- [ ] GET /metrics returns Prometheus format\n- [ ] Workflow counters increment correctly\n- [ ] GPU pod metrics track correctly\n- [ ] Duration histograms populate\n- [ ] Can scrape with Prometheus","status":"closed","priority":2,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:33:16Z","created_by":"Shubham Goel","updated_at":"2026-04-16T11:42:50Z","closed_at":"2026-04-16T17:12:50Z","close_reason":"Implemented: app/metrics.py with all required metrics, /metrics endpoint via prometheus_fastapi_instrumentator, deps added to pyproject.toml","labels":["metrics","observability","phase-5"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-7tt","depends_on_id":"Chatterbox-TTS-Server-4sz","type":"blocks","created_at":"2026-04-16T16:06:27Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-7tt","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:06:05Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-1o1","title":"[P5] Add structured logging with structlog","description":"Implement structured JSON logging for production debugging.\n\n## Why Structured Logging\n- Machine-parseable (JSON)\n- Consistent format across services\n- Easy to query in log aggregators\n- Context propagation (request_id, workflow_id)\n\n## Dependencies\n```toml\nstructlog = \"^24.0.0\"\n```\n\n## Configuration\n```python\n# app/logging.py\nimport structlog\nimport logging\nimport sys\n\ndef configure_logging(json_logs: bool = True):\n    '''Configure structlog for the application'''\n    \n    processors = [\n        structlog.contextvars.merge_contextvars,\n        structlog.processors.add_log_level,\n        structlog.processors.TimeStamper(fmt=\"iso\"),\n        structlog.processors.StackInfoRenderer(),\n    ]\n    \n    if json_logs:\n        processors.append(structlog.processors.JSONRenderer())\n    else:\n        # Pretty print for development\n        processors.append(structlog.dev.ConsoleRenderer())\n    \n    structlog.configure(\n        processors=processors,\n        wrapper_class=structlog.make_filtering_bound_logger(logging.INFO),\n        context_class=dict,\n        logger_factory=structlog.PrintLoggerFactory(),\n        cache_logger_on_first_use=True,\n    )\n    \n    # Also configure standard library logging\n    logging.basicConfig(\n        format=\"%(message)s\",\n        stream=sys.stdout,\n        level=logging.INFO,\n    )\n```\n\n## Middleware for Request Context\n```python\n# app/middleware.py\nfrom starlette.middleware.base import BaseHTTPMiddleware\nimport structlog\nimport uuid\n\nclass RequestContextMiddleware(BaseHTTPMiddleware):\n    async def dispatch(self, request, call_next):\n        request_id = request.headers.get(\"X-Request-ID\", str(uuid.uuid4()))\n        \n        structlog.contextvars.clear_contextvars()\n        structlog.contextvars.bind_contextvars(\n            request_id=request_id,\n            path=request.url.path,\n            method=request.method,\n        )\n        \n        response = await call_next(request)\n        response.headers[\"X-Request-ID\"] = request_id\n        return response\n```\n\n## Usage in Code\n```python\n# In any module\nimport structlog\n\nlogger = structlog.get_logger()\n\n# Log with context\nlogger.info(\"workflow_started\", workflow_id=str(wf.id), premise=premise[:100])\nlogger.warning(\"pod_creation_slow\", pod_id=pod.id, wait_time_sec=wait_time)\nlogger.error(\"tts_failed\", job_id=job_id, error=str(e))\n```\n\n## Sample Output\n```json\n{\n  \"event\": \"workflow_started\",\n  \"workflow_id\": \"abc-123\",\n  \"premise\": \"A family moves into...\",\n  \"request_id\": \"req-456\",\n  \"level\": \"info\",\n  \"timestamp\": \"2026-04-16T10:00:00Z\"\n}\n```\n\n## Verification\n- [ ] Logs output as JSON in production mode\n- [ ] Logs output as pretty-printed in dev mode\n- [ ] Request ID propagates through request\n- [ ] Workflow ID appears in all related logs\n- [ ] Stack traces included for errors","status":"closed","priority":2,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:33:06Z","created_by":"Shubham Goel","updated_at":"2026-04-16T11:13:49Z","closed_at":"2026-04-16T16:43:49Z","close_reason":"Completed: Structured logging implemented and verified","labels":["logging","observability","phase-5"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-1o1","depends_on_id":"Chatterbox-TTS-Server-4sz","type":"blocks","created_at":"2026-04-16T16:06:26Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-1o1","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:06:04Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-ysz","title":"[P4] Create new workflow dialog","description":"Build the UI for triggering new workflows.\n\n## Design\n```\n┌────────────────────────────────────────────────────────────────┐\n│  New Content Pipeline                                     [X]  │\n├────────────────────────────────────────────────────────────────┤\n│                                                                │\n│  Story Premise *                                               │\n│  ┌─────────────────────────────────────────────────────────┐   │\n│  │ A family moves into an old Victorian house, unaware     │   │\n│  │ that the previous owners disappeared under mysterious   │   │\n│  │ circumstances. Their teenage daughter begins hearing    │   │\n│  │ whispers from the walls...                              │   │\n│  └─────────────────────────────────────────────────────────┘   │\n│                                                                │\n│  Voice                                                         │\n│  [Gianna ▾] ▶ Preview                                         │\n│                                                                │\n│  Options                                                       │\n│  [✓] Generate Images                                          │\n│  [✓] Stitch Video                                             │\n│                                                                │\n│  Advanced                                                      │\n│  Target Word Count: [5000]                                     │\n│                                                                │\n│  Estimated Cost: ~$1.50  (TTS: $0.80, Images: $0.50, GPU: $0.20)│\n│                                                                │\n│                              [Cancel]  [Start Pipeline]        │\n└────────────────────────────────────────────────────────────────┘\n```\n\n## Components\n\n### Dialog\n```typescript\n// src/components/new-workflow-dialog.tsx\n\"use client\";\n\nimport { Dialog, DialogContent, DialogHeader, DialogTitle } from \"@/components/ui/dialog\";\nimport { Button } from \"@/components/ui/button\";\nimport { Textarea } from \"@/components/ui/textarea\";\nimport { Select } from \"@/components/ui/select\";\nimport { Checkbox } from \"@/components/ui/checkbox\";\n\ninterface NewWorkflowDialogProps {\n  open: boolean;\n  onOpenChange: (open: boolean) =\u003e void;\n  onSubmit: (input: WorkflowInput) =\u003e void;\n}\n\nexport function NewWorkflowDialog({ open, onOpenChange, onSubmit }: NewWorkflowDialogProps) {\n  const [premise, setPremise] = useState(\"\");\n  const [voice, setVoice] = useState(\"Gianna\");\n  const [generateImages, setGenerateImages] = useState(true);\n  const [stitchVideo, setStitchVideo] = useState(true);\n  const [targetWordCount, setTargetWordCount] = useState(5000);\n  const [isSubmitting, setIsSubmitting] = useState(false);\n  \n  const handleSubmit = async () =\u003e {\n    setIsSubmitting(true);\n    try {\n      await onSubmit({\n        workflow_type: \"content_pipeline\",\n        input: {\n          premise,\n          voice_name: voice,\n          generate_images: generateImages,\n          stitch_video: stitchVideo,\n          target_word_count: targetWordCount,\n        },\n      });\n      onOpenChange(false);\n    } finally {\n      setIsSubmitting(false);\n    }\n  };\n  \n  return (\n    \u003cDialog open={open} onOpenChange={onOpenChange}\u003e\n      \u003cDialogContent\u003e\n        \u003cDialogHeader\u003e\n          \u003cDialogTitle\u003eNew Content Pipeline\u003c/DialogTitle\u003e\n        \u003c/DialogHeader\u003e\n        {/* Form fields */}\n      \u003c/DialogContent\u003e\n    \u003c/Dialog\u003e\n  );\n}\n```\n\n### Voice Preview\n```typescript\n// Voice selection with audio preview\nconst { data: voices } = useVoices();\n\n\u003cSelect value={voice} onValueChange={setVoice}\u003e\n  {voices?.map((v) =\u003e (\n    \u003cSelectItem key={v.name} value={v.name}\u003e\n      {v.name}\n    \u003c/SelectItem\u003e\n  ))}\n\u003c/Select\u003e\n\u003cButton variant=\"ghost\" size=\"sm\" onClick={() =\u003e playVoicePreview(voice)}\u003e\n  ▶ Preview\n\u003c/Button\u003e\n```\n\n### Cost Estimate\n```typescript\n// Rough cost estimation based on word count\nfunction estimateCost(wordCount: number, generateImages: boolean) {\n  const ttsMinutes = wordCount / 150;  // ~150 words per minute\n  const ttsCost = ttsMinutes * 0.10;   // $0.10 per minute (rough)\n  const imageCost = generateImages ? (wordCount / 500) * 0.05 : 0;  // ~1 image per 500 words\n  const gpuCost = (ttsMinutes + (generateImages ? 5 : 0)) * 0.01;  // Rough GPU time\n  return { tts: ttsCost, images: imageCost, gpu: gpuCost, total: ttsCost + imageCost + gpuCost };\n}\n```\n\n## Verification\n- [ ] Dialog opens from \"New Workflow\" button\n- [ ] Premise textarea accepts multiline input\n- [ ] Voice dropdown shows available voices\n- [ ] Voice preview plays audio\n- [ ] Options checkboxes work\n- [ ] Cost estimate updates dynamically\n- [ ] Submit triggers workflow and closes dialog\n- [ ] Error handling shows toast on failure","status":"open","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:32:52Z","created_by":"Shubham Goel","updated_at":"2026-04-16T10:32:52Z","labels":["dialog","phase-4","web-ui"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-ysz","depends_on_id":"Chatterbox-TTS-Server-46d","type":"blocks","created_at":"2026-04-16T16:05:28Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-ysz","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:06:00Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-m78","title":"[P4] Create settings page","description":"Build the configuration page for API keys and defaults.\n\n## Design\n```\n┌────────────────────────────────────────────────────────────────┐\n│  Settings                                                      │\n├────────────────────────────────────────────────────────────────┤\n│                                                                │\n│  ┌─ LLM Configuration ─────────────────────────────────────┐   │\n│  │ Provider:    [OpenAI ▾]                                 │   │\n│  │ API Key:     [••••••••••••••••] [Show]                  │   │\n│  │ Model:       [gpt-4o ▾]                                 │   │\n│  │ [Test Connection]                                       │   │\n│  └─────────────────────────────────────────────────────────┘   │\n│                                                                │\n│  ┌─ GPU Provider ──────────────────────────────────────────┐   │\n│  │ Provider:    [RunPod ▾]                                 │   │\n│  │ API Key:     [••••••••••••••••] [Show]                  │   │\n│  │ GPU Type:    [RTX 4090 ▾]                               │   │\n│  │ [Test Connection]                                       │   │\n│  └─────────────────────────────────────────────────────────┘   │\n│                                                                │\n│  ┌─ Default Workflow Settings ─────────────────────────────┐   │\n│  │ Default Voice:       [Gianna ▾]                         │   │\n│  │ Generate Images:     [✓]                                │   │\n│  │ Stitch Video:        [✓]                                │   │\n│  │ Target Word Count:   [5000]                             │   │\n│  └─────────────────────────────────────────────────────────┘   │\n│                                                                │\n│  ┌─ Notifications ─────────────────────────────────────────┐   │\n│  │ Slack Webhook:       [https://hooks.slack.com/...]      │   │\n│  │ Notify on Failure:   [✓]                                │   │\n│  │ Notify on Complete:  [ ]                                │   │\n│  └─────────────────────────────────────────────────────────┘   │\n│                                                                │\n│  [Save Settings]                                               │\n│                                                                │\n└────────────────────────────────────────────────────────────────┘\n```\n\n## Implementation Notes\n\n### Security\n- API keys are stored server-side only\n- Frontend only shows masked values\n- Settings API requires authentication (future)\n\n### API Endpoints\n```python\n# app/routes/settings.py\n@router.get(\"/api/settings\")\nasync def get_settings():\n    '''Get current settings (keys masked)'''\n    return {\n        \"llm\": {\n            \"provider\": settings.llm_provider,\n            \"api_key_set\": bool(settings.openai_api_key),\n            \"model\": settings.llm_model,\n        },\n        \"gpu\": {\n            \"provider\": settings.gpu_provider,\n            \"api_key_set\": bool(settings.runpod_api_key),\n            \"gpu_type\": settings.gpu_type,\n        },\n        \"defaults\": {\n            \"voice\": settings.default_voice,\n            \"generate_images\": settings.default_generate_images,\n            \"stitch_video\": settings.default_stitch_video,\n            \"target_word_count\": settings.default_target_word_count,\n        },\n        \"notifications\": {\n            \"slack_webhook_set\": bool(settings.slack_webhook_url),\n            \"notify_on_failure\": settings.notify_on_failure,\n            \"notify_on_complete\": settings.notify_on_complete,\n        },\n    }\n\n@router.put(\"/api/settings\")\nasync def update_settings(request: UpdateSettingsRequest):\n    '''Update settings (validates and saves)'''\n    # Validate and save to config file or DB\n```\n\n### Test Connection\n```typescript\n// Test LLM connection\nasync function testLlmConnection() {\n  const res = await fetch(\"/api/settings/test-llm\", { method: \"POST\" });\n  const data = await res.json();\n  if (data.success) {\n    toast.success(\"LLM connection successful!\");\n  } else {\n    toast.error(`LLM connection failed: ${data.error}`);\n  }\n}\n```\n\n## Verification\n- [ ] Settings load correctly\n- [ ] API keys show as masked\n- [ ] Test connection works for LLM\n- [ ] Test connection works for GPU provider\n- [ ] Save persists settings\n- [ ] Invalid API key shows error","status":"open","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:32:36Z","created_by":"Shubham Goel","updated_at":"2026-04-16T10:32:36Z","labels":["phase-4","settings","web-ui"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-m78","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:06:00Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-m78","depends_on_id":"Chatterbox-TTS-Server-g1n","type":"blocks","created_at":"2026-04-16T16:05:29Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-mxm","title":"[P4] Add SWR polling for live updates","description":"Implement real-time updates using SWR polling.\n\n## Why Polling (not WebSockets)\n- Simpler implementation\n- Works with any deployment (no sticky sessions)\n- Sufficient for this use case (not real-time chat)\n- Hatchet dashboard uses WebSockets if you need detailed logs\n\n## SWR Configuration\n```typescript\n// src/lib/hooks.ts\nimport useSWR from \"swr\";\n\nconst fetcher = (url: string) =\u003e fetch(url).then((res) =\u003e res.json());\n\n// Workflow list - poll every 5 seconds\nexport function useWorkflows(status?: string) {\n  const params = new URLSearchParams();\n  if (status) params.set(\"status\", status);\n  \n  return useSWR(\n    `/api/workflows?${params}`,\n    fetcher,\n    {\n      refreshInterval: 5000,\n      revalidateOnFocus: true,\n    }\n  );\n}\n\n// Workflow detail - poll every 2 seconds (more frequent for running workflows)\nexport function useWorkflow(id: string) {\n  const { data, error, ...rest } = useSWR(`/api/workflows/${id}`, fetcher, {\n    refreshInterval: (data) =\u003e {\n      // Poll faster for running workflows\n      if (data?.status === \"running\") return 2000;\n      // Slow poll for completed/failed\n      return 10000;\n    },\n    revalidateOnFocus: true,\n  });\n  \n  return { data, error, ...rest };\n}\n\n// GPU pods - poll every 10 seconds\nexport function useGpuPods(status?: string) {\n  const params = new URLSearchParams();\n  if (status) params.set(\"status\", status);\n  \n  return useSWR(\n    `/api/gpu-pods?${params}`,\n    fetcher,\n    { refreshInterval: 10000 }\n  );\n}\n```\n\n## Loading and Error States\n```typescript\n// src/components/data-loader.tsx\ninterface DataLoaderProps\u003cT\u003e {\n  data: T | undefined;\n  error: Error | undefined;\n  isLoading: boolean;\n  children: (data: T) =\u003e React.ReactNode;\n}\n\nexport function DataLoader\u003cT\u003e({ data, error, isLoading, children }: DataLoaderProps\u003cT\u003e) {\n  if (isLoading) return \u003cSkeleton /\u003e;\n  if (error) return \u003cErrorCard error={error} /\u003e;\n  if (!data) return \u003cEmptyState /\u003e;\n  return children(data);\n}\n```\n\n## Optimistic Updates\n```typescript\n// For actions like terminate pod\nasync function handleTerminate(podId: string) {\n  // Optimistically update UI\n  mutate(\n    \"/api/gpu-pods\",\n    (data) =\u003e ({\n      ...data,\n      items: data.items.filter((p) =\u003e p.id !== podId),\n    }),\n    false\n  );\n  \n  // Make API call\n  await terminatePod(podId);\n  \n  // Revalidate\n  mutate(\"/api/gpu-pods\");\n}\n```\n\n## Verification\n- [ ] Workflow list refreshes every 5 seconds\n- [ ] Workflow detail refreshes every 2 seconds when running\n- [ ] GPU pods refresh every 10 seconds\n- [ ] Loading states display correctly\n- [ ] Error states display correctly\n- [ ] Optimistic updates work for terminate","status":"open","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:32:23Z","created_by":"Shubham Goel","updated_at":"2026-04-16T10:32:23Z","labels":["phase-4","polling","web-ui"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-mxm","depends_on_id":"Chatterbox-TTS-Server-46d","type":"blocks","created_at":"2026-04-16T16:05:27Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-mxm","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:59Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-at3","title":"[P4] Create GPU pods page (cost tracking)","description":"Build the GPU pod monitoring page.\n\n## Design\n```\n┌────────────────────────────────────────────────────────────────┐\n│  GPU Pods                                                      │\n├────────────────────────────────────────────────────────────────┤\n│                                                                │\n│  Summary:                                                      │\n│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │\n│  │ Active: 2    │  │ Today: $4.20 │  │ Month: $89   │         │\n│  └──────────────┘  └──────────────┘  └──────────────┘         │\n│                                                                │\n│  Active Pods                                                   │\n│  ┌─────────────────────────────────────────────────────────┐   │\n│  │ pod-abc123    Running    RTX 4090    $0.45/hr           │   │\n│  │ Workflow: abc123   Uptime: 15m   [Terminate]            │   │\n│  ├─────────────────────────────────────────────────────────┤   │\n│  │ pod-def456    Running    RTX 4090    $0.45/hr           │   │\n│  │ Workflow: def456   Uptime: 8m    [Terminate]            │   │\n│  └─────────────────────────────────────────────────────────┘   │\n│                                                                │\n│  Recent Terminated                                             │\n│  ┌─────────────────────────────────────────────────────────┐   │\n│  │ pod-ghi789    Terminated    Total: $0.30    Reason: ok  │   │\n│  │ pod-jkl012    Terminated    Total: $0.15    Reason: fail│   │\n│  └─────────────────────────────────────────────────────────┘   │\n│                                                                │\n└────────────────────────────────────────────────────────────────┘\n```\n\n## API\n```typescript\n// src/lib/api.ts\nexport async function fetchGpuPods(status?: string) {\n  const params = status ? `?status=${status}` : \"\";\n  const res = await fetch(`${API_BASE}/api/gpu-pods${params}`);\n  return res.json();\n}\n\nexport async function terminatePod(podId: string) {\n  const res = await fetch(`${API_BASE}/api/gpu-pods/${podId}`, {\n    method: \"DELETE\",\n  });\n  if (!res.ok) throw new Error(\"Failed to terminate pod\");\n}\n```\n\n## Components\n\n### PodCard\n```typescript\n// src/components/pod-card.tsx\ninterface Pod {\n  id: string;\n  provider: string;\n  workflow_id: string | null;\n  status: string;\n  gpu_type: string;\n  cost_per_hour_cents: number;\n  total_cost_cents: number;\n  created_at: string;\n  terminated_at: string | null;\n}\n\nexport function PodCard({ pod, onTerminate }: { pod: Pod; onTerminate: () =\u003e void }) {\n  // Status badge\n  // Workflow link (if associated)\n  // Cost info\n  // Uptime calculation\n  // Terminate button with confirmation dialog\n}\n```\n\n### CostSummary\n```typescript\n// src/components/cost-summary.tsx\n// Cards showing active pods count, today's cost, monthly cost\n```\n\n## Verification\n- [ ] Active pods listed with details\n- [ ] Cost summary calculates correctly\n- [ ] Terminate button works with confirmation\n- [ ] Recently terminated pods shown\n- [ ] Links to associated workflows work","status":"open","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:32:13Z","created_by":"Shubham Goel","updated_at":"2026-04-16T10:32:13Z","labels":["monitoring","phase-4","web-ui"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-at3","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:58Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-at3","depends_on_id":"Chatterbox-TTS-Server-g1n","type":"blocks","created_at":"2026-04-16T16:05:27Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-8zk","title":"[P4] Create workflow detail page","description":"Build the detailed workflow view showing steps, chunks, and logs.\n\n## Design\n```\n┌────────────────────────────────────────────────────────────────┐\n│  ← Back    Workflow #abc123                    [Cancel] [Retry]│\n├────────────────────────────────────────────────────────────────┤\n│                                                                │\n│  Status: Running          Started: 5 min ago                   │\n│  Premise: \"A family moves into a house with a dark history...\" │\n│                                                                │\n│  ┌─ Steps ─────────────────────────────────────────────────┐   │\n│  │ ✓ generate_story    2m 30s    story_id: xyz             │   │\n│  │ ● tts_synthesis     Running   pod: pod-abc123           │   │\n│  │ ○ image_generation  Pending                             │   │\n│  │ ○ stitch_final      Pending                             │   │\n│  └─────────────────────────────────────────────────────────┘   │\n│                                                                │\n│  ┌─ Chunk Progress (TTS) ──────────────────────────────────┐   │\n│  │ Chunk 1: ✓ \"It was a dark and stormy night...\"  5.2s    │   │\n│  │ Chunk 2: ✓ \"The wind howled through...\"         4.8s    │   │\n│  │ Chunk 3: ● \"Sarah heard a noise...\"             Running │   │\n│  │ Chunk 4: ○ \"She crept down the stairs...\"       Pending │   │\n│  │ ...                                                     │   │\n│  └─────────────────────────────────────────────────────────┘   │\n│                                                                │\n│  ┌─ GPU Pod ───────────────────────────────────────────────┐   │\n│  │ ID: pod-abc123   Status: Running   Cost: $0.45          │   │\n│  │ GPU: RTX 4090    Uptime: 15 min                         │   │\n│  │ [Terminate Pod]                                         │   │\n│  └─────────────────────────────────────────────────────────┘   │\n│                                                                │\n│  [View in Hatchet Dashboard]                                   │\n│                                                                │\n└────────────────────────────────────────────────────────────────┘\n```\n\n## Components\n\n### StepProgress\n```typescript\n// src/components/step-progress.tsx\ninterface Step {\n  name: string;\n  status: \"pending\" | \"running\" | \"completed\" | \"failed\";\n  duration_sec?: number;\n  output?: Record\u003cstring, any\u003e;\n  error?: string;\n}\n\nexport function StepProgress({ steps }: { steps: Step[] }) {\n  // Vertical list with status icons\n  // Duration for completed steps\n  // Error message for failed steps\n  // Output preview (story_id, etc.)\n}\n```\n\n### ChunkProgress\n```typescript\n// src/components/chunk-progress.tsx\ninterface Chunk {\n  index: number;\n  text: string;\n  tts_status: string;\n  tts_duration_sec?: number;\n  image_status: string;\n}\n\nexport function ChunkProgress({ chunks }: { chunks: Chunk[] }) {\n  // Collapsible list of chunks\n  // Status icon per chunk\n  // Preview of text (truncated)\n  // Duration if completed\n}\n```\n\n### PodInfo\n```typescript\n// src/components/pod-info.tsx\n// Shows pod status, cost, uptime\n// Terminate button (with confirmation)\n```\n\n## SWR Hook\n```typescript\nexport function useWorkflow(id: string) {\n  return useSWR(\n    [\"workflow\", id],\n    () =\u003e fetchWorkflow(id),\n    { refreshInterval: 2000 }  // Poll every 2 seconds for detail view\n  );\n}\n```\n\n## Verification\n- [ ] Steps show with correct status\n- [ ] Chunk progress updates in real-time\n- [ ] Pod info displays cost\n- [ ] Link to Hatchet dashboard works\n- [ ] Retry button triggers retry\n- [ ] Cancel button cancels and terminates pod","status":"open","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:32:03Z","created_by":"Shubham Goel","updated_at":"2026-04-16T10:32:03Z","labels":["detail","phase-4","web-ui"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-8zk","depends_on_id":"Chatterbox-TTS-Server-46d","type":"blocks","created_at":"2026-04-16T16:05:26Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-8zk","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:58Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-46d","title":"[P4] Create dashboard home page (workflow list)","description":"Build the main dashboard showing all workflows.\n\n## Design\n```\n┌────────────────────────────────────────────────────────────────┐\n│  Creepy Pasta Dashboard                                        │\n├────────────────────────────────────────────────────────────────┤\n│                                                                │\n│  [+ New Workflow]                    Active Pods: 2  Cost: $3.40│\n│                                                                │\n│  Filter: [All ▾] [Running ▾] [Completed ▾] [Failed ▾]          │\n│                                                                │\n│  ┌─────────────────────────────────────────────────────────┐   │\n│  │ Workflow #abc123   Running      Step 2/4: TTS           │   │\n│  │ \"The Haunted Mirror\" ████████░░░░ 47%                   │   │\n│  │ Started: 5 min ago   GPU: pod-xyz789                    │   │\n│  └─────────────────────────────────────────────────────────┘   │\n│                                                                │\n│  ┌─────────────────────────────────────────────────────────┐   │\n│  │ Workflow #def456   Completed    Duration: 12m           │   │\n│  │ \"Whispers in the Walls\"  Cost: $1.20                    │   │\n│  │ [View Story] [Download Audio] [View Images]             │   │\n│  └─────────────────────────────────────────────────────────┘   │\n└────────────────────────────────────────────────────────────────┘\n```\n\n## Components\n```typescript\n// src/components/workflow-card.tsx\ninterface WorkflowCardProps {\n  workflow: {\n    id: string;\n    status: \"pending\" | \"running\" | \"completed\" | \"failed\";\n    current_step: string | null;\n    input: { premise: string };\n    progress?: { completed_steps: number; total_steps: number };\n    created_at: string;\n    completed_at: string | null;\n    gpu_pod?: { id: string; cost_cents: number };\n  };\n}\n\nexport function WorkflowCard({ workflow }: WorkflowCardProps) {\n  // Status badge color\n  // Progress bar if running\n  // Duration if completed\n  // Error message if failed\n  // Action buttons (view, retry, cancel)\n}\n```\n\n## SWR Hook\n```typescript\n// src/lib/hooks.ts\nexport function useWorkflows(status?: string) {\n  return useSWR(\n    [\"workflows\", status],\n    () =\u003e fetchWorkflows(status),\n    { refreshInterval: 5000 }  // Poll every 5 seconds\n  );\n}\n```\n\n## Page Implementation\n```typescript\n// src/app/workflows/page.tsx\n\"use client\";\n\nexport default function WorkflowsPage() {\n  const [statusFilter, setStatusFilter] = useState\u003cstring\u003e();\n  const { data, error, isLoading } = useWorkflows(statusFilter);\n  \n  // Header with stats and \"New Workflow\" button\n  // Filter tabs\n  // Workflow card list\n}\n```\n\n## Verification\n- [ ] Workflows load and display\n- [ ] Status filter works\n- [ ] Auto-refreshes every 5 seconds\n- [ ] Progress bar shows for running workflows\n- [ ] Click navigates to detail page","status":"open","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:31:48Z","created_by":"Shubham Goel","updated_at":"2026-04-16T10:31:48Z","labels":["dashboard","phase-4","web-ui"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-46d","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:57Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-46d","depends_on_id":"Chatterbox-TTS-Server-g1n","type":"blocks","created_at":"2026-04-16T16:05:25Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"id":"Chatterbox-TTS-Server-g1n","title":"[P4] Set up Next.js + shadcn scaffold","description":"Create the web UI project structure.\n\n## Location\n`services/creepy-brain/web/`\n\n## Stack\n- Next.js 16\n- React 19\n- TypeScript\n- Tailwind CSS\n- shadcn/ui components\n- SWR for data fetching\n\n## Setup Commands\n```bash\ncd services/creepy-brain\nnpx create-next-app@latest web --typescript --tailwind --eslint --app --src-dir\ncd web\nnpx shadcn@latest init\nnpx shadcn@latest add button card badge progress table tabs\n```\n\n## Project Structure\n```\nweb/\n├── src/\n│   ├── app/\n│   │   ├── layout.tsx           # Root layout with nav\n│   │   ├── page.tsx             # Dashboard redirect\n│   │   ├── workflows/\n│   │   │   ├── page.tsx         # Workflow list\n│   │   │   └── [id]/page.tsx    # Workflow detail\n│   │   ├── gpu-pods/\n│   │   │   └── page.tsx         # Pod monitoring\n│   │   └── settings/\n│   │       └── page.tsx         # Configuration\n│   ├── components/\n│   │   └── ui/                  # shadcn components\n│   └── lib/\n│       ├── api.ts               # API client\n│       └── hooks.ts             # SWR hooks\n├── package.json\n├── tailwind.config.ts\n├── next.config.ts\n└── tsconfig.json\n```\n\n## API Client Setup\n```typescript\n// src/lib/api.ts\nconst API_BASE = process.env.NEXT_PUBLIC_API_URL || \"http://localhost:8006\";\n\nexport async function fetchWorkflows(status?: string) {\n  const params = status ? `?status=${status}` : \"\";\n  const res = await fetch(`${API_BASE}/api/workflows${params}`);\n  if (!res.ok) throw new Error(\"Failed to fetch workflows\");\n  return res.json();\n}\n\nexport async function fetchWorkflow(id: string) {\n  const res = await fetch(`${API_BASE}/api/workflows/${id}`);\n  if (!res.ok) throw new Error(\"Failed to fetch workflow\");\n  return res.json();\n}\n```\n\n## Verification\n- [ ] `npm run dev` starts development server\n- [ ] shadcn components render correctly\n- [ ] API client can reach backend\n- [ ] Pages route correctly","status":"open","priority":2,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:31:36Z","created_by":"Shubham Goel","updated_at":"2026-04-16T10:31:36Z","labels":["phase-4","scaffold","web-ui"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-g1n","depends_on_id":"Chatterbox-TTS-Server-4yj","type":"blocks","created_at":"2026-04-16T16:05:25Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-g1n","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:56Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"id":"Chatterbox-TTS-Server-4yj","title":"[P3] Add workflow API endpoints","description":"Create REST API for triggering and monitoring workflows.\n\n## Endpoints\n\n### POST /api/workflows\nTrigger a new workflow.\n\n```python\n@router.post(\"/api/workflows\")\nasync def create_workflow(request: CreateWorkflowRequest):\n    '''Trigger a new content pipeline workflow'''\n    if request.workflow_type != \"content_pipeline\":\n        raise HTTPException(400, \"Unknown workflow type\")\n\n    async with get_session() as session:\n        workflow = await WorkflowService(session).create(\n            workflow_type=request.workflow_type,\n            input_json=request.input.model_dump(),\n        )\n\n    run = await hatchet.client.admin.run_workflow(\n        ContentPipeline,\n        input=request.input.model_dump(),\n        options={\"additional_metadata\": {\"workflow_id\": str(workflow.id)}},\n    )\n\n    async with get_session() as session:\n        await WorkflowService(session).update(\n            workflow.id,\n            hatchet_run_id=run.workflow_run_id,\n            status=\"running\",\n        )\n\n    return WorkflowResponse(id=workflow.id, status=\"running\", created_at=workflow.created_at)\n```\n\n### GET /api/workflows\nList workflows with filtering.\n\n### GET /api/workflows/{id}\nGet detailed workflow status with steps, chunks, and active GPU pods.\n\n```python\nreturn WorkflowDetailResponse(\n    id=workflow.id,\n    status=workflow.status,\n    current_step=workflow.current_step,\n    input=workflow.input_json,\n    steps=[...],    # From workflow_steps table\n    chunks=[...],   # From workflow_chunks table (tts + image progress per chunk)\n    gpu_pods=[...], # All pods for this workflow (tts pod, image pod — separate entries)\n    error=workflow.error,\n    created_at=workflow.created_at,\n    completed_at=workflow.completed_at,\n)\n```\n\nNote: `gpu_pods` is a list because TTS synthesis and image generation run on **separate pods**\nwith separate Docker images. Both are tracked in `gpu_pods` table with `workflow_id`.\n\n### POST /api/workflows/{id}/retry\nRetry a failed workflow from the failed step.\n\n### DELETE /api/workflows/{id}\nCancel a running workflow — terminates all active GPU pods for this workflow.\n\n## Verification\n- [ ] POST /api/workflows triggers workflow\n- [ ] GET /api/workflows lists with filters\n- [ ] GET /api/workflows/{id} shows steps, chunks, and all gpu_pods\n- [ ] Retry works on failed workflow\n- [ ] Cancel terminates all active pods (TTS + image if running) and updates status","status":"closed","priority":2,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:31:25Z","created_by":"Shubham Goel","updated_at":"2026-04-17T22:02:53Z","closed_at":"2026-04-18T03:32:53Z","close_reason":"Implemented in commit 05afe4d — POST/GET/retry/cancel endpoints, schemas/workflow.py, stitch_video guard removed","labels":["api","phase-3"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-4yj","depends_on_id":"Chatterbox-TTS-Server-ea6","type":"blocks","created_at":"2026-04-16T16:05:20Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-4yj","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:53Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"id":"chatterbox-tts-lite-elp","title":"Add CI/CD builds for music-server and sfx-server","description":"Add GitHub Actions workflow to build and push Docker images for the new GPU services.\n\n**Changes:** Add music-server and sfx-server to the existing Docker build matrix in `.github/workflows/` so they build on push to main and push to `ghcr.io/sgoel1220/music-server:main` and `ghcr.io/sgoel1220/sfx-server:main`.","acceptance_criteria":"- [ ] music-server Docker image builds in CI\n- [ ] sfx-server Docker image builds in CI\n- [ ] Images pushed to GHCR on main branch push\n- [ ] Build matrix updated (not duplicated workflows)","status":"closed","priority":3,"issue_type":"task","owner":"codex@local","created_at":"2026-04-23T12:19:09Z","created_by":"Codex","updated_at":"2026-04-23T12:27:30Z","closed_at":"2026-04-23T12:27:30Z","close_reason":"Superseded by detailed phased beads","labels":["audio-design","ci-cd"],"dependencies":[{"issue_id":"chatterbox-tts-lite-elp","depends_on_id":"chatterbox-tts-lite-d03","type":"blocks","created_at":"2026-04-23T12:20:35Z","created_by":"shubham","metadata":"{}"},{"issue_id":"chatterbox-tts-lite-elp","depends_on_id":"chatterbox-tts-lite-fia","type":"blocks","created_at":"2026-04-23T12:20:30Z","created_by":"shubham","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-dpi","title":"Fix mypy Literal type error in stitch.py:242","description":"app/workflows/steps/stitch.py line 242 has a mypy --strict error: \"Incompatible types in assignment (expression has type 'Literal[False]', variable has type 'Literal[True]')\". Pre-existing, unrelated to cost tracking changes.","status":"open","priority":3,"issue_type":"bug","owner":"codex@local","created_at":"2026-04-19T21:23:48Z","created_by":"Codex","updated_at":"2026-04-19T21:23:48Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-x08","title":"Create backend /api/gpu-pods endpoint","description":"Frontend GPU Pods page calls GET /api/gpu-pods and DELETE /api/gpu-pods/:id but neither endpoint exists in the backend. GPU pod data is currently only embedded in WorkflowDetailResponse. Need to create a standalone gpu-pods router with list and terminate endpoints, or adjust frontend to work without them.","status":"open","priority":3,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-18T21:21:12Z","created_by":"Codex","updated_at":"2026-04-18T21:21:12Z","labels":["api","backend"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-262","title":"Add SWR error retry button to workflow and GPU pod pages","description":"When API is down, pages show error text forever with no way to retry. Add a retry button that calls SWR mutate to re-fetch. Apply to workflows/page.tsx and gpu-pods/page.tsx error states.","status":"open","priority":3,"issue_type":"task","owner":"codex@local","created_at":"2026-04-18T21:21:10Z","created_by":"Codex","updated_at":"2026-04-18T21:21:10Z","labels":["frontend","ux"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-o85","title":"Audit remaining tuple-shaped internal data","description":"Typing cleanup removed tuple returns from the reported voice and synthesis paths, but some private helpers still use tuple-shaped internal data, for example text chunking segments and pending chunk queues. Audit these cases against AGENTS.md's Pydantic-over-tuples rule and replace genuinely structured tuples with named Pydantic models where it improves clarity without over-engineering.","status":"closed","priority":3,"issue_type":"task","owner":"codex@local","created_at":"2026-04-18T15:15:25Z","created_by":"Codex","updated_at":"2026-04-18T16:05:42Z","closed_at":"2026-04-18T16:05:42Z","close_reason":"Fixed _segment_text() in chunking.py: removed dead tuple first element (always None), now returns list[str]. Updated consumer loop accordingly. Remaining tuple uses (scheduler, tts pending queue) are algorithmic internals where Pydantic would be over-engineering.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-2fy","title":"Fix pre-existing mypy errors in llm/client.py and gpu/runpod.py","description":"mypy reports 6 errors in files not modified by bead 03n:\\n- app/llm/client.py:42,52: Unused 'type: ignore' comments\\n- app/gpu/runpod.py:83,109,161,176: Returning Any from typed functions\\n\\nThese are pre-existing and should be fixed separately.","status":"closed","priority":3,"issue_type":"bug","owner":"codex@local","created_at":"2026-04-18T13:05:27Z","created_by":"Codex","updated_at":"2026-04-18T15:20:08Z","closed_at":"2026-04-18T15:20:08Z","close_reason":"Fixed AGENTS typing/layering violations and the surfaced mypy issues in llm/client.py and gpu/runpod.py","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-kz3","title":"Add CLEANUP_GPU_POD to StepName enum for DB audit trail","description":"The cleanup_gpu_pod on-failure step has no StepName enum value, so its execution is not tracked in WorkflowStep DB table. If cleanup fails (GPU pod not terminated), there is no audit trail. Either add CLEANUP_GPU_POD to StepName enum or document the gap explicitly.","status":"closed","priority":3,"issue_type":"task","owner":"codex@local","created_at":"2026-04-18T12:50:56Z","created_by":"Codex","updated_at":"2026-04-18T16:04:24Z","closed_at":"2026-04-18T16:04:24Z","close_reason":"Added CLEANUP_GPU_POD to StepName enum. Migration 0010 updates check constraints on workflow_steps.step_name and workflows.current_step to include 'cleanup_gpu_pod'.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-oma","title":"Consolidate EmptyModel into shared workflows/types module","description":"recon.py and test_workflow.py each define their own EmptyModel pydantic class. These should be consolidated into a shared location (e.g. app/workflows/types.py) to avoid silent divergence. Acceptance: single EmptyModel definition imported by both modules.","status":"closed","priority":3,"issue_type":"chore","owner":"codex@local","created_at":"2026-04-18T12:50:35Z","created_by":"Codex","updated_at":"2026-04-18T16:04:22Z","closed_at":"2026-04-18T16:04:22Z","close_reason":"Created app/workflows/types.py with single EmptyModel definition. recon.py, test_workflow.py, main.py, and routes/workflows.py all now import from this shared module.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-4cc","title":"Fix mypy errors in app/gpu/runpod.py","description":"runpod.py has 4 mypy strict errors: 'Returning Any from function declared to return dict[str,object]' at lines 83, 109, 161, 176. Surfaced during engine bead mypy run. Fix by casting the runpod SDK return values to the declared types.","status":"closed","priority":3,"issue_type":"chore","owner":"codex@local","created_at":"2026-04-18T12:32:59Z","created_by":"Codex","updated_at":"2026-04-18T16:04:25Z","closed_at":"2026-04-18T16:04:25Z","close_reason":"Already resolved — app/gpu/runpod.py passes mypy with no errors. All SDK calls already use cast() wrappers.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-k6y","title":"In-process normalization cache useless across Hatchet retries","description":"normalize_text() caches results in a module-level dict keyed by SHA-256 hash. This only helps within the same process invocation. On Hatchet step retry (new task execution), the cache is empty and the LLM call is repeated for the same text.\n\nFix: cache in Redis or Postgres (the text hash + normalized result). Or accept the cost since normalization LLM calls are cheap (~$0.001).\n\nThis is low priority since the cost is minimal, but worth noting for correctness.","status":"open","priority":3,"issue_type":"task","created_at":"2026-04-18T07:35:35Z","updated_at":"2026-04-18T10:24:55Z","labels":["cost","tts"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-db5","title":"Keep ORM defaults and migration server defaults in sync","description":"The initial migration defines server defaults for many columns (`status`, `attempt_number`, `total_tokens_used`, `revision_count`, `is_default`, `total_cost_cents`), but most ORM models only declare Python-side `default=` values. Alembic is also not configured to compare server defaults.\n\nImpact: metadata-created test schemas can differ from migrated schemas, future autogenerate runs may miss or accidentally churn defaults, and direct SQL inserts depend on migration state rather than model metadata.\n\nFix: add matching `server_default=` declarations to ORM columns that intentionally have database defaults, enable or document Alembic `compare_server_default`, and verify a fresh autogenerate after upgrade produces no unintended schema diff.","status":"open","priority":3,"issue_type":"chore","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T22:11:41Z","created_by":"Codex","updated_at":"2026-04-17T22:11:41Z","labels":["alembic","creepy-brain","database","schema"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-qzy","title":"Fix unused type:ignore comments in app/llm/client.py","description":"Pre-existing mypy errors in `app/llm/client.py`:\n- Line 42: Unused \"type: ignore\" comment\n- Line 52: Unused \"type: ignore\" comment\n\nThese were discovered during bead 58x implementation. The type:ignore comments were added when anthropic stubs didn't exist but may now be unnecessary.\n\n## Acceptance Criteria\n- [ ] Remove or update unused type:ignore comments\n- [ ] mypy --strict passes on client.py (with --ignore-missing-imports for anthropic)","status":"closed","priority":3,"issue_type":"chore","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T18:56:57Z","created_by":"Shubham Goel","updated_at":"2026-04-18T16:06:12Z","closed_at":"2026-04-18T16:06:12Z","close_reason":"No type:ignore comments remain in app/llm/client.py — they were removed during prior work. mypy passes cleanly.","labels":["cleanup","creepy-brain","mypy"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-6bu","title":"Add audio loudness normalization to TTS output","description":"Normalize loudness of generated TTS audio chunks to a consistent target (e.g. -23 LUFS / EBU R128) before stitching.\n\n**Why:** Raw TTS output can vary in loudness across chunks, voices, and sessions. Normalization ensures consistent listening volume for the final creepy pasta audio.\n\n**Approach:**\n- Use `pyloudnorm` (same algorithm as Audacity's Loudness Normalization)\n- Apply after each chunk is synthesized, before stitching\n- Target: -23 LUFS (broadcast standard) or -16 LUFS (podcast standard)\n\n**Implementation:**\n```python\nimport pyloudnorm as pyln\nimport soundfile as sf\n\nmeter = pyln.Meter(sample_rate)\nloudness = meter.integrated_loudness(audio_data)\nnormalized = pyln.normalize.loudness(audio_data, loudness, -23.0)\n```\n\nAdd `pyloudnorm` to dependencies in `creepy-brain/pyproject.toml`.\n\n**Acceptance criteria:**\n- All TTS chunks normalized before stitching\n- Final output has consistent loudness regardless of voice or chunk content\n- No clipping introduced\n","status":"open","priority":3,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T18:43:17Z","created_by":"Shubham Goel","updated_at":"2026-04-17T18:43:17Z","labels":["audio","quality"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-mdp","title":"[P1-cleanup] Fix untyped decorator errors in creepy-brain routes","description":"mypy reports \"Untyped decorator makes function untyped [untyped-decorator]\" for all route handlers in app/routes/stories.py, app/routes/runs.py, app/routes/health.py, app/routes/voices.py, and app/routes/blobs.py. This is caused by FastAPI router decorators not being typed. Fix by adding stubs or using typed alternatives.","status":"closed","priority":3,"issue_type":"chore","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T11:13:19Z","created_by":"Shubham Goel","updated_at":"2026-04-18T16:06:13Z","closed_at":"2026-04-18T16:06:13Z","close_reason":"No untyped decorator errors remain in routes — mypy passes with no issues on all 8 route files. FastAPI stubs are present or errors were resolved during prior work.","labels":["creepy-brain","phase1-cleanup","typing"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-wal","title":"Fix pre-existing mypy errors in test_workflow.py","description":"Two unused `# type: ignore` comments in `services/creepy-brain/app/workflows/test_workflow.py` cause mypy strict-mode failures on every run, creating noise that can mask real errors.\\n\\n```\\napp/workflows/test_workflow.py:13: error: Unused \\\"type: ignore\\\" comment  [unused-ignore]\\napp/workflows/test_workflow.py:20: error: Unused \\\"type: ignore\\\" comment  [unused-ignore]\\n```\\n\\n## Fix\\n\\nRemove the two `# type: ignore` comments from `test_workflow.py` lines 13 and 20 (or replace with a narrower suppression if genuinely needed).\\n\\n## Acceptance criteria\\n\\n- [ ] `python3 -m mypy app/workflows/test_workflow.py --ignore-missing-imports` exits 0\\n- [ ] No `[unused-ignore]` errors in CI output","status":"open","priority":3,"issue_type":"chore","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T10:19:58Z","created_by":"Shubham Goel","updated_at":"2026-04-17T10:19:58Z","labels":["creepy-brain","mypy","test"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-dy9","title":"[P2-followup] Workflow step execute() functions return dict[str, object] instead of Pydantic models","description":"**CLAUDE.md violation**: All workflow step `execute()` functions return `dict[str, object]`.\n\n## Affected Files\n- `app/workflows/steps/tts.py` line 64: `-\u003e dict[str, object]`\n- `app/workflows/steps/story.py` line 36: `-\u003e dict[str, object]`\n- `app/workflows/steps/cleanup.py` line 35: `-\u003e dict[str, object]`\n- `app/workflows/steps/image.py` line 11: `-\u003e dict[str, object]`\n- `app/workflows/steps/stitch.py` line 11: `-\u003e dict[str, object]`\n- `app/workflows/test_workflow.py` lines 14, 21: `-\u003e dict[str, object]`\n- `app/workflows/content_pipeline.py` all task functions: `-\u003e dict[str, object]`\n\n## CLAUDE.md Rule\n\u003e **NEVER use `dict` as a return type. ALWAYS create a Pydantic model.**\n\n## Note\nThere ARE Pydantic output schemas defined in `models/schemas.py`:\n- `GenerateStoryStepOutput`\n- `TtsSynthesisStepOutput`\n- `ImageGenerationStepOutput`\n- `StitchFinalStepOutput`\n\nBut the actual functions return raw dicts that are then `.model_dump()`'d in some cases.\n\n## Fix\nChange return types to use the Pydantic models, or create `TypedDict`s if serialization to dict is required by Hatchet:\n```python\nasync def execute(input: WorkflowInputSchema, ctx: Context) -\u003e GenerateStoryStepOutput:\n    ...\n    return GenerateStoryStepOutput(...)\n```\n\nIf Hatchet requires dict, use wrapper:\n```python\nasync def _execute_impl(...) -\u003e GenerateStoryStepOutput:\n    ...\n\nasync def execute(...) -\u003e dict[str, object]:\n    return _execute_impl(...).model_dump()\n```\n\n## Acceptance Criteria\n- [ ] Each step has a typed Pydantic output model\n- [ ] Internal logic uses models, converts to dict only at boundary if needed\n- [ ] mypy --strict passes","status":"open","priority":3,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T10:00:28Z","created_by":"Shubham Goel","updated_at":"2026-04-17T10:22:05Z","labels":["phase-2","pydantic","typing","workflow"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-z2k","title":"[P2-followup] cleanup.py hardcodes runpod_api_key - doesn't work with LocalProvider in DEV_MODE","description":"**DEV_MODE broken**: cleanup step always uses RunPod provider.\n\n## Problem\n`cleanup.py` line 69:\n```python\nprovider = get_provider(settings.runpod_api_key)\n```\n\n`get_provider()` in `app/gpu/__init__.py` only returns `RunPodProvider`:\n```python\ndef get_provider(api_key: str) -\u003e GpuProvider:\n    return RunPodProvider(api_key)\n```\n\nThere's no LocalProvider integration, so:\n1. In DEV_MODE with local GPU, cleanup will fail with invalid RunPod API key\n2. LocalProvider pods won't be cleaned up on workflow failure\n\n## Fix\nThe factory needs to check settings for provider type:\n```python\ndef get_provider() -\u003e GpuProvider:\n    if settings.dev_mode:\n        return LocalProvider(settings.local_tts_url)\n    return RunPodProvider(settings.runpod_api_key)\n```\n\nNote: bead `se6` (closed) was supposed to implement LocalProvider + factory, but the factory still only returns RunPod.\n\n## Acceptance Criteria\n- [ ] `get_provider()` returns appropriate provider based on settings\n- [ ] cleanup.py works in DEV_MODE\n- [ ] No API key required for local dev","status":"closed","priority":3,"issue_type":"bug","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T10:00:14Z","created_by":"Shubham Goel","updated_at":"2026-04-18T10:24:55Z","closed_at":"2026-04-18T15:54:55Z","labels":["gpu","local-dev","phase-2"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-dei","title":"[P2-followup] workflow_service.get_chunks_for_image_step returns dict - violates CLAUDE.md Pydantic rule","description":"**CLAUDE.md violation**: Function returns `list[dict[str, object]]` instead of Pydantic model.\n\n## Location\n`workflow_service.py` lines 130-156:\n```python\nasync def get_chunks_for_image_step(\n    session: AsyncSession,\n    workflow_id: uuid.UUID,\n) -\u003e list[dict[str, object]]:\n    ...\n    return [\n        {\n            \"index\": c.chunk_index,\n            \"text\": c.chunk_text,\n            \"blob_id\": str(c.tts_audio_blob_id) if c.tts_audio_blob_id else None,\n        }\n        for c in chunks\n    ]\n```\n\n## CLAUDE.md Rule\n\u003e **NEVER use `dict` as a return type. ALWAYS create a Pydantic model.**\n\n## Fix\n```python\nclass ChunkForImageStep(BaseModel):\n    index: int\n    text: str\n    blob_id: str | None\n\nasync def get_chunks_for_image_step(...) -\u003e list[ChunkForImageStep]:\n    ...\n```\n\n## Acceptance Criteria\n- [ ] Return type is Pydantic model list\n- [ ] mypy --strict passes","status":"open","priority":3,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T10:00:08Z","created_by":"Shubham Goel","updated_at":"2026-04-17T10:00:08Z","labels":["phase-2","pydantic","typing"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-blb","title":"[P2-followup] tts.py uses private ctx._data.parents API - fragile coupling to Hatchet internals","description":"**Fragile code**: TTS step accesses private Hatchet SDK internals.\n\n## Problem\n`tts.py` line 77:\n```python\nparents: dict[str, Any] = ctx._data.parents\n```\n\nThe comment admits (lines 75-76):\n\u003e ctx._data.parents is a dict[str, Any] keyed by task name; accessing it directly avoids a circular import with content_pipeline.py\n\n## Issues\n1. `_data` prefix indicates private API - not part of public contract\n2. May break with Hatchet SDK updates\n3. Hides a design smell (circular import workaround)\n\n## Fix\nUse public Hatchet API `ctx.task_output()`:\n```python\nstory_output = ctx.task_output(\"generate_story\")\n```\n\nIf this causes circular import, restructure the code:\n- Move task functions to separate module from workflow definition\n- Or use string-based task references\n\n## Acceptance Criteria\n- [ ] Replace `ctx._data.parents` with public API\n- [ ] No circular imports\n- [ ] Works with Hatchet SDK updates","status":"open","priority":3,"issue_type":"chore","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T10:00:03Z","created_by":"Shubham Goel","updated_at":"2026-04-17T10:00:03Z","labels":["hatchet","phase-2","tech-debt","workflow"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-c1y","title":"[P2-followup] Duplicate PodStatus enum definitions (gpu/base.py vs models/enums.py)","description":"Two separate enum definitions for pod status with identical values, violating DRY.\n\n## Locations\n**`app/gpu/base.py`** lines 9-14:\n```python\nclass PodStatus(str, Enum):\n    CREATING = \"creating\"\n    RUNNING = \"running\"\n    READY = \"ready\"\n    TERMINATED = \"terminated\"\n    ERROR = \"error\"\n```\n\n**`app/models/enums.py`** lines 58-65:\n```python\nclass GpuPodStatus(str, Enum):\n    CREATING = \"creating\"\n    RUNNING = \"running\"\n    READY = \"ready\"\n    TERMINATED = \"terminated\"\n    ERROR = \"error\"\n```\n\n## Impact\n- Confusion about which to use\n- Import errors if wrong one is used\n- Potential for drift if one is updated but not the other\n\n## Fix\nKeep only `GpuPodStatus` in `app/models/enums.py` (canonical location for all enums). Update `app/gpu/base.py` to import from there:\n```python\nfrom app.models.enums import GpuPodStatus as PodStatus\n```\n\nOr rename consistently and use one.\n\n## Acceptance Criteria\n- [ ] Single source of truth for pod status enum\n- [ ] All imports updated\n- [ ] mypy passes","status":"open","priority":3,"issue_type":"chore","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T09:59:55Z","created_by":"Shubham Goel","updated_at":"2026-04-17T09:59:55Z","labels":["DRY","gpu","phase-2","typing"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-84y","title":"[P2-followup] RunPodProvider httpx client resource leak - aclose() never called","description":"**Resource leak**: `RunPodProvider` creates an `httpx.AsyncClient` but callers never close it.\n\n## Problem\n`runpod.py` line 65-68:\n```python\nself._client = httpx.AsyncClient(\n    headers={\"Authorization\": f\"Bearer {api_key}\"},\n    timeout=30.0,\n)\n```\n\nThere's an `aclose()` method at line 216-218, but:\n1. `tts.py` creates a provider via `get_provider()` and never calls `aclose()`\n2. `cleanup.py` also creates a provider and never closes it\n3. No context manager protocol (`__aenter__`/`__aexit__`)\n\n## Impact\n- Open file descriptors and connections accumulate\n- Potential connection pool exhaustion in long-running workers\n- Resource warnings in tests\n\n## Fix Options\n1. Add `async with` support (implement `__aenter__`/`__aexit__`)\n2. Change callers to use `try/finally` with `aclose()`\n3. Use `httpx.AsyncClient` per-request instead of instance-level\n\nOption 1 is cleanest:\n```python\nasync def __aenter__(self) -\u003e \"RunPodProvider\":\n    return self\n\nasync def __aexit__(self, *args) -\u003e None:\n    await self.aclose()\n```\n\n## Acceptance Criteria\n- [ ] Provider implements async context manager\n- [ ] All callers use `async with get_provider(...) as provider:`\n- [ ] No resource warnings in tests","status":"open","priority":3,"issue_type":"bug","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T09:59:49Z","created_by":"Shubham Goel","updated_at":"2026-04-17T10:22:06Z","labels":["gpu","phase-2","resource-leak","runpod"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-t64","title":"[P2-followup] Bare asserts in workflow steps (tts.py, test_workflow.py) stripped by python -O","description":"Multiple workflow step files use bare `assert` statements for critical runtime checks. These are stripped when Python runs with `-O` flag.\n\n## Locations\n\n**tts.py**:\n- Line 119: `assert pod.endpoint_url is not None`\n- Line 193-194: `assert session_maker is not None`\n\n**test_workflow.py**:\n- Line 25: `assert isinstance(raw_value, int)`\n\n**cleanup.py**:\n- Line 46: `assert session_maker is not None`\n\n**story.py**:\n- Line 52: `assert session_maker is not None`\n\n## Impact\nRunning `python -O` or `PYTHONOPTIMIZE=1` causes these checks to be silently skipped, leading to:\n- NoneType errors instead of clear assertion messages\n- Silent failures in production\n\n## Fix\nReplace with explicit runtime checks:\n```python\n# BAD\nassert session_maker is not None\n\n# GOOD\nif session_maker is None:\n    raise RuntimeError(\"DB not initialized — call init_db() first\")\n```\n\nNote: bead `hhg` covers the same pattern in `runpod.py`, but these are in different files.\n\n## Acceptance Criteria\n- [ ] All bare `assert` statements in workflow code replaced with explicit checks\n- [ ] Each check has a descriptive error message\n- [ ] Code works correctly with `python -O`","status":"open","priority":3,"issue_type":"bug","owner":"sgoel1220@gmail.com","created_at":"2026-04-17T09:59:38Z","created_by":"Shubham Goel","updated_at":"2026-04-17T10:22:06Z","labels":["assertion","phase-2","workflow"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-0k0","title":"[P2-followup] Add unit tests for GPU providers","description":"There are no tests for `app/gpu/*`. Unit tests would catch the correctness issues already found (port hardcode, created_at lie, status mapping, idempotency behavior, dataclass→model migration).\n\n## Scope\n- Create `services/creepy-brain/tests/gpu/`\n- Mock `httpx.AsyncClient` for RunPod tests (use `respx` or `httpx.MockTransport`)\n- Cover:\n  - `LocalProvider` idempotency + lifecycle (blocked on LocalProvider bead)\n  - `RunPodProvider.create_pod` idempotent replay (existing pod returned)\n  - `_parse_pod` edge cases: missing runtime, terminated pods, multiple ports\n  - `wait_for_ready` timeout path\n- pytest-asyncio already in dev deps","status":"open","priority":3,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T16:07:38Z","created_by":"Shubham Goel","updated_at":"2026-04-16T16:07:38Z","labels":["gpu","phase-2","testing"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-0k0","depends_on_id":"Chatterbox-TTS-Server-se6","type":"blocks","created_at":"2026-04-16T21:37:49Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-0k0","depends_on_id":"Chatterbox-TTS-Server-x7k","type":"discovered-from","created_at":"2026-04-16T21:37:48Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-qr6","title":"[P2-followup] Replace dict[str, object] return types in RunPod client with Pydantic models","description":"`app/gpu/runpod.py::_gql` returns `dict[str, object]` and callers index by key (`data.get(\"podRentInterruptable\")`). This violates the CLAUDE.md rule \"NEVER use `dict` as a return type. ALWAYS create a Pydantic model.\"\n\n## Approach\n- Define Pydantic response models per operation (`CreatePodResponse`, `GetPodResponse`, `ListPodsResponse`, `TerminatePodResponse`)\n- Keep a single typed `_gql(query, variables, response_model: Type[T]) -\u003e T` helper\n- Remove `# type: ignore` / `isinstance(..., dict)` clutter in callers\n- mypy --strict passes on `app/gpu/`\n\nLow priority because the code works — this is type-safety hygiene.","status":"open","priority":3,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T16:07:33Z","created_by":"Shubham Goel","updated_at":"2026-04-16T16:07:33Z","labels":["gpu","phase-2","runpod","typing"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-qr6","depends_on_id":"Chatterbox-TTS-Server-8dn","type":"discovered-from","created_at":"2026-04-16T21:37:47Z","created_by":"shubham","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-qwf","title":"[P2-followup] Test workflow verification gaps: retries, input, DEV_MODE default","description":"The vxb acceptance criteria are not fully met by the current code:\n\n1. **Retry logic untested**: vxb explicitly lists \"Tests retry logic\" as a verification item. `app/workflows/test_workflow.py` declares no `retries=` on any task. Add at least one task that flaps once and succeeds on retry.\n2. **Input not passed**: the trigger endpoint calls `test_workflow.aio_run_no_wait()` with no input, but the bead design example shows `input={\"test\": True}`. The workflow uses `EmptyModel`; either keep it consistent or accept a Pydantic input model.\n3. **DEV_MODE default disables test endpoint**: `DEV_MODE=false` is the default in `.env.example`, so `POST /api/workflows/test` returns 404 out of the box. Verification criteria cannot be satisfied by a fresh checkout without editing `.env`. Either flip the local default to `true` (dev) or document the required env change prominently in README.\n\n## Acceptance criteria\n- [ ] Test workflow demonstrates at least one retry on transient failure (use `retries=1` + deliberate first-attempt failure gated by ctx state, or a dedicated `retry_step`)\n- [ ] Trigger endpoint and workflow agree on input schema\n- [ ] Out-of-the-box `docker compose up` + `POST /api/workflows/test` works (dev default or documented)\n- [ ] README explains the end-to-end smoke test","status":"open","priority":3,"issue_type":"bug","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T16:07:28Z","created_by":"Shubham Goel","updated_at":"2026-04-17T10:22:07Z","labels":["hatchet","phase-2","verification"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-qwf","depends_on_id":"Chatterbox-TTS-Server-vxb","type":"discovered-from","created_at":"2026-04-16T21:37:46Z","created_by":"shubham","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-98k","title":"[P2-followup] Convert GPU dataclasses to Pydantic models","description":"`app/gpu/base.py` defines `GpuPodSpec` and `GpuPod` as `@dataclass`es. CLAUDE.md mandates Pydantic for ALL data structures, including \"Internal data transfer objects\". These are exactly that.\n\n## Changes\n- [ ] `GpuPodSpec` → `BaseModel` with `Field()` defaults\n- [ ] `GpuPod` → `BaseModel` (add `ConfigDict(use_enum_values=False)` as needed)\n- [ ] Update `RunPodProvider._parse_pod` to construct the Pydantic model\n- [ ] Keep `PodStatus` as `StrEnum` / `str, Enum` (already correct)\n- [ ] mypy --strict passes on `app/gpu/`\n\n## Notes\n- `GpuPod.status` is mutated in `wait_for_ready` — if we move to Pydantic, either mark the field mutable (default) or construct a new instance","status":"open","priority":3,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T16:07:07Z","created_by":"Shubham Goel","updated_at":"2026-04-17T10:22:07Z","labels":["gpu","phase-2","typing"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-98k","depends_on_id":"Chatterbox-TTS-Server-se6","type":"related","created_at":"2026-04-16T21:37:48Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-98k","depends_on_id":"Chatterbox-TTS-Server-x7k","type":"discovered-from","created_at":"2026-04-16T21:37:44Z","created_by":"shubham","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-9y3","title":"[P1-cleanup] Add PYTHONUNBUFFERED=1 to Dockerfile","description":"Without `ENV PYTHONUNBUFFERED=1`, Python buffers stdout and logs appear in batches or on shutdown — especially painful when the container crashes and the final log line never flushes.\n\nFix: add `ENV PYTHONUNBUFFERED=1` (and typically `PYTHONDONTWRITEBYTECODE=1`) to the creepy-brain Dockerfile.","acceptance_criteria":"- Dockerfile sets PYTHONUNBUFFERED=1\n- `docker logs` shows logs in real time","status":"open","priority":3,"issue_type":"chore","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:14:09Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:14:09Z","labels":["creepy-brain","infra","phase1-cleanup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-3kg","title":"[P1-cleanup] Delete dead code in app/models/types.py","description":"`app/models/types.py` is not imported anywhere (verified via grep). It was likely superseded by `PydanticType` used elsewhere.\n\nFix: delete the file. Verify with mypy + test suite.","acceptance_criteria":"- `app/models/types.py` removed\n- No imports broken\n- mypy + tests clean","status":"open","priority":3,"issue_type":"chore","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:14:00Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:14:00Z","labels":["creepy-brain","dead-code","phase1-cleanup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-524","title":"[P1-cleanup] Document reason for type: ignore in LLM client","description":"`app/llm/client.py:48` uses `# type: ignore[arg-type]` without a comment. CLAUDE.md requires a specific reason for every `type: ignore`.\n\nFix: add a short inline comment explaining why mypy cannot verify the argument (likely anthropic SDK message type variance).","acceptance_criteria":"- Every `# type: ignore` in client.py has an explanation","status":"open","priority":3,"issue_type":"chore","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:58Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:13:58Z","labels":["creepy-brain","phase1-cleanup","style"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-5y3","title":"[P1-cleanup] Move HealthResponse/ServiceInfo to schemas/common.py","description":"`app/schemas/__init__.py` still defines `HealthResponse` and `ServiceInfo` inline. The package was split into files for other schemas; finish the split.\n\nFix: create `app/schemas/common.py`, move the models there, re-export from `__init__.py`.","acceptance_criteria":"- `__init__.py` contains only re-exports\n- `common.py` holds HealthResponse / ServiceInfo","status":"open","priority":3,"issue_type":"chore","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:41Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:13:41Z","labels":["creepy-brain","phase1-cleanup","refactor"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-44w","title":"[P1-cleanup] Replace side-effect metrics import with explicit init","description":"`main.py:10` uses `import app.metrics as _metrics  # noqa: F401` purely for its side-effects (registering Prometheus metric objects). This pattern is brittle and easy to miss.\n\nFix: define an explicit `init_metrics()` function in `app.metrics` that creates/registers the metrics, and call it from `create_app()` or the lifespan. Keeps side effects intentional and discoverable.","acceptance_criteria":"- No side-effect-only imports in main.py\n- `init_metrics()` (or similar) called explicitly\n- `/metrics` endpoint still works","status":"open","priority":3,"issue_type":"chore","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:38Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:13:38Z","labels":["creepy-brain","phase1-cleanup","style"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-zm4","title":"[P1-cleanup] Unify router import style in main.py","description":"`main.py:65-66` imports `stories_router` inline inside `create_app()` while other routers are imported at module top. Inconsistent and hides the dependency graph.\n\nFix: move `from app.routes.stories import router as stories_router` to the top-level imports block with the others.","acceptance_criteria":"- All router imports are at module top\n- `create_app()` contains only `include_router` calls","status":"open","priority":3,"issue_type":"chore","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T12:13:35Z","created_by":"Shubham Goel","updated_at":"2026-04-16T12:13:35Z","labels":["creepy-brain","phase1-cleanup","style"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-lm4","title":"[P3] Create recon cron job for orphaned pods","description":"Background job to clean up pods that weren't terminated properly.\n\n## Problem\nEdge cases where cleanup doesn't run:\n- Worker crashes during execution\n- Network partition during cleanup\n- Bug in cleanup logic\n\n## Solution\nCron job every 5 minutes that finds and terminates orphaned pods.\n\n## Definition of Orphaned\nA pod is orphaned if ANY of:\n1. Running \u003e 30 min with no active workflow\n2. Associated workflow is completed/failed/cancelled\n3. Created \u003e 2 hours ago (hard limit regardless of workflow status)\n\n## Implementation\n```python\n# app/workflows/recon.py\nfrom . import hatchet\nfrom app.gpu import get_provider\nfrom app.services.workflow_service import WorkflowService\nfrom app.db import get_session\nfrom app.config import settings\nfrom datetime import datetime, timedelta, timezone\nimport structlog\n\nlogger = structlog.get_logger()\n\n@hatchet.cron(\"*/5 * * * *\")  # Every 5 minutes\nasync def recon_orphaned_pods():\n    '''Find and terminate orphaned GPU pods'''\n    provider = get_provider(settings)\n    now = datetime.now(timezone.utc)\n    \n    # Get all active pods from provider\n    active_pods = await provider.list_active_pods()\n    \n    terminated_count = 0\n    for pod in active_pods:\n        should_terminate = False\n        reason = \"\"\n        \n        # Rule 1: Hard limit - 2 hours\n        age_hours = (now - pod.created_at).total_seconds() / 3600\n        if age_hours \u003e 2:\n            should_terminate = True\n            reason = f\"hard_limit_exceeded (age={age_hours:.1f}h)\"\n        \n        # Rule 2: Check associated workflow\n        if not should_terminate:\n            async with get_session() as session:\n                workflow = await WorkflowService(session).get_by_pod_id(pod.id)\n                \n                if not workflow:\n                    # No workflow - pod running \u003e 30 min is orphaned\n                    age_min = (now - pod.created_at).total_seconds() / 60\n                    if age_min \u003e 30:\n                        should_terminate = True\n                        reason = f\"no_workflow (age={age_min:.0f}m)\"\n                elif workflow.status in (\"completed\", \"failed\", \"cancelled\"):\n                    should_terminate = True\n                    reason = f\"workflow_{workflow.status}\"\n        \n        if should_terminate:\n            logger.warning(\n                \"recon: terminating orphaned pod\",\n                pod_id=pod.id,\n                reason=reason,\n                age_hours=age_hours,\n            )\n            try:\n                await provider.terminate_pod(pod.id)\n                terminated_count += 1\n                \n                # Record in database\n                async with get_session() as session:\n                    await session.execute(\n                        update(GpuPod)\n                        .where(GpuPod.id == pod.id)\n                        .values(\n                            status=\"terminated\",\n                            terminated_at=now,\n                            termination_reason=f\"recon: {reason}\",\n                        )\n                    )\n                    await session.commit()\n            except Exception as e:\n                logger.error(\n                    \"recon: failed to terminate pod\",\n                    pod_id=pod.id,\n                    error=str(e),\n                )\n    \n    return {\n        \"checked\": len(active_pods),\n        \"terminated\": terminated_count,\n    }\n```\n\n## Verification\n- [ ] Cron job registered in Hatchet\n- [ ] Runs every 5 minutes\n- [ ] Terminates pods \u003e 2 hours old\n- [ ] Terminates pods with completed/failed workflows\n- [ ] Terminates pods \u003e 30 min with no workflow\n- [ ] Doesn't terminate active workflow pods","status":"closed","priority":3,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:31:16Z","created_by":"Shubham Goel","updated_at":"2026-04-18T10:24:55Z","closed_at":"2026-04-18T15:54:55Z","labels":["cost-control","phase-3","recon","workflow"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-lm4","depends_on_id":"Chatterbox-TTS-Server-cmz","type":"blocks","created_at":"2026-04-16T16:06:25Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-lm4","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:52Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"Chatterbox-TTS-Server-ea6","title":"[P3] Implement stitch_final step","description":"Implement the final stitching step. Pulls WAV chunk blobs from Postgres, stitches locally, encodes to MP3, optionally creates video. Terminates the TTS GPU pod.\n\n## Step Behavior\n\n1. Pull WAV chunk blobs from Postgres (saved by `tts_synthesis` step)\n2. Decode WAV → float32 arrays, concatenate, encode to MP3 once\n3. Save final MP3 blob\n4. If images exist and `stitch_video=True`: create video with ffmpeg\n5. Save final video blob\n6. Terminate TTS GPU pod\n7. Mark workflow complete\n\n## Implementation\n\n```python\n# app/workflows/steps/stitch.py\nimport subprocess\nimport tempfile\nimport io\nimport soundfile as sf\nimport numpy as np\nfrom pathlib import Path\nfrom app.gpu import get_provider\nfrom app.services.blob_service import BlobService\nfrom app.services.workflow_service import WorkflowService\nfrom app.audio.encoding import encode_to_mp3\nfrom app.db import get_session\nfrom app.config import settings\n\nasync def execute(ctx) -\u003e dict:\n    input_data = ctx.workflow_input()\n    tts_output = ctx.step_output(\"tts_synthesis\")\n    image_output = ctx.step_output(\"image_generation\")\n    workflow_id = ctx.workflow_run_id\n\n    async with get_session() as session:\n        blob_service = BlobService(session)\n\n        # 1. Pull WAV blobs and stitch in memory\n        arrays: list[np.ndarray] = []\n        sr: int | None = None\n        for chunk in sorted(tts_output[\"chunks\"], key=lambda c: c[\"index\"]):\n            wav_bytes = await blob_service.get(chunk[\"blob_id\"])\n            audio, chunk_sr = sf.read(io.BytesIO(wav_bytes), dtype=\"float32\")\n            arrays.append(audio)\n            if sr is None:\n                sr = chunk_sr\n\n        stitched = np.concatenate(arrays)\n\n        # 2. Encode to MP3 once\n        mp3_bytes = encode_to_mp3(stitched, sr)\n        final_audio_blob_id = await blob_service.save(\n            workflow_id=workflow_id,\n            blob_type=\"final_audio\",\n            data=mp3_bytes,\n            mime_type=\"audio/mpeg\",\n        )\n\n    result = {\"final_audio_blob_id\": str(final_audio_blob_id)}\n\n    # 3. Create video if images exist\n    if input_data.get(\"stitch_video\", True) and not image_output.get(\"skipped\"):\n        images = image_output.get(\"images\", [])\n        if images:\n            with tempfile.TemporaryDirectory() as tmpdir:\n                tmpdir = Path(tmpdir)\n                async with get_session() as session:\n                    blob_service = BlobService(session)\n                    for img in images:\n                        img_data = await blob_service.get(img[\"blob_id\"])\n                        (tmpdir / f\"image_{img['chunk_index']:03d}.png\").write_bytes(img_data)\n                    mp3_path = tmpdir / \"final.mp3\"\n                    mp3_path.write_bytes(mp3_bytes)\n\n                video_path = tmpdir / \"final_video.mp4\"\n                subprocess.run([\n                    \"ffmpeg\", \"-y\",\n                    \"-framerate\", \"1/5\",\n                    \"-pattern_type\", \"glob\",\n                    \"-i\", str(tmpdir / \"image_*.png\"),\n                    \"-i\", str(mp3_path),\n                    \"-c:v\", \"libx264\", \"-pix_fmt\", \"yuv420p\",\n                    \"-c:a\", \"copy\", \"-shortest\",\n                    str(video_path),\n                ], check=True, capture_output=True)\n\n                async with get_session() as session:\n                    final_video_blob_id = await BlobService(session).save(\n                        workflow_id=workflow_id,\n                        blob_type=\"final_video\",\n                        data=video_path.read_bytes(),\n                        mime_type=\"video/mp4\",\n                    )\n                result[\"final_video_blob_id\"] = str(final_video_blob_id)\n\n    # 4. Terminate TTS GPU pod\n    provider = get_provider(settings)\n    if tts_output.get(\"pod_id\"):\n        await provider.terminate_pod(tts_output[\"pod_id\"])\n\n    # 5. Mark workflow complete\n    async with get_session() as session:\n        await WorkflowService(session).complete(workflow_id=workflow_id, result=result)\n\n    return result\n```\n\n## Notes\n- WAV blobs (from tts_synthesis) are decoded and re-encoded to MP3 exactly once here — no intermediate encoding on the GPU\n- `soundfile` and `numpy` are the only audio deps needed (no GPU, no heavy ML libs)\n- `ffmpeg` must be installed in the creepy-brain container\n- Image pod is already terminated by `image_generation` step — only TTS pod is terminated here\n\n## Verification\n- [ ] WAV chunks pulled from Postgres and stitched in memory\n- [ ] Single MP3 encode (no double-encode quality loss)\n- [ ] Final MP3 blob saved\n- [ ] Video created when images exist and `stitch_video=True`\n- [ ] TTS GPU pod terminated\n- [ ] Workflow marked complete with result blob IDs","status":"closed","priority":3,"issue_type":"task","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:30:53Z","created_by":"Shubham Goel","updated_at":"2026-04-17T21:48:08Z","closed_at":"2026-04-18T03:18:08Z","close_reason":"Implemented in commit e57d669 — stitch.py created with WAV stitching, MP3 encoding, video creation, and pod termination","labels":["phase-3","stitch","workflow"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-ea6","depends_on_id":"Chatterbox-TTS-Server-83y","type":"blocks","created_at":"2026-04-16T16:05:18Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-ea6","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:50Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"Chatterbox-TTS-Server-83y","title":"[P3] Implement image_generation step","description":"Implement the image generation step using **scene-based grouping** (5-10 chunks per scene, one image per scene) instead of per-chunk images.\n\n## Design Change: Scene-Based Images\nInstead of generating one image per chunk, chunks are grouped into scenes (5-10 chunks each) and one background image is generated per scene. This produces natural visual pacing for the video — each background holds for ~30-60s of narration.\n\n## Dependencies\n- `5h4` — chunk-to-scene grouping logic\n- `58x` — scene-level LLM image prompt generation\n\n## GPU Pod Contract (unchanged)\n```\nPOST /generate\nBody: { prompt: str, negative_prompt: str, width: int, height: int }\nResponse: PNG bytes (Content-Type: image/png), HTTP 200 always\n```\n\n## Step Behavior\n1. Check `generate_images` flag — skip if false\n2. Get chunk list from `tts_synthesis` step output\n3. Group chunks into scenes using `group_chunks_into_scenes()`\n4. Spin up **image GPU pod** (separate from TTS pod)\n5. Wait for pod ready\n6. For each scene:\n   - Generate image prompt via `generate_scene_image_prompt(scene.combined_text)`\n   - `POST /generate { prompt, negative_prompt, width: 1280, height: 720 }` → PNG bytes\n   - Save PNG blob to Postgres\n   - Update all chunks in the scene with the same `image_blob_id` and `image_prompt`\n7. Terminate image pod\n8. Return scene image blob IDs with chunk mapping\n\n## Output Schema\n```python\nclass ImageStepOutput(BaseModel):\n    scenes: list[SceneImageResult]\n    pod_id: str\n\nclass SceneImageResult(BaseModel):\n    scene_index: int\n    chunk_indices: list[int]\n    image_blob_id: str\n    image_prompt: str\n```\n\n## Important\n- Image pod uses `settings.image_server_image` (not TTS image)\n- TTS pod from previous step is NOT reused\n- One LLM call + one GPU call per scene (not per chunk) — much cheaper\n- `stitch_final` step must be updated to handle scene-level images (each image holds for the duration of its grouped chunks)\n\n## Verification\n- [ ] Chunks grouped into scenes of 5-10\n- [ ] One image prompt generated per scene via LLM\n- [ ] One POST /generate per scene (not per chunk)\n- [ ] PNG blobs saved per scene\n- [ ] All chunks in a scene share the same image_blob_id\n- [ ] Image pod terminated at end of step\n- [ ] Step skipped cleanly when `generate_images=False`","notes":"Timing: each chunk is ~120-200 words (~1 min narration at 150 wpm). 5-10 chunks per scene = 5-10 min per background image.","status":"closed","priority":3,"issue_type":"task","assignee":"Shubham Goel","owner":"sgoel1220@gmail.com","created_at":"2026-04-16T10:30:39Z","created_by":"Shubham Goel","updated_at":"2026-04-17T20:08:05Z","closed_at":"2026-04-18T01:38:05Z","close_reason":"Completed - implemented image_generation step with scene-based grouping, LLM prompt generation, GPU pod management, PNG validation, and chunk image updates","labels":["images","phase-3","workflow"],"dependencies":[{"issue_id":"Chatterbox-TTS-Server-83y","depends_on_id":"Chatterbox-TTS-Server-58x","type":"blocks","created_at":"2026-04-18T00:17:12Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-83y","depends_on_id":"Chatterbox-TTS-Server-5h4","type":"blocks","created_at":"2026-04-18T00:17:12Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-83y","depends_on_id":"Chatterbox-TTS-Server-duy","type":"blocks","created_at":"2026-04-16T16:05:17Z","created_by":"shubham","metadata":"{}"},{"issue_id":"Chatterbox-TTS-Server-83y","depends_on_id":"Chatterbox-TTS-Server-fme","type":"parent-child","created_at":"2026-04-16T16:05:50Z","created_by":"shubham","metadata":"{}"}],"dependency_count":3,"dependent_count":1,"comment_count":0}
{"id":"chatterbox-tts-lite-9qp","title":"Shared GPU pod for music + SFX models","description":"Optimize GPU cost by running both ACE-Step and EzAudio on a single GPU pod. Requires model swapping or sufficient VRAM for both. Would reduce pod spin-up time and RunPod costs.","status":"open","priority":4,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-23T12:25:25Z","created_by":"Codex","updated_at":"2026-04-23T12:25:25Z","labels":["audio-design","future","optimization"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-61k","title":"SFX timing refinement via forced alignment word timestamps","description":"Use forced alignment (e.g. whisper-timestamped) to get word-level timestamps from TTS audio, then place SFX at the exact moment the relevant word/phrase is spoken rather than at coarse beginning/middle/end positions.","status":"open","priority":4,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-23T12:25:20Z","created_by":"Codex","updated_at":"2026-04-23T12:25:20Z","labels":["audio-design","future"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-mh3","title":"Per-scene music transition styles (crossfade, hard cut, fade-to-silence)","description":"Allow LLM to specify transition style between music scenes. Currently uses simple crossfade; add support for hard cuts, fade-to-silence, and configurable crossfade durations based on narrative mood shifts.","status":"open","priority":4,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-23T12:25:15Z","created_by":"Codex","updated_at":"2026-04-23T12:25:15Z","labels":["audio-design","future"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-nt6","title":"Dynamic audio ducking (raise music during narration pauses)","description":"Instead of static -12dB music bed, implement dynamic ducking that raises music volume during narration pauses and lowers it when speech resumes. Requires voice activity detection on the narration track to identify pause regions.","status":"open","priority":4,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-23T12:25:10Z","created_by":"Codex","updated_at":"2026-04-23T12:25:10Z","labels":["audio-design","future"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"chatterbox-tts-lite-epo","title":"File bead: dynamic audio ducking (future)","description":"Enhance audio mixing to dynamically duck music volume during narration speech and raise it during pauses. Requires silence/speech detection in the narration WAV.\n\nThis is a follow-up to the static -12dB mixing in the initial audio design implementation.","acceptance_criteria":"- [ ] Detect speech vs silence segments in narration audio\n- [ ] Music volume raises during pauses (to configurable level)\n- [ ] Music volume lowers during speech (to configurable level)\n- [ ] Smooth transitions (no abrupt volume jumps)\n- [ ] Configurable attack/release times","status":"closed","priority":4,"issue_type":"feature","owner":"codex@local","created_at":"2026-04-23T12:19:17Z","created_by":"Codex","updated_at":"2026-04-23T12:27:51Z","closed_at":"2026-04-23T12:27:51Z","close_reason":"Superseded by chatterbox-tts-lite-nt6","labels":["audio-design","enhancement"],"dependencies":[{"issue_id":"chatterbox-tts-lite-epo","depends_on_id":"chatterbox-tts-lite-bw9","type":"blocks","created_at":"2026-04-23T12:20:40Z","created_by":"shubham","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
