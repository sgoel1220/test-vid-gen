# requirements.txt for the lite RunPod image
# NVIDIA / CUDA 12.1 runtime dependencies for lite_clone_server.py

# Use PyTorch index for CUDA 12.1 wheels
--extra-index-url https://download.pytorch.org/whl/cu121

# Pin CUDA-specific builds — unpinned names resolve to CPU-only wheels from PyPI
torch==2.5.1+cu121
torchaudio==2.5.1+cu121
torchvision==0.20.1+cu121

conformer==0.3.2
# diffusers installed from source in Dockerfile for ZImagePipeline support
# (pinned to 29b9109 — last commit before autoencoder_rae.py required transformers>=4.48)
transformers==4.46.3
einops>=0.7.0,<1.0
omegaconf==2.3.0
resampy==0.4.3
resemble-perth==1.0.1

fastapi>=0.100.0,<0.116.0
uvicorn[standard]>=0.27.0,<1.0

numpy>=1.24.0,<2.0        # numpy 2.x breaks resampy, conformer, audiotsm, praat-parselmouth
soundfile>=0.12.0,<0.13.0
librosa>=0.10.0,<0.11.0
safetensors>=0.4.0,<0.5.0
descript-audio-codec==1.0.0

PyYAML>=6.0
python-multipart>=0.0.5
requests>=2.28.0
watchdog>=3.0.0
aiofiles>=23.0.0
unidecode>=1.3.0
inflect>=7.0.0
tqdm>=4.65.0
hf_transfer>=0.1.4

# persistence layer
httpx>=0.27.0
tenacity>=8.2.0
pydantic>=2.9
pydantic-settings>=2.5
aiosqlite>=0.20.0

# type checking
mypy>=1.10

accelerate>=0.26.0
sentencepiece>=0.2.0
Pillow>=10.0.0
pydub>=0.25.0
audiotsm>=0.1.2
praat-parselmouth>=0.4.0
