# Minimal requirements for stateless TTS synthesis
# PyTorch is pre-installed in base image (pytorch/pytorch:2.5.1-cuda12.1-cudnn9-runtime)

# Audio (torchaudio not in base image)
torchaudio==2.5.1

# FastAPI server
fastapi>=0.100.0,<0.116.0
uvicorn[standard]>=0.27.0,<1.0

# Audio I/O
numpy>=1.24.0,<2.0
soundfile>=0.12.0,<0.13.0

# Chatterbox dependencies
conformer==0.3.2
einops>=0.7.0,<1.0
omegaconf==2.3.0
resampy==0.4.3
resemble-perth==1.0.1
safetensors>=0.4.0,<0.5.0
descript-audio-codec==1.0.0

# HuggingFace for model download
huggingface_hub>=0.20.0
hf_transfer>=0.1.4

# Pydantic for request validation
pydantic>=2.9
