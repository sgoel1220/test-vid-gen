# Creepy Brain Service - Environment Variables
# Copy this file to .env and fill in your values

# Server
HOST=0.0.0.0
PORT=8006
RELOAD=false

# Logging: false for pretty dev logs, true for production JSON
JSON_LOGS=false

# PostgreSQL
# When using docker-compose: POSTGRES_HOST=localhost, POSTGRES_PORT=5433 (mapped host port)
# When running inside Docker network: POSTGRES_HOST=postgres, POSTGRES_PORT=5432
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_USER=creepy_brain
POSTGRES_PASSWORD=dev_password
POSTGRES_DB=creepy_brain

# LLM (Anthropic)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
LLM_MODEL=claude-opus-4-6

# Hatchet Workflow Engine
# Create a token in the Hatchet dashboard (http://localhost:8080) after first run
HATCHET_CLIENT_TOKEN=your_hatchet_token_here
# Auth cookie secret — must be 16 or 32 chars; override the dev default in production
HATCHET_COOKIE_SECRETS=devtokensecret16

# Concurrency
MAX_CONCURRENT_GENERATIONS=2
